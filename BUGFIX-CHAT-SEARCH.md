# اصلاح جست‌وجوی کاربران پیام‌رسان

تاریخ: ۲۰۲۶-۰۹-۰۷

مبنای اصلاح: شاخهٔ `main` مخزن TotallCollectionSoftware، نسخهٔ `07a9eb922589f96b379e88972c9ef1df4a65502e`.
این اصلاح در سورسِ فضای کاری انجام شده است؛ نصب عملیاتی یا مخزن GitHub تغییر داده نشده‌اند.

## ایرادهای بازتولیدشده

1. **جست‌وجوی اصلی فقط گفتگوهای موجود را می‌گشت.** همکارِ بدون گفتگوی قبلی هیچ‌وقت در این کادر پیدا نمی‌شد.
2. **جداول چت در زنجیرهٔ مایگریشن EF ثبت نشده بودند.** اسکریپت دستی SQL وجود داشت، اما `Database.Migrate()` آن را اجرا نمی‌کرد. در SQLite قدیمی نیز `EnsureCreated()` جدول جدید اضافه نمی‌کند. روی دیتابیس آزمایشیِ فاقد جدول چت، `/api/chat/users` به‌جای فهرست کاربران خطای HTTP 400 با پیام `no such table: ChatMembers` برمی‌گرداند.
3. **جست‌وجوی نام کامل و فارسی یکدست نبود.** API نام و نام خانوادگی را جدا مقایسه می‌کرد؛ «علی کریمی» با «علي كريمي» تطبیق نداشت. فیلتر محلی پنجرهٔ گفتگو نیز هنگام نبود نتیجه، اشتباهاً تمام کاربران را نمایش می‌داد.
4. **خطای دریافت مخاطبان در پنجرهٔ گفتگو وضعیت مشخص و امکان تلاش مجدد نداشت.**

## تغییرات

- نمایش بخش «کاربران سازمان — شروع گفتگو» در نتایج جست‌وجوی اصلی، مستقل از وجود گفتگوی قبلی.
- اتصال جست‌وجوی پنجرهٔ گفتگوی جدید و انتخاب اعضای گروه به `api/chat/users?search=...`.
- پشتیبانی از نام کامل، نام کاربری و واحد سازمانی؛ یکسان‌سازی «ی/ي/ى»، «ک/ك»، ارقام فارسی/عربی، حروف بزرگ و کوچک انگلیسی، فاصله، نیم‌فاصله و اعراب، فقط هنگام مقایسه. متن ذخیره‌شده تغییر نمی‌کند.
- تأخیر کوتاه ۲۵۰ میلی‌ثانیه‌ای هنگام تایپ و نادیده گرفتن پاسخ‌های دیرهنگامِ جست‌وجوهای قبلی.
- نمایش جداگانهٔ بارگذاری، نتیجهٔ خالی و خطای ارتباط؛ دکمهٔ «تلاش مجدد» در هر دو مسیر.
- ثبت مایگریشن `20260907072032_AddChatModule` و به‌روزرسانی snapshot. ساخت جداول/ایندکس‌های SQL Server شرطی است تا نصب‌هایی که اسکریپت دستی را قبلاً اجرا کرده‌اند حفظ شوند.
- تکمیل جداول مفقود چت در SQLite قدیمی، بدون حذف یا بازنویسی کاربران، اعضا و پیام‌ها.

**منبع هویت مخاطبان:** حساب‌های ورود فعال در جدول `Users`. خود کاربر و حساب‌های غیرفعال نمایش داده نمی‌شوند؛ آنلاین بودن شرط نیست. واحد سازمانی، در صورت وجود پروفایل متناظر، با **نام کاربری** از `SystemUsers` خوانده می‌شود، نه با شناسهٔ عددی آن جدول. رکوردِ صرفاً پرسنلی بدون حساب ورود، مخاطب قابل‌استفادهٔ پیام‌رسان نیست.

## نتیجهٔ آزمون‌ها

- ساخت کامل Solution در Release: موفق، صفر خطا؛ همان ۱۶ هشدار قبلیِ بخش‌های دیگر باقی است.
- ۲۹ آزمون خودکار جدید: موفق؛ شامل جست‌وجو، هویت صحیح مخاطب، حذف خود/غیرفعال از نتایج، عدم افشای شناسهٔ گفتگوهای دیگران، ارتقای SQLite قدیمی و حفظ پیام‌ها در اجرای مجدد.
- ۳۱ بررسیِ اسکریپت جامع قبلی پیام‌رسان: موفق؛ شامل چت خصوصی، گروه، پیام، خواندن، پاسخ، واکنش، پیوست و آزمون‌های دسترسی موجود.
- ۱۱ سناریوی Chromium روی کلاینت Publish‌شده و API واقعی با SQLite آزمایشی: موفق؛ شامل پیدا کردن کاربر بدون گفتگوی قبلی، شروع چت، جست‌وجوی فارسی، خطا و تلاش مجدد، پاسخ دیرهنگام و انتخاب اعضای گروه.
- مشکل دیتابیس قدیمی روی یک فایل SQLite آزمایشی قبل از اصلاح بازتولید و پس از راه‌اندازی نسخهٔ جدید برطرف شد.

**محدودیت اعتبارسنجی:** SQL Server واقعی در این محیط اجرا نشده است. تولید اسکریپت مایگریشنِ شرطی و تطابق مدل/snapshot با provider SQL Server بررسی شده‌اند؛ آزمون اجرایی دیتابیس با SQLite انجام شده است. این آزمون‌ها جای بررسی روی نسخهٔ پشتیبانِ دیتابیس عملیاتی را نمی‌گیرند.

## اعمال در نصب فعلی

1. پیش از انتشار، از دیتابیس و فایل‌های آپلودی/رمزنگاری‌شده نسخهٔ پشتیبان بگیرید و ترجیحاً ابتدا روی محیط آزمایشی اجرا کنید.
2. **هر دو پروژهٔ Client و API را دوباره Publish کنید**؛ تعویض فایل Razor یا انتشار فقط یکی از آن‌ها کافی نیست. ترتیب انتشار تک‌سروره در `DEPLOY.md` موجود است.
3. تنظیمات اتصال فعلی، فایل‌های `uploads` و `SecureFiles` و کلیدهای رمزنگاری نصب خودتان را حفظ کنید؛ با تنظیمات یا فایل‌های محیط آزمایشی جایگزین نکنید.
4. با راه‌اندازی API جدید، مایگریشن SQL Server یا تکمیل SQLite اجرا می‌شود. حساب اتصال دیتابیس باید مجوز لازم برای اعمال تغییرات شِما را داشته باشد. اسکریپت مستقل SQL Server نیز در `inventory/sql/chat/AddChatModule.sql` قرار دارد.
5. پس از انتشار، همهٔ تب‌های برنامه را ببندید و دوباره باز کنید تا نسخهٔ جدید PWA فعال شود؛ در صورت باقی ماندن نسخهٔ قبلی، کش برنامه را پاک و دوباره وارد شوید.
6. در کادر اصلی پیام‌رسان یا «گفتگوی جدید»، نام کامل یا نام کاربری یک **حساب ورود فعال دیگر** را جست‌وجو کنید.

مایگریشن جدید در مسیر ارتقا هیچ جدولی را حذف نمی‌کند. بازگردانی خودکار آن (`Down`) عمداً مسدود است، زیرا ممکن است جداول موجودِ دارای پیام را از نصب دستی تحویل گرفته باشد؛ بازگردانی باید با برنامهٔ پشتیبان‌گیری بررسی‌شده انجام شود.

بستهٔ تحویلی فقط سورس و آزمون‌هاست؛ SDK، وابستگی‌های دانلودشده، خروجی‌های `bin/obj`، دیتابیس آزمایشی و کلیدهای تولیدشدهٔ محیط تست در آن نیستند.
