# گزارش تست یکپارچه — ماژول آرشیو اسناد و مدارک

**تاریخ:** ۱۴۰۵/۰۶/۱۵ (2026-09-06) · **کامیت پایه:** `a3a3f55`
**نتیجه کلی:** ۱۳۲ تست اجرا شد — **۱۳۲ موفق، ۰ ناموفق**

---

## روش

سناریوی یک شرکت واقعی که بایگانی ISO و قراردادهایش را دیجیتال می‌کند، با **چهار کاربر
دارای نقش‌های متفاوت** شبیه‌سازی شد. تست‌ها مستقیماً روی API در حال اجرا
(SQLite، پورت ۵۱۰۰) زده شدند، نه با mock.

| کاربر | نقش | پرمیشن‌ها |
|---|---|---|
| `qa_manager` | مدیر تضمین کیفیت | Read, Create, Delete, **Manage** |
| `qa_author` | نویسنده مدرک | Read, Create |
| `qa_review` | بازرس | Read, Create |
| `qa_guest` | مهمان | Read |

---

## پوشش تست

### دور اول — جریان کاری اصلی (۷۲ تست)

| بخش | موارد بررسی‌شده |
|---|---|
| **درخت پوشه** | ساخت سه سطح تودرتو · ارث‌بری بازگشتی دسترسی · نادیدنی بودن برای کاربر بی‌دسترسی |
| **ساخت مدرک** | همه فیلدها · رد کد تکراری · رد کد خالی · رد کاربر بدون مجوز · ساخت خودکار ورژن ۱ · کپی نفرات تایید |
| **لینک مدارک** | لینک چندتایی · ساخت خودکار لینک معکوس · رد لینک تکراری |
| **پیوست و امنیت** | آپلود · مسدود بودن دانلود و پیش‌نمایش بدون توکن (۴۰۱) · رد کاربر بی‌ربط (۴۰۳) · پیش‌نمایش برای «فقط مشاهده» · مسدود بودن دانلود او |
| **گردش تایید** | فریز خودکار · مسدود شدن ورژن جدید حین گردش · رسیدن کار به کارتابل همه تاییدکنندگان · عدم فعال‌سازی با تایید ناقص · فعال‌سازی خودکار پس از تایید کامل · سیاست تک‌ورژن · بسته شدن کار کارتابل |
| **رد گردش** | یک رد ⇒ کل ورژن Rejected · باز شدن فریز · دست‌نخوردن ورژن فعال قبلی |
| **کارتابل** | اعلان ورژن جدید · کار «به‌روزرسانی مدرک مرتبط» · بستن دستی |
| **چندورژن** | دو ورژن هم‌زمان فعال با `AllowMultipleActiveVersions` |
| **هشدار انقضا** | خلاصه · اجرای دستی · ساخت کار کارتابل · عدم تکرار در اجرای دوم · رد کاربر بدون Manage · فیلتر «رو به انقضا» |
| **فعال/غیرفعال** | غیرفعال‌سازی · مسدود شدن ورژن جدید و لینک · حذف از لیست پیش‌فرض · دیده شدن در زیرمنو · آزاد شدن پس از فعال‌سازی |
| **سطل بازیافت** | انتقال · نمایش حذف‌کننده · عدم دسترسی به پیوست مدرکِ حذف‌شده · بازگشت دسترسی پس از restore · حذف قطعی فقط با Manage |
| **تاریخچه** | ثبت هر ۷ نوع رویداد: Create, NewVersion, Approved, Link, Permissions, Delete, Restore |
| **جستجو** | کد مشتری · عنوان · lookups |

### دور دوم — سناریوهای مرزی و تداخل فازها (۳۲ تست)

| بخش | موارد بررسی‌شده |
|---|---|
| **تداخل انقضا × غیرفعال** | بسته شدن هشدارهای باز هنگام غیرفعال‌سازی · حذف از فیلتر انقضا · حفظ سابقه هشدار پس از فعال‌سازی مجدد |
| **تداخل انقضا × سطل** | مدرک داخل سطل هشدار نمی‌گیرد و در بج‌ها شمرده نمی‌شود |
| **گردش مرزی** | رد تایید توسط غیرتاییدکننده · رد تایید دوباره · مجاز بودن حذف پس از اتمام گردش · **مسدود بودن حذف و غیرفعال‌سازی حین گردش** |
| **دسترسی در لحظه** | نامرئی بودن قبل از دسترسی‌دهی · اثر فوری اعطا و **سلب** دسترسی · ناکافی بودن سطح «خواندن» برای ویرایش · کارکرد «نمایش برای همه» |
| **یکپارچگی پس از حذف قطعی** | پاک شدن لینک معکوس · آزاد شدن کد مدرک |
| **صحت آمار** | `status=all` = فعال + غیرفعال · عدم همپوشانی مجموعه‌ها · تطابق شمارنده بج‌ها با لیست · محدود بودن آمار به دید هر کاربر |
| **جستجو** | فارسی · انگلیسی · کد مشتری · بی‌نتیجه · ورودی شبیه SQL injection |

### دور سوم — مقایسه دو ورژن (۲۸ تست)

| بخش | موارد بررسی‌شده |
|---|---|
| **صحت خروجی** | ترتیب ورژن‌ها · تشخیص تغییر عنوان، انقضا و شرح · علامت `same` برای فیلد یکسان |
| **نفرات گردش** | تشخیص تاییدکننده افزوده‌شده |
| **پیوست‌ها** | مشترک ⇒ same · فقط در یکی ⇒ added/removed |
| **شمارش** | همخوانی `changeCount` با مجموع ردیف‌های متفاوت |
| **ترتیب معکوس** | `from`/`to` جابه‌جا ⇒ همان نتیجه |
| **خطاها** | مقایسه با خودش (۴۰۰) · ورژن ناموجود (۴۰۴) · ورژن مدرک دیگر (۴۰۴) · بدون توکن (۴۰۱) · بدون دسترسی (۴۰۳) |
| **تعامل با فازها** | مدرک داخل سطل بازیافت ⇒ ۴۰۴ و پس از بازگردانی ⇒ ۲۰۰ · سطح View کافی نیست، Read کافی است |

### دور چهارم — بازرسی مستقیم دیتابیس

```
مدارک: 8   ورژن‌ها: 13

رکوردهای یتیم ......................... هیچ ✔  (۸ جدول وابسته + پیوست‌ها)
مدرک حذف‌شده با کار باز کارتابل ........ ندارد ✔
مدرک غیرفعال با کار باز ............... ندارد ✔
سیاست تک‌ورژن فعال .................... رعایت شده ✔
فریز فقط روی ورژن در گردش ............. سازگار ✔
یکتایی کد مدرک ........................ برقرار ✔
هشدار انقضای تکراری ................... ندارد ✔
```

**توزیع رویدادهای تاریخچه:** Create 8 · NewVersion 5 · Permissions 4 · Approve 4 ·
RelatedUpdate 3 · Link 3 · Delete 3 · Approved 3 · Restore 2 · ExpiryAlert 2 ·
Deactivate 2 · Activate 2 · Reject 1

---

## یافته‌ها

**هیچ باگی پیدا نشد.** پنج فاز توسعه با هم تداخل ندارند و قوانین هر فاز در حضور
فازهای دیگر هم برقرار می‌ماند.

یک رفتار که ابتدا خطا به‌نظر می‌رسید ولی **درست** است: اگر مدرک با «نفرات گردش»
ساخته شود، ورژن ۱ بلافاصله وارد گردش تایید می‌شود و تا تعیین تکلیف آن، ثبت
ورژن جدید ممکن نیست. این همان رفتار مورد انتظار فریز است.

### نکات باقی‌مانده (نه باگ، تصمیم طراحی)

1. **کد مدرک پس از حذف نرم آزاد نمی‌شود** — ایندکس یکتا شامل مدارک داخل سطل بازیافت
   هم هست. برای آزادسازی باید مدرک حذف قطعی شود. اگر رفتار دیگری لازم است،
   ایندکس را می‌توان فیلتردار (`WHERE IsDeleted = 0`) کرد.
2. **پیوست سایر ماژول‌ها** (پروژه‌ها، نامه‌ها) همچنان برای هر کاربر واردشده باز است.
   `AttachmentGuard` آماده است؛ برای محدود کردن کافی است نام ماژول به `Protected`
   اضافه و قانونش نوشته شود.

---

## اجرای مجدد

```bash
# سرور با دیتابیس تمیز
cd inventory/src/Inventory.Api
Database__Provider=Sqlite ConnectionStrings__Default="Data Source=/tmp/e2e.db" \
  dotnet run --urls http://0.0.0.0:5100

# تست‌ها
python3 run.py      # جریان کاری اصلی   (۷۲)
python3 edge.py     # سناریوهای مرزی    (۳۲)
python3 compare.py  # مقایسه دو ورژن    (۲۸)
```

اسکریپت‌ها در `tests/doc-archive/` قرار دارند.
