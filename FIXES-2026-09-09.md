# اصلاح خطاهای کامپایل و ReadOnlySpan — ۲۰۲۶/۰۹/۰۹

اصلاحات ابتدا بر پایهٔ سورس `e408ad1575adec374cc89c5ef1a89ff800d2f591` آماده و آزمایش شدند و سپس روی main با مبنای `8265f4c92de5d082ee4610b5d96bc74ec3816acc` ادغام شدند. تغییرات جدید مخزن، از جمله گزارش آرشیو و اصلاح قبلی Popover، حفظ شده‌اند. هیچ تغییری در دیتابیس، migrationها، اطلاعات کاربران، رمزها یا بسته‌های NuGet داده نشده است.

## تغییرات

### ۱. FixedStyle و CalendarStyle

فایل: `inventory/src/Inventory.Client/Extensions/Popover.cs`

اتصال دو interpolated string با `+` نتیجه‌ای از نوع `string` می‌سازد، درحالی‌که `FormattableString.Invariant` ورودی `FormattableString` می‌خواهد. خطای `CS1503` با نسخهٔ قبلی بازتولید شد.

در نسخهٔ آزمایش‌شده از یک interpolated string واحد استفاده شد. هنگام ادغام مشخص شد main در commit `9262038` همین خطا را با جداکردن فراخوانی‌های `Invariant` و انتقال اتصال رشته‌ها به خارج از آن‌ها اصلاح کرده است؛ همان پیاده‌سازی main بدون بازنویسی حفظ شد. هر دو روش خروجی CSS یکسان دارند. قالب عددی invariant، مختصات، z-index، محدودیت‌های viewport و ارتفاع اختیاری منو حفظ شده‌اند.

### ۲. خطای Reverse().ToArray() در تولید آدرس IP

فایل‌ها:

- `inventory/src/Inventory.Api/Controllers/Cctv/NetworkScanController.cs` — `BuildFromCidr`
- `inventory/src/Inventory.Api/Controllers/Cctv/CctvScanController.cs` — `BuildTargets`

با کامپایلر C# 14، فراخوانی extension روی آرایه می‌تواند به `MemoryExtensions.Reverse(Span<T>)` متصل شود. این متد آرایه را درجا برعکس می‌کند و `void` برمی‌گرداند؛ در نتیجه `.ToArray()` بعدی خطای `CS0023` می‌دهد. این خطا با SDK 10.0.401 بازتولید شد.

تبدیل IP بازنویسی شد: چهار بایت آدرس مستقیماً از عدد unsigned با ترتیب network byte order ساخته می‌شوند. وابستگی به `BitConverter`، ترتیب بایت ماشین و overload متد Reverse حذف شد. منطق قبلی انتخاب رنج، fallback به /24 و افزودن IP محلی تغییر نکرده است؛ این اصلاح، بازطراحی محدودیت تعداد مقصدها یا سیاست اسکن نیست.

### ۳. TypeLoadException مربوط به ReadOnlySpan<string>

فایل‌ها:

- `global.json` — جدید، انتخاب SDK خانوادهٔ .NET 8
- `inventory/Directory.Build.props` — `LangVersion=12.0`
- `inventory/src/Inventory.Api/Inventory.Api.csproj` — حذف override قبلی `latest`
- `inventory/src/Inventory.Shared/Inventory.Shared.csproj` — حذف override قبلی `latest`

پروژه `net8.0` است، اما `LangVersion=latest` باعث می‌شد نصب SDK جدیدتر روی ماشین، قواعد انتخاب overload را تغییر دهد. نمونهٔ زیر با C# 14 در expression tree می‌تواند تبدیل ضمنی آرایه به ReadOnlySpan ایجاد کند:

```csharp
string[] roles = ["Admin", "Operator"];
Expression<Func<string, bool>> expression = value => roles.Contains(value);
var predicate = expression.Compile(preferInterpretation: true);
```

با کامپایل SDK 10.0.401 و اجرای .NET 8، همان `TypeLoadException` دربارهٔ `FuncCallInstruction<T0,TRet>` و `ReadOnlySpan<string>` بازتولید شد. همان نمونه پس از انتخاب C# 12 موفق شد و `Contains` به `Enumerable.Contains` متصل شد.

برای پروژه، SDK 8.0.4xx یا feature band جدیدترِ همان 8.0 و زبان C# 12 تثبیت شده‌اند. `rollForward: latestFeature` اجازهٔ انتخاب SDK 9/10 را نمی‌دهد. تغییر `TargetFramework` به نسخهٔ جدیدتر لازم نیست.

**محدودیت تشخیص:** متن exception و سازوکار آن بازتولید و اصلاح شد؛ چون stack trace کامل کاربر موجود نبود، صفحه یا endpoint دقیق رخداد اولیه مشخص نشده است.

## نتیجهٔ راستی‌آزمایی

محیط آزمایش نسخهٔ اصلاحی پیش از ادغام: Linux، .NET SDK **8.0.425**. SDK **10.0.401** فقط برای بازتولید ناسازگاری قبلی استفاده شد. نتایج زیر متعلق به همان نسخهٔ آزموده‌شده‌اند؛ پس از ادغام روی main، بیلد و تست‌ها مجدداً اجرا نشده‌اند. تنها تفاوت اجرایی با نسخهٔ آزموده‌شده، حفظ پیاده‌سازی معادل Popover در main است.

| بررسی | نتیجه |
|---|---|
| بازتولید خطاهای قبلی CS1503 و CS0023 با نمونهٔ سورس قبلی | هر دو بازتولید شدند |
| بازتولید TypeLoadException با C# 14 / اجرای .NET 8 | بازتولید شد |
| اجرای همان expression با C# 12 | موفق |
| Build کل Solution در Release، شامل Shared / API / Client | موفق، **۰ خطا و ۱۶ هشدار nullable** |
| Publish کلاینت Blazor در Release | موفق |
| تست‌های جدید سازگاری | **۲۳ موفق، ۰ ناموفق** |
| تست‌های موجود چت | **۵۷ موفق، ۰ ناموفق** |

برای کنترل مصرف حافظهٔ محیط، بیلد با `-m:1 -p:UseSharedCompilation=false -p:RunAnalyzers=false`، دو پردازندهٔ منطقی و سقف heap حدود ۱٫۱ GiB انجام شد. هشدارهای nullable پنهان نشده و در لاگ آمده‌اند. Publish بدون workload اختیاری `wasm-tools` موفق شد؛ بهینه‌سازی native WASM در این محیط انجام نشده است.

تست‌های جدید این موارد را پوشش می‌دهند:

- خروجی دقیق CSS در فرهنگ‌های فارسی، انگلیسی، آلمانی و عربی؛ ارتفاع اختیاری.
- هر دو تابع تولید IP، آدرس‌های با بیت بالای روشن، رنج‌های /29 و /30، fallback /24 و حفظ رفتار /31 و /32؛ **هیچ اسکن شبکه‌ای اجرا نمی‌شود**.
- انتخاب `Enumerable.Contains` در expression tree، اجرای interpreter و query واقعی EF Core روی SQLite در حافظه.

رابط در مرورگر، SQL Server و محیط ویندوز کاربر در این نوبت تست نشده‌اند. مشکلات امنیتی، RADIS-HR/SQLite و OCR گزارش قبلی خارج از محدودهٔ این بسته‌اند و اصلاح‌شده محسوب نمی‌شوند.

## روش جایگزینی و اجرا

۱. قبل از جایگزینی، از دیتابیس، تنظیمات اتصال، کلیدها و فایل‌های آپلودی نسخهٔ پشتیبان بگیرید. **ZIP را در یک پوشهٔ تازه استخراج کنید؛ روی پوشهٔ نصب عملیاتی بازنویسی نکنید.**

۲. SDK **.NET 8.0.4xx** نصب باشد؛ نسخهٔ تست‌شده 8.0.425 است. برای Visual Studio از نسخهٔ به‌روز با پشتیبانی این SDK استفاده کنید. وجود فقط Runtime یا فقط SDK 9/10 کافی نیست.

۳. فایل `global.json` را در ریشهٔ سورس نگه دارید و فرمان‌ها را از همان پوشه یا زیرپوشهٔ inventory اجرا کنید. نسخه را بررسی کنید:

```powershell
cd TotallCollectionSoftware
dotnet --version
# باید 8.0.4xx (یا feature band جدیدتر همان 8.0) نمایش داده شود.
```

اگر پیام «A compatible .NET SDK was not found» دیدید، SDK 8 لازم را نصب کنید؛ برای دورزدن پیام، `global.json` را حذف نکنید و LangVersion را به latest برنگردانید.

۴. در پوشهٔ تازه، بیلد بگیرید:

```powershell
dotnet restore inventory/Inventory.sln
dotnet build inventory/Inventory.sln -c Release -m:1 -p:UseSharedCompilation=false
```

اگر فقط فایل‌های patch را روی کپی توسعهٔ قبلی اعمال می‌کنید، برنامه و Visual Studio را ببندید و قبل از بیلد، **فقط پوشه‌های تولیدی bin و obj پروژه‌ها** را پاک کنید تا DLL تولیدشده با کامپایلر قبلی باقی نماند. به `wwwroot`، `SecureFiles`، `.key`، `App_Data` و دیتابیس دست نزنید.

۵. برای تولید خروجی در پوشه‌های staging جدید:

```powershell
dotnet publish inventory/src/Inventory.Client -c Release -o staging/client -m:1 -p:UseSharedCompilation=false
dotnet publish inventory/src/Inventory.Api -c Release -o staging/api -m:1 -p:UseSharedCompilation=false
```

برای استقرار تک‌سروره، **محتویات** `staging/client/wwwroot` را در `staging/api/wwwroot` کپی کنید، تنظیمات و مسیرهای پایدار دادهٔ نصب خود را برقرار کنید و سپس نسخهٔ تازه را طبق روش استقرار خود فعال کنید. این ZIP دیتابیس، کلید خصوصی runtime یا فایل‌های کاربران شما را ندارد.

**احتیاط:** اسکریپت موجود `inventory/deploy-single.sh` در نسخهٔ پایه کل `Inventory.Api/wwwroot` را پاک می‌کند. اگر فایل‌های عملیاتی یا `SecureFiles` در آن مسیر دارید، بدون پشتیبان از آن استفاده نکنید؛ برای این به‌روزرسانی مسیر staging توصیه می‌شود.

پس از استقرار کلاینت جدید، مرورگر را با Ctrl+Shift+R بارگذاری مجدد کنید. اگر نسخهٔ PWA قدیمی باقی ماند، سرویس‌ورکر/کش همان مبدأ را پس از اطمینان از ذخیره‌شدن کارهای محلی به‌روزرسانی کنید. پاک‌کردن کش جایگزین بیلد مجدد DLLهای سرور نیست.

## اجرای مجدد تست‌ها

از ریشهٔ سورس:

```powershell
dotnet restore tests/compatibility/Inventory.Compatibility.Tests.csproj --configfile inventory/NuGet.config
dotnet test tests/compatibility/Inventory.Compatibility.Tests.csproj -c Release -m:1 -p:UseSharedCompilation=false

dotnet restore tests/chat/Inventory.Chat.Tests/Inventory.Chat.Tests.csproj --configfile inventory/NuGet.config
dotnet test tests/chat/Inventory.Chat.Tests/Inventory.Chat.Tests.csproj -c Release -m:1 -p:UseSharedCompilation=false
```

اگر پس از clean/rebuild و جایگزینی نسخهٔ سرور و کلاینت، باز هم خطای ReadOnlySpan دیدید، stack trace کامل، مسیر صفحه/API و خروجی `dotnet --info` را بفرستید تا مسیر باقیمانده جداگانه بررسی شود.
