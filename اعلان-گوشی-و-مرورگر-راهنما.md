# اعلان گوشی و مرورگر (مثل اعلان اندروید) — راهنمای کوتاه

سامانه سه مسیر اعلان دارد که با هم کار می‌کنند:

| مسیر | کجا نشان داده می‌شود | وقتی که نرم‌افزار بسته است |
|---|---|---|
| **اعلان داخل نرم‌افزار** (نوار بالای صفحه + صدا و لرزش) | داخل همان مرورگری که نرم‌افزار را باز کرده‌اید | ❌ (نرم‌افزار بسته است) |
| **اعلان گوشی/دسکتاپ (Web Push)** | در نوار اعلان گوشی یا کنار ساعت ویندوز — **دقیقاً مثل اعلان اندروید** | ✅ |
| **بله و ایتا** | در پیام‌رسان روی گوشی کاربر | ✅ |

مسیر اول و سوم همیشه فعال‌اند؛ مسیر دوم (Web Push) نیاز به یک‌بار فعال‌سازی دارد که کامل از داخل خود نرم‌افزار انجام می‌شود.

---

## ۱) مدیر: یک‌بار کلید اعلان را بسازید (۳۰ ثانیه)

1. نرم‌افزار → **تنظیمات** → کارت **«اعلان گوشی و مرورگر (مثل اعلان اندروید)»** → بخش «کلید اعلان سرور».
2. دکمه‌ی **«ساخت و فعال‌سازی کلید اعلان»** را بزنید. همین! کلید ساخته می‌شود، **همان لحظه** روی سرور فعال می‌شود و در فایل `App_Data/push-vapid.json` ذخیره می‌شود.
   - نه ویرایش `appsettings.json` لازم است و نه ری‌استارت سرویس.
   - همان کار از پنجره‌ی زنگ اعلان‌ها → دکمه‌ی **«کلیدهای اعلان»** هم انجام می‌شود.
3. در همان کارت می‌بینید: تعداد **دستگاه‌های ثبت‌شده**، تعداد **کاربران**، تعداد در **صف ارسال** و تعداد **ناموفق** (برای عیب‌یابی).

> اگر می‌خواهید کلیدها از فایل/متغیر محیطی سرور خوانده شوند (روش قبلی)، می‌توانید با دکمه‌ی «حذف کلیدهای نرم‌افزار» به آن حالت برگردید.
> ⚠️ ساخت کلید جدید، اشتراک دستگاه‌های قبلی را باطل می‌کند (کاربران یک‌بار دوباره فعال می‌کنند) — کلید را بی‌دلیل عوض نکنید.

## ۲) هر کاربر: اعلان این دستگاه را فعال کند

1. نرم‌افزار → **تنظیمات** → همان کارت → بخش **«این دستگاه»** → دکمه‌ی **«فعال‌سازی اعلان این دستگاه»**.
   (یا از پنجره‌ی زنگ اعلان‌ها → ردیف «اعلان گوشی/تبلت» → «فعال‌سازی».)
2. مرورگر یک پیام اجازه نشان می‌دهد → **Allow / اجازه دادن**.
3. حالا اعلان‌ها مثل اعلان اندروید روی همان دستگاه می‌آیند — **هم وقتی نرم‌افزار باز است و هم وقتی بسته است**.
4. برای اطمینان، دکمه‌ی **«ارسال آزمایشی»** را بزنید؛ اعلان باید بلافاصله بیاید.
   در همان کارت، وضعیت آخرین ارسال هم نوشته می‌شود (تحویل‌شده / در صف / خطا).

هر دستگاه جداگانه فعال می‌شود؛ روی گوشی، تبلت و دسکتاپ یک‌بار تکرار کنید.

## ۳) پیش‌نیازها (مهم)

| مورد | توضیح |
|---|---|
| **HTTPS** | اعلان پس‌زمینه فقط روی آدرس امن (`https://…`) کار می‌کند. روی `http://` معمولی، نرم‌افزار خودش پیام می‌دهد «اعلان اندروید به HTTPS معتبر نیاز دارد». (localhost استثناست.) |
| **مرورگر** | Chrome / Edge در اندروید و ویندوز، Firefox در دسکتاپ. |
| **iPhone** | در Safari باید از منوی Share گزینه‌ی «Add to Home Screen» را بزنید (iOS 16.4 به بالا) و بعد نرم‌افزار را از آیکن صفحه‌ی اصلی باز کنید. |
| **باز بودن سرویس** | سرور باید اینترنت خروجی داشته باشد (برای رساندن پیام به سرویس Push). آدرس سرویس اعلان (`fcm.googleapis.com`، `updates.push.services.mozilla.com`، `web.push.apple.com`) اگر با فایروال بسته شده باشد، ارسال ناموفق می‌شود و در کارت تنظیمات تعداد «ناموفق» بالا می‌رود. |

## ۴) اعلان وقتی نرم‌افزار داخل مرورگر باز است

- اگر Web Push فعال باشد: هم banner شیشه‌ای داخل نرم‌افزار ظاهر می‌شود، هم اعلان سیستمی گوشی/دسکتاپ.
- اگر Web Push فعال نباشد: banner داخل نرم‌افزار + **صدای کوتاه دو نُتی و لرزش گوشی** پخش می‌شود تا مثل اعلان اندروید حس شود (صدا پس از اولین کلیک روی صفحه فعال می‌شود؛ این محدودیت خود مرورگرهاست).
- کلیک روی اعلان سیستمی → نرم‌افزار باز/فوکوس می‌شود و به همان صفحهٔ مربوطه می‌رود.

## ۵) عیب‌یابی

| نشانه | راه‌حل |
|---|---|
| پیام «کلیدهای اعلان روی سرور تنظیم نشده» | مدیر: دکمه‌ی «ساخت و فعال‌سازی کلید اعلان» را بزند. |
| پیام «این مرورگر اعلان پس‌زمینه ندارد» | از Chrome/Edge به‌روز استفاده کنید (یا در iPhone نرم‌افزار را به صفحه‌ی اصلی اضافه کنید). |
| پیام «اجازه اعلان مسدود است» | در تنظیمات سایت (قفل کنار آدرس) → Notifications → Allow؛ و در تنظیمات گوشی، اعلان‌های مرورگر را روشن کنید. |
| «اعلان این دستگاه را دوباره فعال کنید» | کلید سرور عوض شده؛ یک‌بار فعال‌سازی را تکرار کنید. |
| «ارسال اخیر ناموفق بود (PROVIDER_401/403)» | کلیدهای سرور با اشتراک دستگاه نمی‌خواند → کلید را دوباره بسازید و دستگاه‌ها را دوباره فعال کنید. |
| در کارت تنظیمات «ناموفق» بالا می‌رود | اینترنت خروجی سرور یا فایروال مقصدهای Push را بررسی کنید. |
| اعلان می‌آید ولی روی گوشی دیگر نمی‌آید | اعلان هر دستگاه جداست؛ روی دستگاه دوم هم «فعال‌سازی» را بزنید. |

## ۶) فایل‌های مربوطه در سورس

| فایل | نقش |
|---|---|
| `inventory/src/Inventory.Api/Services/Notification/PushService.cs` | سرویس ارسال Web Push + کلیدهای VAPID (زنده و قابل تغییر) |
| `inventory/src/Inventory.Api/Services/Notification/PushKeyStore.cs` | **جدید** — ذخیره/خواندن کلیدها در `App_Data/push-vapid.json` |
| `inventory/src/Inventory.Api/Services/Notification/PushQueue.cs` | صف پایدار ارسال (تلاش مجدد، انقضا، پاک‌سازی اشتراک باطل) |
| `inventory/src/Inventory.Api/Controllers/System/NotificationsController.cs` | `push-vapid-key`، `push-vapid-generate` (ساخت+فعال‌سازی)، `push-vapid-reset`، `push-overview`، `push-subscribe`، `test-push`، `push-status` |
| `inventory/src/Inventory.Client/wwwroot/js/push-notifications.js` | منطق مرورگر (اجازه، اشتراک، آزمایش) + صدای اعلان درون‌برنامه‌ای |
| `inventory/src/Inventory.Client/wwwroot/push-worker.js` | Service Worker: نمایش اعلان سیستمی و کلیک روی آن |
| `inventory/src/Inventory.Client/Pages/Settings/Index.razor` | کارت «اعلان گوشی و مرورگر» |
| `inventory/src/Inventory.Client/Layout/MainLayout.razor` | ردیف و پنل «اعلان گوشی/تبلت» + اعلان‌های درون‌برنامه‌ای |

> بدون هیچ مایگریشن دیتابیسی: کلیدها در فایل `App_Data/push-vapid.json` و اشتراک دستگاه‌ها در جدول‌های موجود `PushSubscriptions` / `PushDeliveries` نگه داشته می‌شوند.

---

## ۷) مدت نمایش اعلان‌ها و پیام‌ها (قابل تنظیم)

پیش‌تر اعلان زندهٔ بالای صفحه ۱۵ ثانیه و پیام‌های کوتاه ۴ ثانیه نمایش داده می‌شدند؛ حالا **همه** بالا رفته و قابل تنظیم است:

| نوع نمایش | پیش‌فرض جدید | پیش‌فرض قبلی | گزینه‌های قابل انتخاب |
|---|---|---|---|
| اعلان زندهٔ بالای صفحه (نوتیف رویدادها) | **۲ دقیقه** | ۱۵ ثانیه | ۳۰ث، ۱د، ۲د، ۵د، ۱۰د، ۳۰د، **تا بستن دستی** |
| پیام کوتاه (موفق/خطا/اطلاع) | **۲۰ ثانیه** | ۴.۲ ثانیه | ۱۰ث، ۲۰ث، ۳۰ث، ۱د، ۳د، ۱۰د، **تا بستن دستی** |
| اعلان سیستمی گوشی/دسکتاپ (Web Push) | تا کلیک کاربر | محو شدن سریع | با `requireInteraction` دیگر خودش محو نمی‌شود |

**کجا تنظیم می‌شود؟** پنل اعلان‌ها (زنگ بالای صفحه) → ردیف **«مدت نمایش اعلان‌ها و پیام‌ها»** → انتخاب‌ها با یک کلیک اعمال و در همان دستگاه ذخیره می‌شوند (هر کاربر می‌تواند متفاوت تنظیم کند).

**نکته‌های دیگر این تغییر:**
- تعداد اعلان‌های هم‌زمان روی صفحه از ۴ به **۶** رسید تا با ماندگاری طولانی‌تر، اعلان‌ها جای هم را تنگ نکنند.
- «تا بستن دستی» یعنی پیام تا وقتی خود کاربر دکمهٔ ✕ را نزند روی صفحه می‌ماند (مناسب برای هشدارهای مهم مثل کمبود موجودی).
- اعلان سیستمی گوشی (Web Push) اکنون تا **کلیک کاربر** می‌ماند؛ برای دیدن اعلان روی دسکتاپ در نوار اعلان ویندوز و در پنل اعلان اندروید.
- اگر مرورگر حافظهٔ محلی (localStorage) را مسدود کرده باشد، انتخاب کاربر فقط تا پایان همان نشست معتبر است.

### فایل‌های این بخش

| فایل | نقش |
|---|---|
| `inventory/src/Inventory.Client/Services/NotificationTiming.cs` | **جدید** — مرکز تعیین مدت نمایش (گزینه‌ها، پیش‌فرض‌ها، ذخیره/بازیابی، برچسب فارسی) |
| `inventory/src/Inventory.Client/Services/ToastService.cs` | استفاده از مدت تنظیم‌شده به‌جای ۴.۲ ثانیه ثابت |
| `inventory/src/Inventory.Client/Layout/MainLayout.razor` | اعمال مدت روی اعلان زنده + رابط انتخاب مدت در پنل زنگ |
| `inventory/src/Inventory.Client/wwwroot/push-worker.js` | `requireInteraction` برای ماندگاری اعلان سیستمی |
