# 📋 فهرست کامل درخواست‌های پروژه — سامانه انبار و فروش (فروغ آریا)

> جمع‌بندی تمام درخواست‌های ثبت‌شده در طول پروژه — به ترتیب موضوع، با وضعیت انجام

---

## ۱. راه‌اندازی و دیتابیس 🗄️

| # | درخواست | وضعیت |
|---|---------|:-----:|
| ۱ | دانلود پروژه از گیت‌هاب و اکسترکت بدون مشکل | ✅ |
| ۲ | تبدیل Connection String به Windows Authentication | ✅ |
| ۳ | آپدیت دیتابیس خودکار (بدون اجرای دستی بعد از ران) | ✅ |
| ۴ | رفع خطای `Invalid object name 'Roles'` (مایگریشن جامانده) | ✅ |
| ۵ | کاهش حجم workspace (حذف bin/obj — از ۱۱۴MB به ~۴MB) | ✅ |

## ۲. کاربران سیستم و ساختار سازمانی 🏢

| # | درخواست | وضعیت |
|---|---------|:-----:|
| ۱ | رفع خطای «بدنه پاسخ خالی» در افزودن کاربر سیستم (ساخت CRUD جامانده) | ✅ |
| ۲ | فرم کاربر سیستم: نام، نام خانوادگی، واحد (دپارتمان) | ✅ |
| ۳ | کمپانی و واحد → منتقل به «اطلاعات پایه» در منو | ✅ |
| ۴ | حذف کاربران سیستم از تنظیمات | ✅ |
| ۵ | معرف اختیاری در تعریف کاربر (گزینه «بدون معرف») + ساخت خودکار معرف هنگام ورود | ✅ |

## ۳. نقش‌ها و دسترسی‌ها (RBAC) 🔐

| # | درخواست | وضعیت |
|---|---------|:-----:|
| ۱ | اختصاص نقش به کاربران + جستجوی کاربر | ✅ |
| ۲ | آپدیت خودکار دسته‌بندی هنگام تخصیص نقش | ✅ |
| ۳ | دسترسی به ازای هر داشبورد (مالی/مدیریت/سخت‌افزار) | ✅ |
| ۴ | دسترسی به ازای هر گزارش (کاردکس/نقطه سفارش) | ✅ |
| ۵ | انتقال اختصاص نقش به فرم «کاربران» (لاگین) — نه کاربران سیستم | ✅ |
| ۶ | حذف ۳ نقش ثابت؛ انتخاب نقش از بخش دسترسی‌ها | ✅ |
| ۷ | کمبوی نقش با جستجو و چند انتخابی (چیپ‌دار) | ✅ |
| ۸ | نمایش همه‌ی نقش‌های کاربر در لیست کاربران | ✅ |
| ۹ | دسته‌بندی دسترسی‌ها بر اساس ماژول (۶ گروه) | ✅ |
| ۱۰ | مجوز «مشاهده سود» — مخفی شدن سود در فاکتور فروش و لیست فروش | ✅ |
| ۱۱ | بخش معرف در دسترسی‌ها (کیف پول، محصولات، کارت، داشبورد) | ✅ |
| ۱۲ | کمپانی و واحد در دسترسی‌ها | ✅ |
| ۱۳ | **اعمال واقعی نقش‌ها**: منو + گارد مرکزی همه صفحات (URL مستقیم هم بسته) | ✅ |
| ۱۴ | دکمه‌های ویرایش/حذف زیباتر + کارت‌های آمار رسپانسیو | ✅ |

## ۴. درخواست خدمت IT 🎧

| # | درخواست | وضعیت |
|---|---------|:-----:|
| ۱ | نام درخواست‌کننده خودکار از لاگین (غیرفعال + قابل ویرایش) | ✅ |
| ۲ | کمبوی سیستم‌ها بر اساس دامنه دسترسی (کل شرکت / واحد خود) + جستجو + نمایش IP و مالک | ✅ |
| ۳ | مجوزهای IT در بخش مدیریت سیستم (ایجاد، کارشناس، مدیر و...) | ✅ |
| ۴ | نوع درخواست: سخت‌افزار / نرم‌افزار / شبکه / مخابرات | ✅ |
| ۵ | شرح درخواست با ادیتور (Bold/Italic/لیست) | ✅ |
| ۶ | گردش‌کار: ارجاع مدیر به چند کارشناس + شرح اختصاصی هر نفر | ✅ |
| ۷ | کارشناس: گزارش + انتخاب «انجام شد / انجام نشد» | ✅ |
| ۸ | مدیر: تایید یا رد گزارش هر کارشناس (رد = برگشت به کارشناس) | ✅ |
| ۹ | تایید نهایی فقط بعد از تایید همه گزارش‌ها؛ تصمیم تکی وضعیت کلی را عوض نمی‌کند | ✅ |
| ۱۰ | محرمانگی: درخواست‌دهنده فرایند داخلی IT را نمی‌بیند (فقط پاسخ نهایی مدیر) | ✅ |
| ۱۱ | آرشیو کامل رفت‌وبرگشت‌ها (تایم‌لاین) | ✅ |
| ۱۲ | کارتابل‌های جدا: درخواست‌دهنده / کارشناس / مدیر IT + صفحه‌بندی | ✅ |
| ۱۳ | نشانگر «جدید» تا زمان رویت + شماره منحصربه‌فرد `IT/سال شمسی/سریال` | ✅ |
| ۱۴ | مدیر: بستن درخواست توسط خودش + رد درخواست با دلیل | ✅ |
| ۱۵ | پیوست فایل (درخواست‌دهنده/کارشناس/مدیر) | ✅ |
| ۱۶ | سوابق درخواست‌های هر سیستم در شناسنامه سیستم | ✅ |
| ۱۷ | کارت آمار در داشبورد سخت‌افزار: کل / انجام‌شده / در حال اجرا / در انتظار مدیر | ✅ |
| ۱۸ | طراحی کارت‌ویو زیبا + رسپانسیو + قالب اندروید | ✅ |

## ۵. اتصال شعبه‌ها به سرور مرکزی 🌐

| # | درخواست | وضعیت |
|---|---------|:-----:|
| ۱ | کانفیگ IP سرور مرکزی در تنظیمات (هر شعبه یک‌بار تنظیم می‌کند) | ✅ |
| ۲ | کاربر شعبه از داخل نرم‌افزار خودش درخواست می‌زند → خودکار به سرور مرکزی می‌رود (بدون لاگین به سرور مرکزی) | ✅ |
| ۳ | شماره پیگیری + مشاهده وضعیت و پاسخ از راه دور + تایید تکمیل | ✅ |
| ۴ | صفحه عمومی `/request` بدون لاگین (+ لینک در صفحه ورود) | ✅ |

## ۶. نوتیفیکیشن و بلادرنگ (SignalR) 🔔

| # | درخواست | وضعیت |
|---|---------|:-----:|
| ۱ | زنگ 🔔 بالای منو با شمارنده پیام‌های نخوانده + پنل اعلان‌ها | ✅ |
| ۲ | توست پایین-چپ لحظه‌ای (نام فرم + فرستنده) — سراسری کل پروژه | ✅ |
| ۳ | کلیک روی نوتیف → رفتن به همان فرم و همان درخواست | ✅ |
| ۴ | توست خوش‌آمد بعد از لاگین | ✅ |
| ۵ | کارتابل‌ها برخط (بدون رفرش دستی) با SignalR | ✅ |
| ۶ | معماری جامع SignalR برای همه ماژول‌های آینده (`RealtimeService`) | ✅ |
| ۷ | «کارتابل من» زیر داشبوردها (پیام‌ها + موارد منتظر اقدام — قابل توسعه) | ✅ |

## ۷. پیام‌رسان‌های بله و ایتا 📨

| # | درخواست | وضعیت |
|---|---------|:-----:|
| ۱ | نشان بله/ایتا کنار کاربرانی که عضو هستند (+ فیلد موبایل) | ✅ |
| ۲ | ارسال هر نوتیف به بله/ایتای کاربر — با امضای شماره 09111189771 | ✅ |
| ۳ | همگام‌سازی بله (تطبیق مخاطبین ربات با موبایل کاربران) | ✅ |
| ۴ | تنظیمات توکن‌ها در بخش پیام‌رسان‌ها | ✅ |

## ۸. سایر 🧩

| # | درخواست | وضعیت |
|---|---------|:-----:|
| ۱ | ماشین اداری: نوع اتصال شبکه (با IP) یا کابلی (انتخاب سیستم با جستجو + مالک) | ✅ |
| ۲ | کارت‌ویو موبایل برای تعمیرات/خرید/فروش (تا عرض ۹۹۲px) | ✅ |
| ۳ | پنل معرف بدون خطا (حتی بدون معرف مرتبط) + مشاهده کالا با مجوز RBAC | ✅ |
| ۴ | رفع مشکل لاگین کاربر جدید + نمایش خطاها داخل فرم | ✅ |
| ۵ | دکمه «کاربر جدید» سمت چپ فرم | ✅ |
| ۶ | دیتابیس آزمایشی ماندگار در پیش‌نمایش (`demo-db/`) | ✅ |

## ۹. رفع اشکالات اتوماسیون اداری 📝

| # | درخواست | وضعیت |
|---|---------|:-----:|
| ۱ | ویرایشگر متن نامه در «ایجاد نامه داخلی جدید» با امکانات مشابه Word (بدون کتابخانه‌ی خارجی) | ✅ |
| ۲ | تقویم تاریخ در صفحات مرخصی/ماموریت زیر گرید می‌رفت و قابل کلیک نبود | ✅ |
| ۳ | تنظیمات «ساختار شماره نامه» (اجزا + ترتیب) روی نامه‌ها اعمال نمی‌شد | ✅ |
| ۴ | ذخیره‌ی فایل پیوست نامه‌ی داخلی روی دیسک (`wwwroot/uploads/innerletter`) + دانلود و پیش‌نمایش | ✅ |

### جزئیات پیاده‌سازی

**۱) ویرایشگر متن پیشرفته (RichTextEditor)**
- کامپوننت `Shared/RichTextEditor.razor` + `wwwroot/js/rte.js` + `wwwroot/css/rte.css` — کاملاً داخلی، بدون CKEditor/TinyMCE.
- قلم و اندازه، پررنگ/کج/زیرخط/خط‌خورده، بالانویس/زیرنویس، رنگ متن و هایلایت، ترازها، لیست‌ها، تورفتگی،
  جهت راست‌به‌چپ/چپ‌به‌راست، جدول، خط افقی، لینک، تصویر، پاک‌کردن قالب، Undo/Redo، شمارش کلمات و نمای HTML.
- در `ComposeLetterModal.razor` جایگزین ویرایشگر ساده‌ی قبلی شد.

**۲) تقویم شمسی روی همه‌ی صفحات**
- `wwwroot/js/datepicker.js`: پاپ‌آور با `position: fixed` و مختصات محاسبه‌شده نسبت به ورودی، مقید به viewport.
- `Shared/DatePicker.razor`: بازکردن/بستن از طریق JS، بستن با کلیک بیرون و Esc، جای‌گذاری مجدد هنگام اسکرول/تغییر اندازه.
- `app.css`: `.dp-popover` به `position: fixed; z-index: 20050` تغییر کرد تا از context والد (کارت/گرید) بیرون بیاید.
- روی هر ۱۵ صفحه‌ای که از `<DatePicker>` استفاده می‌کنند اعمال می‌شود (مرخصی، ماموریت، حضور و غیاب، سفارش‌ها و…).

**۳) ساختار شماره نامه**
- جدول جدید `LetterNumberSettings` (موجودیت `LetterNumberSetting`) + مایگریشن `AddLetterNumberSettings`.
- فیلد `Code` به `SystemDepartments` اضافه شد (کد واحد برای استفاده در شماره) و در صفحه‌ی «واحد / بخش» قابل ثبت است.
- سرویس `LetterNumberService`: اجزای `prefix, year, month, day, dept, company, user, serial, text1, text2, suffix`
  با ترتیب دلخواه، جداکننده، تعداد ارقام سال/سریال، شماره شروع، گام، ریست سالانه/ماهانه/هرگز و ارقام فارسی.
- API: `GET/POST api/letters/number-settings`, `GET .../parts`, `POST .../preview`.
- صفحه‌ی `‎/settings/letter-numbering` با جابه‌جایی اجزا (بالا/پایین) و پیش‌نمایش زنده؛ از «تنظیمات» و منو در دسترس است.
- **واحد و شرکت از خودِ تنظیمات سیستم خوانده می‌شوند**: اندپوینت `GET api/letters/number-settings/lookups`
  فهرست واحدها («شناسنامه سیستم ← واحد / بخش») و کمپانی‌ها را برمی‌گرداند و در صفحه به‌صورت
  کشویی انتخاب می‌شوند (نه تایپ دستیِ کد). اگر جزء واحد/شرکت انتخاب شود ولی هیچ کدی تعریف نشده باشد، هشدار داده می‌شود.
- سال و ماه شمسی هم اجزای مستقل‌اند (سال ۲ یا ۴ رقمی) و نمونه‌ی هر جزء کنارش نمایش داده می‌شود.
- `InnerLetterService` هنگام ثبت نامه شماره را از همین سرویس می‌گیرد (شماره‌ی سخت‌کدشده حذف شد).

**۴) پیوست‌های نامه روی دیسک**
- `LetterAttachmentStore`: ذخیره در `wwwroot/uploads/innerletter/{letterId}` و پیش‌نویس‌ها در
  `wwwroot/uploads/innerletter/pishnevis/{id}`؛ با محافظت در برابر Path Traversal و پاک‌سازی نام فایل.
- هنگام ارسال پیش‌نویس، فایل‌ها از پوشه‌ی پیش‌نویس به پوشه‌ی نامه منتقل می‌شوند.
- مسیر فایل در `AppAttachment.FilePath` ذخیره می‌شود؛ رکوردهای قدیمی (فایل داخل ستون `Data`) همچنان کار می‌کنند.
- `GET api/letters/attachments/{id}/download` و `GET .../view` (نمایش inline).
- سمت کلاینت، فایل با هدر Authorization خوانده و به‌صورت Blob دانلود یا در تب جدید نمایش داده می‌شود
  (لینک ساده‌ی `href` توکن JWT نداشت و خطای ۴۰۱ می‌گرفت — علت اصلی کار نکردن دانلود).
- امنیت: مسیر استاتیک `/uploads/innerletter` روی سرور مسدود شد؛ پیوست‌ها فقط از API با بررسی
  «حضور در گردش نامه» قابل دریافت‌اند. پوشه‌ی `wwwroot/uploads/` هم به `.gitignore` اضافه شد.
- `sql/Update-Database-Full.sql` هم به‌روزرسانی شد (ستون `Code`، جدول `LetterNumberSettings`،
  رکورد پیش‌فرض «سال/سریال» و ثبت مایگریشن) تا نصب‌های SQL Server بدون اجرای EF هم به‌روز شوند.

---

## 🔑 اطلاعات ورود پیش‌نمایش

| کاربر | رمز | نقش |
|---|---|---|
| admin | admin | مدیر کل + مدیر IT |
| tech1 | 1234 | کارشناس IT |
| sara | 1234 | فروشنده (بدون مشاهده سود) |
| rahmani | 1234 | معرف (با معرف مرتبط) |

## 📁 فایل‌های مهم برای انتقال به ویندوز

- کل پوشه `inventory/src/` (سورس به‌روز)
- `inventory/sql/Update-Database-Full.sql` — اسکریپت کامل دیتابیس SQL Server (idempotent)
- بعد از کپی: `dotnet run` در `src/Inventory.Api` — مایگریشن‌ها خودکار اعمال می‌شوند
