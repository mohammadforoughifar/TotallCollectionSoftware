# اسکواش مایگریشن‌ها (۷۱ ← ۱)

تاریخ: 2026-09-11 — پوشه `Migrations` از **7.6MB** به **964KB** رسید.

## چه شد
- هر ۷۱ مایگریشن قبلی (به‌ترتیب زمانی، بدون هیچ تغییری در منطق) داخل یک مایگریشن واحد
  `20260911100108_SquashedInitial` ادغام شدند؛ `Down` هم با ترتیب معکوس.
- اسنپ‌شات مدل از نو ساخته شد و با `migrations add` خالی (۰ عملیات) راستی‌آزمایی شد.
- ماژول RadisHr (کانتکست و مایگریشن‌های خودش) دست‌نخورده باقی ماند.
- فکتوری دیزاین‌تایم (`Data/DesignTimeDbContextFactory.cs`) اضافه شد تا `dotnet-ef` بدون اجرای کامل برنامه کار کند.

## دیتابیس‌های جدید — کاری لازم نیست
`Migrate()` روی دیتابیس خالی همان نتیجه قبلی را می‌سازد (۱۴۷ جدول + ایندکس‌ها؛ خروجی `migrations script` بررسی شد).

## دیتابیس‌های موجود (SQL Server عملیاتی) — خودکار
در اولین بوت بعد از آپدیت، `MigrateSqlServer` مایگریشن اسکواش‌شده را امتحان می‌کند، با خطای
«شیء از قبل وجود دارد» مواجه می‌شود و آن را به‌صورت خودکار در `__EFMigrationsHistory` مُهر
(Stamps) می‌کند و ادامه می‌دهد. لاگ آن در کنسول دیده می‌شود:
`[DB] مایگریشن ... رد شد (شیء از قبل در دیتابیس است).`

## راه جایگزین دستی (اختیاری)
اگر نخواستید اولین بوت این کار را بکند، قبل از آپدیت روی دیتابیس موجود اجرا کنید:

```sql
DELETE FROM [dbo].[__EFMigrationsHistory];
INSERT INTO [dbo].[__EFMigrationsHistory] ([MigrationId], [ProductVersion])
VALUES ('20260911100108_SquashedInitial', '8.0.1');
```

## مایگریشن بعدی
مثل قبل: `dotnet-ef migrations add <Name> --context Inventory.Api.Data.AppDbContext`
(اگر اسنپ‌شات RadisHr را دست‌کاری کرد، آن را با `git checkout` برگردانید — باگ شناخته‌شده
`dotnet-ef` در پروژه‌های دابل‌کانتکست است؛ موقع `add` پوشه `Modules/RadisHr/Data/Migrations`
را موقتاً جابه‌جا کنید).
