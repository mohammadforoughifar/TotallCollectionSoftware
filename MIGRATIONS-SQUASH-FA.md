# اسکواش مایگریشن‌ها

## اسکواش دوم (۸ ← ۱) — 2026-09-16

هفت مایگریشن HR که بعد از اسکواش اول اضافه شده بودند دوباره در همان مایگریشن
`20260911100108_SquashedInitial` ادغام شدند؛ پوشه `Migrations` از **3.5MB / 100,124 خط**
به **1.1MB / 8,074 خط + designer** رسید (۱۴ فایل حذف شد).

- `Up` = الحاق هر ۸ بلوک به‌ترتیب زمانی، `Down` = همان بلوک‌ها با ترتیب معکوس.
- **نکتهٔ مهم:** هر بلوک داخل یک *local function* قرار گرفته (`M_<MigrationId>()`) چون
  بلوک ماژول چت در مسیر SQLite دستور `return;` دارد؛ الحاق ساده باعث می‌شد بقیهٔ
  مایگریشن‌ها رد شوند. دامنهٔ متغیرهای محلی هر مایگریشن هم جدا می‌ماند.
- شناسهٔ قدیمی‌ترین مایگریشن (`20260911100108_SquashedInitial`) حفظ شد؛ پس دیتابیس‌هایی
  که این مایگریشن را در `__EFMigrationsHistory` دارند **هیچ کاری pending ندارند** و
  حلقهٔ `MigrateSqlServer` هم درگیر نمی‌شود.
- `Designer` از مدل آخرین مایگریشن (`AddHrPerfPhase4`) گرفته شد و فقط شناسه/نام کلاس
  عوض شد؛ `AppDbContextModelSnapshot.cs` دست‌نخورده است (هنوز هم جلوتر از مدل
  مایگریشن‌هاست، چون موجودیت‌هایی مثل `DocTemporaryGrant` دستی اضافه شده‌اند).

### راستی‌آزمایی (بدون SQL Server)
- `dotnet build Inventory.sln` → **0 error**.
- `dotnet ef migrations list` → فقط `20260911100108_SquashedInitial`.
- `dotnet ef migrations script` قبل و بعد از اسکواش مقایسه شد: T-SQL **یکسان**
  (۱۸۷ `CREATE TABLE`، ۲۲۲ `CREATE INDEX`، ۱۵۷ `ALTER TABLE` در هر دو)؛ تنها تفاوت
  ۷۲ خط مربوط به `INSERT INTO [__EFMigrationsHistory]`/`COMMIT` مایگریشن‌های حذف‌شده است.
- تست‌ها: همان مجموعهٔ قبل (۵ تست از قبل خراب، بدون تغییر) — `ChatMigrationTests`
  و `ChatFileTests` و `NavigationTests` پیش از این تغییر هم شکست می‌خوردند
  (`HasPendingModelChanges()` در این ریپو `true` است و آن تست هنوز شناسه‌های
  مایگریشن حذف‌شده در اسکواش اول را صدا می‌زند).

### هشدار عملیاتی
اگر دیتابیسی دارید که **فقط** `SquashedInitial` را دارد ولی هفت مایگریشن HR روی آن
اعمال نشده، بعد از این تغییر آن‌ها را pending نمی‌بیند. در آن حالت قبل از آپدیت،
سطر `20260911100108_SquashedInitial` را از `__EFMigrationsHistory` پاک کنید تا
مایگریشن ادغام‌شده از نو اجرا شود (یا جداول HR را دستی بسازید).

---

## اسکواش اول (۷۱ ← ۱)

تاریخ: 2026-09-11 — پوشه `Migrations` از **7.6MB** به **964KB** رسید.

## چه شد
- هر ۷۱ مایگریشن قبلی (به‌ترتیب زمانی، بدون هیچ تغییری در منطق) داخل یک مایگریشن واحد
  `20260911100108_SquashedInitial` ادغام شدند؛ `Down` هم با ترتیب معکوس.
- اسنپ‌شات مدل از نو ساخته شد و با `migrations add` خالی (۰ عملیات) راستی‌آزمایی شد.
- ماژول RadisHr (کانتکست و مایگریشن‌های خودش) دست‌نخورده باقی ماند.
- فکتوری دیزاین‌تایم (`Data/DesignTimeDbContextFactory.cs`) اضافه شد تا `dotnet-ef` بدون اجرای کامل برنامه کار کند.

## دیتابیس‌های جدید — کاری لازم نیست
`Migrate()` روی دیتابیس خالی همان نتیجه قبلی را می‌سازد (۱۴۷ جدول + ایندکس‌ها؛ خروجی `migrations script` بررسی شد).

## دیتابیس‌های موجود (SQL Server عملیاتی) — خودکار
در اولین بوت بعد از آپدیت، `MigrateSqlServer` مایگریشن اسکواش‌شده را امتحان می‌کند، با خطای
«شیء از قبل وجود دارد» مواجه می‌شود و آن را به‌صورت خودکار در `__EFMigrationsHistory` مُهر
(Stamps) می‌کند و ادامه می‌دهد. لاگ آن در کنسول دیده می‌شود:
`[DB] مایگریشن ... رد شد (شیء از قبل در دیتابیس است).`

## راه جایگزین دستی (اختیاری)
اگر نخواستید اولین بوت این کار را بکند، قبل از آپدیت روی دیتابیس موجود اجرا کنید:

```sql
DELETE FROM [dbo].[__EFMigrationsHistory];
INSERT INTO [dbo].[__EFMigrationsHistory] ([MigrationId], [ProductVersion])
VALUES ('20260911100108_SquashedInitial', '8.0.1');
```

## مایگریشن بعدی
مثل قبل: `dotnet-ef migrations add <Name> --context Inventory.Api.Data.AppDbContext`
(اگر اسنپ‌شات RadisHr را دست‌کاری کرد، آن را با `git checkout` برگردانید — باگ شناخته‌شده
`dotnet-ef` در پروژه‌های دابل‌کانتکست است؛ موقع `add` پوشه `Modules/RadisHr/Data/Migrations`
را موقتاً جابه‌جا کنید).
