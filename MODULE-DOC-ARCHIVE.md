# ماژول آرشیو اسناد و مدارک (DocArchive)

ماژول کامل بایگانی اسناد با پوشه‌بندی درختی، دسترسی نفر‌به‌نفر، ورژن‌گذاری، گردش تایید و کارتابل.
طراحی و کدنویسی دقیقاً بر اساس الگوی سایر ماژول‌های برنامه (RbacControllerBase، DTO مشترک، Modal/SearchSelect/AttachmentBox، NavMenu، NotifyService).

---

## ۱) پوشه‌بندی (مثل بایگانی)

- پوشه و زیرپوشه **نامحدود** (`DocFolder.ParentId`).
- درخت پوشه در سمت چپ صفحه `‎/doc-archive` با شمارنده مدارک هر پوشه.
- ساخت زیرپوشه نیازمند دسترسی **نوشتن** روی پوشه والد؛ ساخت پوشه ریشه فقط برای **مدیر آرشیو**.
- سازنده پوشه به‌صورت خودکار دسترسی **کامل** می‌گیرد.

## ۲) اختصاص نفر و سطح دسترسی

سطوح دسترسی (`DocAccessLevel`) — عدد بزرگ‌تر شامل پایین‌تر است:

| سطح | نام | توضیح |
|---|---|---|
| 1 | مشاهده | فقط دیدن عنوان/کد در لیست — محتوا و ورژن‌ها نمایش داده نمی‌شود |
| 2 | خواندن | باز کردن مدرک، دیدن ورژن‌ها، پیوست‌ها و تاریخچه |
| 3 | نوشتن | ثبت/ویرایش مدرک، ثبت ورژن جدید، لینک کردن |
| 4 | کامل | همه‌چیز + مدیریت دسترسی، حذف، فعال/غیرفعال کردن ورژن، دریافت کار در کارتابل |

- **دانلود** یک فلگ مستقل (`CanDownload`) است و جدا از سطح دسترسی داده می‌شود.
- دسترسی هم روی **پوشه** (`DocFolderPermission`) و هم روی **مدرک** (`DocumentPermission`) قابل تعریف است.
- **ارث‌بری:** دسترسی پوشه به زیرپوشه‌ها و مدارک داخلش می‌رسد. **بالاترین سطح برنده است.**
- **«نمایش برای همه»** (`IsPublic`) روی پوشه و مدرک: همه کاربران حداقل سطح *خواندن* می‌گیرند؛ با سوییچ جداگانه می‌توان دانلود عمومی را هم آزاد کرد.
- کمبوی افزودن نفر، **سرچ‌دار** با نمایش آواتار است.

## ۳) فیلدهای مدرک

- **عنوان** — اجباری
- **کد** — اجباری و **یکتا در کل سیستم** (ایندکس یکتا در دیتابیس + اعتبارسنجی با پیام فارسی)
- **شماره مدرک مشتری** (`CustomerCode`) — اختیاری، نیازی به یکتا بودن ندارد؛ در لیست ستون جدا دارد و **جستجو** رویش کار می‌کند
- **تاریخ انقضا** — اختیاری (تاریخ‌شمار شمسی `DatePicker`)؛ مدارک منقضی در لیست قرمز و با آیکن هشدار، و فیلتر «نزدیک انقضا» (۳۰ روز)
- **نفرات گردش/تاییدیه** — کمبوی **چندانتخابی سرچ‌دار** (`MultiSearchSelect`) با چیپ و آواتار
- پوشه، توضیحات، سیاست ورژن، مجوز عمومی، دسترسی اختصاصی

## ۴) لینک مدارک به هم

- **کمبوی چندانتخابی سرچ‌دار** (`MultiSearchSelect`) — هم‌زمان چند مدرک انتخاب می‌شود، هم در فرم ثبت/ویرایش مدرک و هم در پنل «مدارک مرتبط» صفحه جزئیات.
- لینک **دوطرفه** بین مدارک (`DocumentLink`)؛ تکراری‌ها نادیده گرفته می‌شوند.
- فقط مدارک **فعال** قابل لینک شدن هستند.
- هنگام **ورژن خوردن** یک مدرک:
  - در کارتابل همه کاربران دارای **دسترسی کامل** به آن مدرک، کار «اعلان ورژن» می‌نشیند.
  - در کارتابل صاحبان **مدارک مرتبط**، کار «به‌روزرسانی مرتبط» ایجاد می‌شود تا مدرک لینک‌شده بررسی/به‌روزرسانی و در صورت نیاز ورژن بخورد.
  - در تاریخچه مدرک مرتبط هم رویداد ثبت می‌شود.

## ۵) ورژن‌گذاری و پیوست

- همه مدارک ورژن می‌خورند (`DocumentVersion`: شماره ورژن، عنوان، شرح تغییرات، انقضای ورژن، وضعیت).
- روی **هر ورژن** پیوست جداگانه (کامپوننت مشترک `AttachmentBox` با `Module="DocVersion"`).
- ورژن فریز‌شده قابل آپلود پیوست نیست.

## ۶) فریز و گردش تایید

- اگر مدرک نفرات گردش داشته باشد، ورژن جدید بلافاصله:
  - وضعیت **InReview** می‌گیرد، **فریز** می‌شود (`IsFrozen`) و **غیرفعال** می‌ماند → به گردش عادی نمی‌آید.
  - در **کارتابل** همه تاییدکنندگان می‌نشیند + اعلان (SignalR / Push / بله‌وایتا).
- تا پایان گردش، **ورژن جدید دیگری ثبت نمی‌شود**.
- **رد** یک نفر ⇒ کل ورژن رد، از فریز خارج و برای اصلاح به ثبت‌کننده برمی‌گردد.
- **تایید همه** ⇒ فریز برداشته می‌شود و اگر گزینه «پس از تایید خودکار فعال شود» فعال بوده باشد، ورژن **به‌صورت خودکار فعال** می‌گردد.

## ۷) سیاست فعال بودن ورژن

در فرم مدرک، کاربر یکی را انتخاب می‌کند:

- **فقط یک ورژن فعال** — با فعال شدن ورژن جدید، ورژن‌های قبلی خودکار غیرفعال می‌شوند.
- **چند ورژن هم‌زمان فعال** — همه ورژن‌های تاییدشده می‌توانند فعال بمانند.

دارنده دسترسی کامل می‌تواند هر ورژن تاییدشده را دستی فعال/غیرفعال کند (ورژن فریز یا رد‌شده قابل فعال‌سازی نیست).

## ۸) طراحی

عیناً مطابق سایر فرم‌های برنامه: کارت با هدر گرادیانی، `Modal`، `SearchSelect`، `DatePicker` شمسی، `Pager`، Toast فارسی، کلیدهای میانبر F1/F2/Esc، اعداد فارسی (`.Num()` / `.ToFa()`)، RTL و آیکن‌های Bootstrap.

---

## ۹) وضعیت مدرک: فعال / غیرفعال

- هر مدرک وضعیت **فعال/غیرفعال** دارد (`IsActive`) به‌همراه **دلیل**، **تاریخ** و **نام غیرفعال‌کننده**.
- مدرک غیرفعال کاملاً **فقط‌خواندنی** است — این عملیات روی آن **بلاک** می‌شود (با پیام فارسی):
  ویرایش · ثبت ورژن جدید · فعال/غیرفعال کردن ورژن · تایید/رد گردش · لینک کردن (از آن و به آن) · مدیریت دسترسی · آپلود پیوست
- هنگام غیرفعال‌سازی، **کارهای باز آن در کارتابل همه کاربران بسته می‌شود** و دیگر کار جدید (اعلان ورژن / به‌روزرسانی مرتبط) نمی‌گیرد.
- اگر ورژنی **در گردش تایید** باشد، غیرفعال‌سازی اجازه داده نمی‌شود تا اول گردش تعیین‌تکلیف شود.
- مدارک غیرفعال از لیست اصلی حذف شده و در **زیرمنوی «مدارک غیرفعال»** (در درخت پوشه‌ها، همراه با شمارنده) قرار می‌گیرند.
- فعال‌سازی مجدد همه عملیات را برمی‌گرداند. تغییر وضعیت نیازمند **دسترسی کامل** است و به دارندگان دسترسی کامل اعلان می‌رود.
- **حذف** مدرک حالا soft-delete جداگانه‌ای است (`IsDeleted`) و با غیرفعال‌سازی اشتباه نمی‌شود.

## ۱۰) هشدار انقضای مدارک

سرویس پس‌زمینه `DocExpiryWatcher` روزی یک‌بار (پیش‌فرض ساعت ۷ صبح) مدارک را بررسی می‌کند و
برای دارندگان **دسترسی کامل** هر مدرک، **کار کارتابل** (`Kind = "ExpiryAlert"`) و اعلان می‌سازد.

- **آستانه‌های پیش‌فرض:** ۶۰، ۳۰، ۷ و ۰ روز مانده (۰ = امروز منقضی می‌شود / منقضی شده).
- **بدون تکرار:** جدول `DocExpiryAlerts` با ایندکس یکتای `(DocumentId, ThresholdDays, ExpireDate)`
  تضمین می‌کند هر مدرک برای هر آستانه فقط یک‌بار هشدار بگیرد.
- **تشدید سطح:** با نزدیک‌تر شدن تاریخ، آستانه بعدی جداگانه هشدار می‌دهد (۶۰ ⇒ ۳۰ ⇒ ۷ ⇒ ۰).
- **تغییر تاریخ انقضا:** چون `ExpireDate` بخشی از کلید یکتاست، اگر تاریخ عوض شود چرخه هشدار از نو شروع می‌شود.
- **مدارک غیرفعال یا حذف‌شده هشدار نمی‌گیرند.**
- در لیست مدارک: بج شمارش معکوس با رنگ‌بندی (قرمز ≤۷ روز، نارنجی ≤۳۰، آبی ≤۶۰) و در صفحه جزئیات هم نمایش داده می‌شود.
- در درخت کنار لیست: دو زیرمنوی **«منقضی شده»** و **«رو به انقضا»** همراه با شمارنده — شمارنده فقط مدارکی را می‌شمارد که کاربر به آن‌ها دسترسی دارد.
- دکمه **«بررسی انقضا»** برای اجرای فوری (فقط با دسترسی `DocArchive.Manage`).

### تنظیمات (اختیاری، در `appsettings.json`)

```json
"DocArchive": {
  "ExpiryThresholds": [90, 60, 30, 7, 0],
  "ExpiryCheckHour": 7
}
```

## ۱۱) تاریخچه مدرک (Audit Trail)

هر رویداد مهم در جدول `DocumentLogs` ثبت و در کارت **«تاریخچه رویدادها»** صفحه جزئیات نمایش داده می‌شود.

- رویدادهای ثبت‌شده: ایجاد، ویرایش، ورژن جدید، تایید/رد، فعال‌سازی ورژن، لینک/حذف لینک،
  به‌روزرسانی مدرک مرتبط، **تغییر دسترسی‌ها**، غیرفعال‌سازی، هشدار انقضا، حذف و بازگردانی.
- هر ردیف: تاریخ و ساعت، **بج رنگی نوع رویداد**، کاربر، شرح و شماره ورژن مرتبط.
- **فیلتر کشویی** بر اساس نوع رویداد، همراه با تعداد هر نوع.

## ۱۲) سطل بازیافت

حذف مدرک از ابتدا **نرم** بود (`IsDeleted`)؛ حالا رابط کاربری کامل دارد.

- زیرمنوی **«سطل بازیافت»** در درخت با شمارنده — ستون «حذف شده در» و نام حذف‌کننده.
- **بازگرداندن** (نیازمند دسترسی کامل): اگر پوشه اصلی حذف شده باشد مدرک به اولین پوشه منتقل می‌شود و در پیام اعلام می‌گردد.
- **حذف قطعی** (فقط `DocArchive.Manage`): مدرک به‌همراه ورژن‌ها، پیوست‌ها، دسترسی‌ها، لینک‌های دوطرفه،
  کارهای کارتابل، هشدارهای انقضا و تاریخچه پاک می‌شود و **کد مدرک آزاد می‌گردد**.
- **خالی کردن سطل** برای حذف یکجا. هر دو عملیات حذف قطعی نیازمند تایپ عبارت «حذف قطعی» هستند.
- هنگام حذف، کارهای باز کارتابل آن مدرک بسته می‌شوند.

> ⚠️ نکته: ایندکس یکتای کد مدرک شامل مدارک حذف‌شده هم می‌شود؛ یعنی تا وقتی مدرکی در سطل بازیافت است،
> کدش آزاد نمی‌شود. برای استفاده مجدد از کد باید مدرک را حذف قطعی کنید.

## ۱۳) امنیت پیوست‌ها و پیش‌نمایش داخل برنامه

### رفع یک رخنه امنیتی
مسیر دانلود پیوست‌ها `[AllowAnonymous]` بود و هیچ کنترل دسترسی‌ای نداشت؛ یعنی **هر فردی بدون ورود
به سیستم** تنها با حدس زدن شناسه عددی (`/api/attachments/download/1`, `/2`, …) می‌توانست هر فایلی را
دانلود کند و کل سیستم دسترسی چندسطحی دور زده می‌شد.

اکنون:
- `AllowAnonymous` حذف شد؛ دانلود، پیش‌نمایش، لیست، آپلود و حذف همگی نیازمند احراز هویت‌اند.
- سرویس `IAttachmentGuard` بر اساس **ماژول صاحب پیوست** سطح دسترسی را تعیین می‌کند.
- برای `DocVersion` دسترسی از روی مدرکِ صاحبِ ورژن محاسبه می‌شود:
  `View` ⇒ فقط پیش‌نمایش · `Read` به بالا با `CanDownload` ⇒ دانلود · مدرکِ داخل سطل بازیافت ⇒ هیچ دسترسی.
- ماژول‌های دیگر رفتار قبلی (باز برای کاربران واردشده) را حفظ می‌کنند تا چیزی از کار نیفتد؛
  برای محدود کردن هر ماژول کافی است نامش به `Protected` اضافه و قانونش در `AttachmentGuard` نوشته شود.

### پیش‌نمایش بدون دانلود
- endpoint جدید `GET /api/attachments/preview/{id}` فایل را با `Content-Disposition: inline` برمی‌گرداند.
- کامپوننت `FilePreviewModal` فایل را با هدر احراز هویت می‌گیرد و به **blob URL** تبدیل می‌کند —
  بنابراین توکن در نوار آدرس افشا نمی‌شود و پنجره دانلود مرورگر باز نمی‌شود.
- فرمت‌های پشتیبانی‌شده: PDF، PNG، JPEG، GIF، WebP، BMP، SVG و متن ساده.
- در `AttachmentBox` هر پیوست بر اساس دسترسی کاربر نمایش داده می‌شود: دکمه مشاهده، دکمه دانلود
  (فقط در صورت داشتن حق)، بج **«فقط مشاهده»** و آیکون قفل برای فایل‌های غیرقابل پیش‌نمایش.
- این کار سطح دسترسی «مشاهده بدون دانلود» را که در فاز اول طراحی شده بود، بالاخره **کاربردی** می‌کند.

> نکته: JWT از طریق `?access_token=` روی مسیرهای `/download` و `/preview` هم پذیرفته می‌شود
> تا لینک‌های مستقیم `<a href>` کار کنند.

## ۱۴) مقایسه دو ورژن

در کارت «ورژن‌ها» (وقتی مدرک حداقل دو ورژن دارد) دکمه **مقایسه** ظاهر می‌شود.

- انتخاب دو ورژن از کمبوهای بالای مودال + دکمه **جابه‌جایی**؛
  ترتیب اهمیتی ندارد — سرور همیشه قدیمی‌تر را سمت چپ می‌گذارد.
- **چهار بخش خروجی:**
  1. **فیلدهای مدرک** — عنوان، شرح تغییر، تاریخ انقضا، وضعیت، فعال بودن، فریز،
     فعال‌سازی خودکار، ثبت‌کننده، تاریخ ثبت و تاریخ تایید
  2. **نفرات گردش تایید** — چه کسی اضافه/حذف شده و نظرش چه بوده
  3. **پیوست‌ها** — بر اساس نام فایل: مشترک، فقط در یکی
  4. **رویدادهای تاریخچه بین آن دو ورژن**
- رنگ‌بندی: افزوده‌شده سبز، حذف‌شده قرمز با خط خورده، تغییریافته زرد.
- سوییچ **«فقط تفاوت‌ها»** برای پنهان کردن ردیف‌های یکسان (پیش‌فرض روشن).
- بج شمارش کل تغییرات در بالای مودال.

**دسترسی:** حداقل سطح **خواندن** لازم است — سطح «مشاهده» کافی نیست.
مدرکِ داخل سطل بازیافت قابل مقایسه نیست.

## دسترسی‌های RBAC (خودکار به دیتابیس اضافه می‌شوند)

| مجوز | توضیح |
|---|---|
| `DocArchive.Read` | ورود به ماژول و دیدن پوشه/مدارکِ مجاز |
| `DocArchive.Create` | ساخت پوشه و ثبت مدرک |
| `DocArchive.Delete` | حذف پوشه/مدرک |
| `DocArchive.Manage` | **مدیر آرشیو** — دسترسی کامل به همه پوشه‌ها و مدارک |

## صفحات

| مسیر | صفحه |
|---|---|
| `/doc-archive` | پوشه‌ها و مدارک (Explorer) + زیرمنوی مدارک غیرفعال |
| `/doc-archive/documents/{id}` | جزئیات مدرک: ورژن‌ها، گردش، پیوست، مدارک مرتبط، دسترسی‌ها، تاریخچه |
| `/doc-archive/cartable` | کارتابل آرشیو (تایید ورژن / اعلان ورژن / به‌روزرسانی مرتبط) |

## فایل‌های افزوده‌شده

**بک‌اند**
- `Inventory.Api/Entities/DocArchive/DocArchiveEntities.cs` — ۹ موجودیت
- `Inventory.Api/Services/DocArchive/DocAccessService.cs` — محاسبه دسترسی با ارث‌بری
- `Inventory.Api/Services/DocArchive/DocumentService.cs` — مدرک، ورژن، گردش، لینک، کارتابل
- `Inventory.Api/Controllers/DocArchive/DocFoldersController.cs`
- `Inventory.Api/Controllers/DocArchive/DocumentsController.cs`
- `Inventory.Api/Migrations/*_AddDocArchiveModule.cs` — ۹ جدول + ایندکس‌ها
- `Inventory.Api/Migrations/*_AddDocCustomerCodeAndStatus.cs` — ستون‌های `CustomerCode`، `IsDeleted`، `DeactivatedAt`، `DeactivatedByName`، `DeactivateReason`
- `Inventory.Api/Migrations/*_AddDocExpiryAlerts.cs` — جدول `DocExpiryAlerts`
- `Inventory.Api/Migrations/*_AddDocSoftDeleteInfo.cs` — ستون‌های `DeletedAt` و `DeletedByName`
- `Inventory.Api/Services/DocArchive/DocExpiryWatcher.cs` — سرویس پس‌زمینه هشدار انقضا
- `Inventory.Api/Services/AttachmentGuard.cs` — کنترل دسترسی پیوست‌ها
- `Inventory.Client/Shared/FilePreviewModal.razor` + `wwwroot/js/file-preview.js` — پیش‌نمایش فایل
- `Inventory.Client/Pages/DocArchive/VersionCompareModal.razor` + `DiffTable.razor` — مقایسه دو ورژن

**مشترک**
- `Inventory.Shared/Dtos/DocArchiveDtos.cs`

**کلاینت**
- `Inventory.Client/Services/DocArchiveServices.cs`
- `Inventory.Client/Shared/MultiSearchSelect.razor` — کمبوی چندانتخابی سرچ‌دار (قابل استفاده در کل برنامه)
- `Inventory.Client/Pages/DocArchive/DocArchiveExplorer.razor`
- `Inventory.Client/Pages/DocArchive/DocumentView.razor`
- `Inventory.Client/Pages/DocArchive/DocumentFormModal.razor`
- `Inventory.Client/Pages/DocArchive/DocPermissionEditor.razor`
- `Inventory.Client/Pages/DocArchive/DocCartable.razor`

**ویرایش‌شده:** `AppDbContext.cs`، `RbacSeeder.cs`، `Program.cs` (API و Client)، `NavMenu.razor`

## API های افزوده‌شده (فاز دوم)

| متد | مسیر | توضیح |
|---|---|---|
| GET | `/api/doc-archive/documents?status=active\|inactive\|all` | فیلتر وضعیت مدرک |
| POST | `/api/doc-archive/documents/links/many` | لینک چندتایی مدارک مرتبط |
| PUT | `/api/doc-archive/documents/{id}/active` | فعال/غیرفعال کردن مدرک (با دلیل) |
| GET | `/api/doc-archive/documents?expiry=expiring\|expired\|all` | فیلتر انقضا |
| GET | `/api/doc-archive/expiry-summary` | شمارش منقضی‌شده و رو به انقضا (بج‌ها) |
| POST | `/api/doc-archive/expiry-check` | اجرای فوری بررسی انقضا (نیازمند Manage) |
| GET | `/api/doc-archive/documents?status=deleted` | فهرست سطل بازیافت |
| PUT | `/api/doc-archive/documents/{id}/restore` | بازگرداندن مدرک (دسترسی کامل) |
| DELETE | `/api/doc-archive/documents/{id}/purge` | حذف قطعی یک مدرک (Manage) |
| DELETE | `/api/doc-archive/trash` | خالی کردن سطل بازیافت (Manage) |
| GET | `/api/attachments/preview/{id}` | پیش‌نمایش inline پیوست (بدون نیاز به حق دانلود) |
| GET | `/api/doc-archive/documents/{id}/compare?from=&to=` | مقایسه دو ورژن (نیازمند سطح خواندن) |

## اجرا

```bash
cd inventory/src/Inventory.Api
dotnet ef database update      # جداول جدید ساخته می‌شوند
dotnet run
```
پرمیشن‌های `DocArchive` در اولین اجرا خودکار seed شده و به نقش Admin داده می‌شوند.
