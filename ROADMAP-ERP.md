# نقشه راه ماژول ERP — گام‌های پیشنهادی بعدی

> وضعیت: ۱۴۰۵-۰۶-۱۵ (سپتامبر ۲۰۲۶) — بر پایه بررسی کد فعلی (brunch main, commit 84d33ec)
> وضعیت فعلی خوب: RBAC ماژول‌محور، AuditLog، مایگریشن‌های EF، SignalR، Push (WebPush)، فشرده‌سازی داده‌های Legacy آریا، مستندات ماژول (MODULE-*.md)، راهنمای DEPLOY.

---

## 🔴 اولویت ۱ — زیرساخت کیفیت (الان صفر است، بیشترین اثر)

### 1.1 راه‌اندازی CI با GitHub Actions ⬜
- هیچ فایل `.github/workflows` وجود ندارد → هر تغییر فقط با build دستی تأیید می‌شود.
- مینیموم: `build + test` روی هر push/PR + `publish` سایت Blazor و API در هر تگ.
- خروجی: آرتیفکت برای استقرار (API + wwwroot کلاینت + SQL اسکریپت).

### 1.2 پروژه‌های تست ⬜
- هیچ پروژه‌ی Test در solution نیست؛ منطق مالی (انبارداری → حسابداری → فاکتور → خزانه) بدون تست، ریسک‌آفرین است.
- `Inventory.Api.Tests`: xUnit + `WebApplicationFactory` (Integration) + Testcontainers برای SQL Server واقعی.
- `Inventory.Client.Tests`: bUnit برای کامپوننت‌های Blazor (مخصوصاً فرم‌های اسناد و NavMenu).
- تمرکز اول: قواعد مالی — شماره‌گذاری سند، بستن دوره، گردش انبار، idempotency ارسال به حسابداری.

### 1.3 Test Seed پایدار ⬜
- داده‌ی اولیه ثابت برای تست‌ها + سناریوی «مودولار»: فاکتور → سند حسابداری → سند خزانه → انبارگردانی.

---

## 🟠 اولویت ۲ — مهندسی و نگهداشت کد

### 2.1 نگهبان‌های کد (Guardrails) ⬜
- `.editorconfig` + `.gitattributes` (خطای CRLF روی سیستم‌های مختلف) + `Directory.Build.props`.
- Analysers مایکروسافت (EnforceCodeStyleInBuild) و رفع ~۲۰ هشدار فعلی (CS8600/CS8601/CS1998 …).
- تصمیم: `Nullable` روی همه پروژه‌ها فعال و در بلندمدت «خطا» شود.

### 2.2 حذف دوگانگی اسکریپت SQL دستی vs مایگریشن ⬜
- همزمان `Migrations/*.cs` و `sql/*/AddXxxModule.sql` (+ `.original.cs.txt`) نگهداری می‌شوند → خطر انحراف (drift).
- پیشنهاد: اسکریپت‌ها را با `dotnet ef migrations script` تولید کنید یا کاملاً حذف و فقط مایگریشن بماند.
- خط `NuGet.config` (رفع packageSourceMapping) می‌تواند به `Repository.props` منتقل شود.

### 2.3 مدیریت بسته‌ها ⬜
- `Directory.Packages.props` (Central Package Management) + `Renovate`/`Dependabot` برای PR خودکار به‌روزرسانی.
- بررسی مجوزها: **QuestPDF** و **SixLabors.ImageSharp** — برای شرکت‌های با درآمد سالانه > ۱ میلیون دلار لایسنس تجاری لازم است.

### 2.4 Observability ⬜
- لاگ ساخت‌یافته: Serilog (فایل + SEQ) + Correlation ID در همه درخواست‌ها.
- مدیریت خطای سراسری: Middleware + `ProblemDetails` (الان مسیرها مستقیم try/catch یکنواختی ندارند).
- `AddHealthChecks` (مسیر `/health` و `/health/ready`) + متریک‌ها (OpenTelemetry یا Prometheus).

### 2.5 Job زمان‌بندی‌شده ⬜
- امروز فقط `DocExpiryWatcher` به‌صورت `HostedService` است؛ برای گزارش دوره‌ای، بکاپ، اعلان و هشدار سیستمی: Hangfire یا Quartz + داشبورد.

---

## 🟡 اولویت ۳ — تکمیل منطق کسب‌وکار ماژول ERP

### 3.1 ادغام ماژول Export (WIP) ⬜
- ۷ فایل `Services/Export/*` فقط روی برنچ `erp-five-modules` است و وارد main نشده.
- تکمیل و ادغام: خروجی یکپارچه اکسل/PDF برای همه ماژول‌ها + جایگزینی مستقیم‌کاری‌های ClosedXML.

### 3.2 گزارش‌های مالی رسمی ⬜
- داشبورد مالی هست؛ گزارش‌های ترازنامه، سود و زیان، دفتر روزنامه/کل، گزارش دوره و مقایسه سال قبل باید ساخته شوند.

### 3.3 سال مالی شمسی و بستن دوره ⬜
- `PersianDate` هست؛ تعریف سال مالی (از ۱ فروردین) + قفل اسناد دوره بسته + شماره‌گذاری مستمر و بدون شکاف.
- گزارش موجودی و مغایرت‌گیری (reconciliation) بین سه ماژول مالی.

### 3.4 سامانه مودیان و فاکتور الکترونیکی ⬜
- اگر فاکتور فروش دارید، الزام قانونی صدور فاکتور الکترونیکی (مودیان) و اتصال چاپگر مالی مطرح است؛ این بزرگ‌ترین گام «بیرون از کد» است.

### 3.5 حلقه‌های مالی بین ماژول‌ها ⬜
- انبار → حسابداری → فاکتور → خزانه: تست کنید سند دوبل ثبت نشود (idempotency)، ارسال مجدد بعد از خطا، و گزارش مغایرت.

### 3.6 پشتیبانی موبایل/اسکنر ⬜
- انبارگردانی و بارکد: حالت PWA آفلاین (service worker هست) + اسکنر USB/بلوتوثی + چاپ برچسب.

---

## 🟢 اولویت ۴ — استقرار و عملیات

### 4.1 Docker و استقرار ⬜
- `Dockerfile` + `docker-compose` (SQL Server + API + Client) کنار `DEPLOY.md`؛ یا اسکریپت publish و IIS.

### 4.2 امنیت ⬜
- رمزها خارج از appsettings: `user-secrets` یا متغیر محیط (الان `ConnectionStrings` در appsettings است).
- HTTPS redirection + HTTPS، CORS دقیق (الان سیاست `wasm` عمومی است)، **Rate limiting** (الان نیست)، Security Headers.
- پشتیبان‌گیری خودکار دیتابیس + تست بازیابی.

### 4.3 مقیاس‌پذیری SignalR ⬜
- الان in-memory است؛ برای چند اینستنس `Redis Backplane` لازم است.

---

## ⚪ اولویت ۵ — پروسه و تیم

### 5.1 نسخه‌بندی ⬜
- Tag نسخه (v0.1.0…) + CHANGELOG + گیت‌فلوی مشخص (main محافظت‌شده، PR review اجباری).

### 5.2 پاکسازی ریپو ⬜
- ۲۰+ برنچ قدیمی (`arena/…`، `Abbac`، `Lak`، `Salehian`، …) — تصمیم: بایگانی/حذف.
- اگر تاریخچه هم خواستید سبک شود: rebase/squash + push --force؛ **قبلش بکاپ کامل** بگیرید.
- فایل‌های بزرگ `*.Designer.cs` مشروع‌اند؛ حجم ریپو را می‌توان با حذف برنچ‌های مرده کم کرد.

### 5.3 مستندات تصمیم ⬜
- ADR برای تصمیم‌های معماری (مثلاً چرا SQL دستی در کنار مایگریشن).
- مستندات ماژول‌ها (MODULE-*.md) — عالی‌اند؛ فقط با هر تغییر ماژول به‌روز شوند.

---

## ترتیب پیشنهادی اجرا (۴ هفته)

| هفته | گام‌ها |
|------|--------|
| ۱ | 1.1 (CI)، 2.1 (guardrails)، 2.2 (رفع دوگانگی SQL) |
| ۲ | 1.2+1.3 (تست‌های مالی)، 3.1 (ادغام Export) |
| ۳ | 2.4 (Observability)، 3.3 (سال مالی/بستن دوره) |
| ۴ | 4.1 (Docker)، 3.2 (گزارش‌های مالی)، 4.2 (امنیت) |

> گام بعدی فوری پیشنهادی: **CI + پروژه تست + اولویت اول تست‌های قواعد مالی** — چون الان هیچ‌کدام نیست و بیشترین ریسک در منطق مالی ماژول‌های پنج‌گانه است.
