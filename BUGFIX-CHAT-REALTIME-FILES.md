# اصلاح شمارنده، ارتباط زنده و فایل‌های پیام‌رسان

تاریخ: ۲۰۲۶-۰۹-۰۷ — این نسخه اصلاح جست‌وجوی قبلی را نیز حفظ می‌کند.
تغییرها در سورس فضای کاری انجام شده‌اند؛ نصب عملیاتی و مخزن GitHub تغییر نکرده‌اند.

## ۱. پیام‌های خوانده‌نشده

- تعداد پیام نخوانده کنار **نام مخاطب/گروه** قرار گرفت، نه انتهای پیش‌نمایش پیام.
- شمارندهٔ هر گفتگو و نشان هدر با رویدادهای زنده به‌روز می‌شوند.
- تعداد واقعی از پیام‌های حذف‌نشده و نشانگر آخرین پیام خوانده‌شده محاسبه می‌شود؛ دریافت تکراری یک رویداد عدد را دو برابر نمی‌کند.
- فقط پیام‌های نمایش‌داده‌شده علامت خواندن می‌گیرند. پیام جدیدتری که هم‌زمان برسد نخوانده می‌ماند و نشانگر خواندن عقب نمی‌رود.
- خواندن در یک تب، از طریق اعلان سرور به تب‌های همان حساب و فرستنده نیز اطلاع داده می‌شود؛ صفحهٔ پنهان پیام تازه را صرفاً به‌دلیل باز بودن گفتگو خوانده اعلام نمی‌کند.

## ۲. نمایش زنده، تایپ و اتصال مجدد

علت‌های قبلی شامل اتکا به echo سوکت برای نمایش پیام خود کاربر، ارسال تکراری از اتاق و کانال خصوصی، استفاده از `IsOutgoing` فرستنده برای همهٔ گیرندگان، ناهماهنگی امضای رویداد ویرایش و نبود تلاش مجدد برای اتصال اولیه بود.

- پاسخ موفق HTTP بلافاصله در صفحه درج می‌شود؛ نمایش پیام خود کاربر به فعال بودن سوکت وابسته نیست.
- پیام‌ها با شناسهٔ سرور ادغام می‌شوند تا echo سوکت حباب تکراری نسازد. جهت حباب برای هر گیرنده جداگانه تعیین می‌شود.
- رویدادها از یک مسیر خصوصی برای هر عضو ارسال و در dispatcher بلزور اعمال می‌شوند.
- وضعیت تایپ با ورودی متن ارسال می‌شود، هنگام ادامهٔ تایپ زودتر از موعد پاک نمی‌شود و با توقف یا ارسال پیام پاک می‌شود.
- Enter پیام را ارسال می‌کند؛ Shift+Enter خط جدید می‌سازد. تأیید ورودی IME به‌اشتباه ارسال تلقی نمی‌شود.
- اتصال اولیهٔ ناموفق، قطع اتصال و بازیابی عضویت اتاق‌ها مدیریت می‌شوند. پیام‌های زمان قطع اتصال از API بازیابی می‌شوند. یک همگام‌سازی پشتیبان نیز فعال است: در حالت متصل حدود ۳۰ ثانیه و در حالت قطع اتصال حدود ۵ ثانیه، با لحاظ زمان تلاش اتصال.
- هاب JWT معتبر می‌خواهد؛ `userId` در query هویت کاربر را تعیین نمی‌کند. عضویت پیش از پیوستن یا اعلان تایپ کنترل می‌شود.

## ۳. آپلود، پیش‌نمایش و دانلود

- فایل با نام، طول و MIME معتبر سمت سرور ثبت می‌شود؛ مقدار جعلی فرستاده‌شده در درخواست پیام جایگزین این اطلاعات نمی‌شود.
- فایل‌های جدید خارج از `wwwroot` در **`App_Data/chat`** ذخیره می‌شوند و اطلاعات مالکیت در جدول **`ChatAttachments`** قرار دارد.
- انتخاب فایل ابتدا وضعیت بارگذاری و سپس «آمادهٔ ارسال» را نمایش می‌دهد. **برای فرستادن فایل، دکمهٔ ارسال را بزنید**؛ می‌توانید پیش از ارسال متن هم اضافه کنید.
- اگر ارسال پیام شکست بخورد، متن و پیوست برای تلاش مجدد حفظ می‌شوند. انتخاب دوبارهٔ همان نام فایل نیز کار می‌کند. هنگام بارگذاری/ارسال، جابه‌جایی گفتگو متوقف می‌شود تا فایل برای شخص دیگری ارسال نشود.
- سقف هر فایل **۵۰ MiB** است. بدنهٔ HTTP فضای اضافه برای multipart دارد. فایل خالی و بیش‌ازحد مجاز رد می‌شود.
- دانلود از مبدأ API انجام می‌شود، نه مبدأ احتمالی سرور جداگانهٔ Client. نام فارسی، MIME صحیح، درخواست Range و فایل‌های بزرگ پشتیبانی می‌شوند.
- تصویر، صدا و ویدیو پیش‌نمایش دارند؛ همهٔ پیوست‌ها دکمهٔ دانلود دارند. HTML/SVG و انواع ناشناخته به‌صورت فایل دانلود می‌شوند، نه محتوای فعال inline.
- دسترسی فایل با نشست معتبر و عضویت در گفتگو کنترل می‌شود. فایل بارگذاری‌شدهٔ ارسال‌نشده در اختیار بارگذار است.
- پیام‌های قدیمیِ دارای مسیر `/uploads/chat/...` از endpoint مجازِ همان پیام خوانده می‌شوند؛ فایل‌های قبلی منتقل یا حذف نشده‌اند. دسترسی مستقیم استاتیک، حتی با مسیرهای معادل مانند `/uploads//chat/...`، در برنامه مسدود است.

مسیرهای مهم:

- `POST /api/chat/conversations/{id}/attachments`
- `GET /api/chat/messages/{id}/download`
- `GET /api/chat/messages/{id}/preview`
- `POST /api/chat/upload` برای سازگاری با کلاینت قبلی؛ فایل بدون گفتگو هنگام اولین ارسال به گفتگوی مجاز متصل می‌شود.

آدرس‌های مرورگری دانلود/رسانه به توکن نشست وابسته‌اند. آن‌ها را به‌عنوان لینک عمومی منتشر نکنید؛ فایل را داخل گفتگو به اشتراک بگذارید. در reverse proxy نیز مقدار `access_token` را از لاگ URLها حذف/پوشیده کنید.

## ۴. تغییر دیتابیس و نگهداری فایل‌ها

مایگریشن جدید: `20260907081538_AddPrivateChatAttachments`.

- در SQL Server از زنجیرهٔ EF اجرا می‌شود. اسکریپت ارتقا از نسخهٔ قبلی چت نیز در `inventory/sql/chat/AddPrivateChatAttachments.sql` قرار دارد.
- SQLite موجود از `ChatAttachmentSchemaV1` برای ایجاد جدول مفقود استفاده می‌کند؛ `EnsureCreated()` به‌تنهایی کافی نیست.
- مسیر ارتقا کاربران، گفتگوها و پیام‌ها را حذف نمی‌کند. بازگردانی خودکار این مایگریشن مسدود است تا اطلاعات مالکیت فایل‌ها حذف نشود.
- فایل‌های ارسال‌نشده/کنارگذاشته‌شده خودکار پاک نمی‌شوند؛ برای پاک‌سازی دوره‌ای باید سیاست نگهداری جداگانه و بررسی‌شده تعریف شود.

## ۵. علت و رفع «فایل پیوست روی سرور یافت نشد.» پس از استقرار مجدد

**گزارش کاربر:** حتی فایل تازه‌بارگذاری‌شده نیز هنگام دانلود با خطای «فایل پیوست روی سرور یافت نشد.» مواجه می‌شود و گفتگوها/پیام‌ها (ردیف دیتابیس) سالم‌اند.

**بازتولید قطعی (شبیه‌سازی دو استقرار روی یک دیتابیس SQLite):**
فایل در نمونهٔ A (پوشهٔ ریلیز A) آپلود و پیام ارسال شد؛ دانلود از همان نمونه ۲۰۰ شد.
سپس نمونهٔ B با **همان دیتابیس** ولی در **پوشهٔ ریلیز تازه** (App_Data خالی) راه‌اندازی شد؛ دانلود همان پیام از B شد:
`404 {"message":"فایل پیوست روی سرور یافت نشد."}` — یعنی ردیف دیتابیس هست، اما فایل فیزیکی در پوشه‌ای است که نمونهٔ جدید دنبالش می‌گردد. درخواست بدون نشست همان‌جا ۴۰۱ ماند (مدل مجوز دست‌نخورده).

**علت:** محل پیش‌فرض ذخیره (`App_Data/chat` کنار API) زیر پوشهٔ ریلیز ساخته می‌شود؛ هر استقرار در پوشهٔ تازه یا بازآفرینی App_Data، فایل‌های قبلی را از دید نمونهٔ جدید خارج می‌کند.

**تغییر سورس (`ChatAttachmentService`):**
- دو تنظیم جدید (اختیاری؛ مقدار خالی = رفتار قبلی): `Files:ChatRoot` (ذخیرهٔ جدید، خارج از wwwroot) و `Files:ChatLegacyRoot` (فایل‌های قدیمی `wwwroot/uploads/chat`). در `appsettings.json` مستند شده‌اند؛ معادل متغیر محیط: `Files__ChatRoot=...`.
- در راه‌اندازی، مسیر واقعی ذخیره‌گاه با نوشتن/حذف probe ثبت می‌شود:
  `[ChatFiles] ذخیره‌گاه فایل‌های چت '...' آماده است (contentRoot: '...', پوشهٔ جاری: '...')` — با همین خط در لاگ سرویس، مسیر مؤثر هر نمونه را چک کنید.
- اگر فایل در مسیر اصلی نبود، پیش از اعلام خطا چند مسیر جایگزین شناخته‌شده بررسی می‌شود: `Files:ChatRoot`، `{ContentRoot}/App_Data/chat`، `{پوشهٔ جاری فرآیند}/App_Data/chat` (و همین سه‌گانه برای ریشهٔ قدیمی). هیچ مسیر دلخواهی پذیرفته نمی‌شود (محدودیت مسیر حفظ شده است).
- اگر فایل در مسیر جایگزین پیدا شود، لاگ هشدار «در مسیر اصلی نبود؛ از مسیر جایگزین سرو می‌شود» ثبت و دانلود با موفقیت انجام می‌شود — بدون جابه‌جایی فایل.

**اقدام عملیاتی برای نصب کاربر (ترتیب پیشنهادی):**
1. بکاپ دیتابیس و همهٔ پوشه‌های داده (از جمله تمام `App_Data/chat` های قدیمی در هر ریلیز قبلی).
2. یک پوشهٔ پایدار برای فایل‌های چت تعریف کنید (مثلاً `D:\ChatFiles` یا `/var/lib/totall/chat` یا پوشهٔ ریلیزی که واقعاً فایل‌ها در آن‌اند) و دسترسی نوشتن حساب سرویس را بدهید.
3. همان مسیر را روی همهٔ نمونه‌ها به‌صورت `Files__ChatRoot` (یا در `appsettings.json`) تنظیم کنید. برای دانلود پیام‌های قدیمیِ مسیر `wwwroot/uploads/chat`، `Files:ChatLegacyRoot` را نیز در صورت نیاز همان‌طور تنظیم کنید.
4. در صورت تمایل، فایل‌های `App_Data/chat` قدیمی را به پوشهٔ پایدار منتقل کنید؛ تا زمان انتقال، fallback همان فایل‌ها را از جای قبلی سرو می‌کند (جابه‌جایی با توقف کوتاه سرویس امن‌تر است).
5. API را راه‌اندازی و خط probe در لاگ را بررسی کنید؛ دانلود یک پیام قدیمی با پیوست را در UI یا از `GET /api/chat/messages/{id}/download` امتحان کنید.

**شبیه‌سازی دو نمونه پس از رفع:** نمونهٔ B با `Files__ChatRoot` اشاره‌شده به پوشهٔ واقعی فایل‌ها، پیام قدیمی نمونهٔ A را ۲۰۰ و بایت‌به‌بایت یکسان دانلود کرد؛ فایل تازه‌ای که B آپلود کرد در همان پوشهٔ مشترک نوشته شد و A نیز آن را ۲۰۰ دانلود کرد (سازگاری چندنمونه‌ای/چندریلیزی).

## ۶. دانلود پیوست در همان صفحه — رفع «تب دوم نرم‌افزار باز می‌شود و فقط با Ctrl+Shift+R دانلود می‌شود»

**گزارش کاربر:** روی فایل (یا تصویر) در پیام‌رسان کلیک می‌کند؛ به‌جای دانلود همان‌جا، تب/پنجرهٔ جدیدی باز می‌شود که خودِ نرم‌افزار را نشان می‌دهد؛ بعد از Ctrl+Shift+R در آن تب، بالاخره فایل دانلود می‌شود.

**علت (دو لایه):**
1. کلاینت، فایل را با `<a target="_blank">` (و کلیک روی تصویر، تب جدید با آدرس preview) باز می‌کرد؛
2. سرویس‌ورکر PWA (`service-worker.published.js`) در `onFetch` همهٔ **درخواست‌های ناوبری** را cache-first از `index.html` جواب می‌داد مگر اینکه دقیقاً یکی از فایل‌های asset باشد. پس باز شدن آدرس فایل (`/api/chat/messages/{id}/download|preview`) در تب جدید، از کشِ برنامه سرو می‌شد (نمایش خودِ نرم‌افزار) و هرگز به سرور نمی‌رسید. **Ctrl+Shift+R سرویس‌ورکر را دور می‌زند** و درخواست مستقیم به سرور می‌رود — به همین دلیل فقط با رفرش سخت دانلود انجام می‌شد.

**تغییر سورس:**
- `ChatPage.razor`:
  - دکمهٔ دانلود پیام: `target="_blank"` حذف شد؛ لینک دانلود همان صفحه/تب کار می‌کند (پاسخ سرور `Content-Disposition: attachment` دارد).
  - کلیک روی تصویر پیام: به‌جای تب جدید، **لایت‌باکس در همان صفحه** باز می‌شود (پیش‌نمایش بزرگ + دکمهٔ «دانلود فایل» همان‌جا)؛ بستن با دکمه/کلیک پشت لایت‌باکس.
- `service-worker.published.js` (`onFetch`): مسیرهای داده‌ای — `/api/…`، `/hubs/…`، `/signalr/…` و هر URL دارای `access_token` — همیشه مستقیم به شبکه می‌روند و هرگز از کش سرو نمی‌شوند و با `index.html` پاسخ داده نمی‌شوند (توکن هم در کش ذخیره نمی‌شود). رفتار کشِ خودِ برنامه (نمایش آفلاین برای مسیرهای عادی) دست‌نخورده است.
- مدل مجوز هیچ تغییری نکرد: همان endpoint دارای کنترل عضویت؛ فقط کلید رفتار UI و SW اصلاح شد.

**نکتهٔ استقرار:** کاربرانی که قبلاً نسخه را باز کرده‌اند، سرویس‌ورکر کهنه را دارند؛ پس از انتشار، برنامه را کامل ببندند یا دو بار رفرش کنند تا سرویس‌ورکر جدید فعال شود (روش چک: در DevTools > Application > Service Workers نسخهٔ فعال را ببینند). برای اطمینان از دانلود همان‌جا، کش/دادهٔ سایت هم گزینهٔ مطمئن‌تری است.

## ۷. انتشار روی نصب فعلی

1. ابتدا از **دیتابیس، `App_Data`، `wwwroot/uploads`، `SecureFiles` و کلیدهای رمزنگاری موجود** پشتیبان بگیرید. قبل از نصب عملیاتی روی محیط آزمایشی بررسی کنید.
2. هر دو پروژهٔ API و Client باید دوباره Publish شوند؛ فقط تغییر فایل Razor یا انتشار یکی از آن‌ها کافی نیست. روش تک‌سروره در `DEPLOY.md` آمده است.
3. فایل‌های داده، کلیدها و تنظیمات اتصالِ نصب خودتان را حفظ کنید. گزینهٔ پاک‌کردن فایل‌های اضافی مقصد در ابزار استقرار نباید این پوشه‌ها را حذف کند.
4. API جدید را راه‌اندازی کنید تا ارتقای دیتابیس اجرا شود؛ حساب دیتابیس باید مجوز لازم داشته باشد. مسیر `App_Data/chat` نیز باید برای حساب سرویس قابل نوشتن باشد.
5. reverse proxy باید `/hubs/chat` را درست عبور دهد؛ برای WebSocket، Upgrade و مهلت اتصال مناسب را فعال کنید.
6. **محدودیت IIS/nginx مستقل از محدودیت برنامه است.** برای ارسال فایل ۵۰ MiB، سقف بدنهٔ درخواست حداقل `53477376` بایت (۵۱ MiB) لازم است. در nginx مقدار `client_max_body_size 51m;` در location مربوط به API چت نمونهٔ مناسبی است. در IIS، `requestFiltering/requestLimits/maxAllowedContentLength` را نیز متناسب تنظیم کنید. تنظیمات proxy/IIS در این محیط تغییر نکرده‌اند.
7. اگر nginx/IIS فایل‌های استاتیک را **مستقیماً و بدون عبور از ASP.NET Core** سرو می‌کند، مسیر `/uploads/chat/` را در همان لایه نیز مسدود کنید؛ دانلود فایل قدیمی باید از `/api/chat/messages/{id}/download` عبور کند. فایل‌های جدید از ابتدا خارج از ریشهٔ استاتیک هستند.
8. پس از انتشار، تب‌ها/PWA را ببندید و دوباره باز کنید تا کلاینت و **سرویس‌ورکر جدید** فعال شوند (دو بار رفرش یا بستن کامل برنامه برای فعال‌شدن SW کافی است)؛ در صورت باقی ماندن نسخهٔ قبلی، کش برنامه را پاک و دوباره وارد شوید.
9. پس از اولین استقرارِ دارای فایل چت، `Files:ChatRoot` را روی مسیر پایدار/اشتراکی تنظیم کنید (بخش ۵)؛ تا آن زمان `App_Data/chat` زیر پوشهٔ ریلیز ساخته می‌شود و با استقرار بعدی در پوشهٔ تازه، دانلود پیوست‌های قبلی خطای «روی سرور یافت نشد» می‌دهد.

این بسته فقط سورس است؛ فایل‌های واقعی نصب، دیتابیس آزمایشی، SDK و خروجی‌های `bin/obj` در آن نیستند.

## ۸. اعتبارسنجی انجام‌شده

- **۵۷ آزمون C# موفق**: جست‌وجوی قبلی، شمارنده و خواندن محدود، فایل و مالکیت، MIME، رد ورودی نامعتبر، مسیرهای استاتیک معادل، حفظ اطلاعات در ارتقای تکراری و تطابق مدل/snapshot؛ به‌علاوهٔ ۴ آزمون تازهٔ ذخیره: ریشهٔ پیکربندی‌پذیر (`Files:ChatRoot`)، بازگشت به مسیرهای جایگزین پس از پاک‌شدن ذخیره‌گاه اصلی همراه با آزمون رد مسیر ناشناس، سازگاری فایل‌های قدیمی از ریشهٔ جایگزین و بی‌اثری مقدار خالی تنظیم.
- **۱۱ سناریوی HTTP/SignalR واقعی موفق**: هویت معتبر، جلوگیری از جعل userId و ورود غیرعضو، تایپ، تحویل یک‌باره با جهت صحیح، رسید خواندن، ویرایش، فایل فارسی، Range، فایل ۳۲ MiB با مقایسهٔ hash و رد فایل بیش از سقف.
- **۱۳ سناریوی مرورگر با چند حساب مستقل موفق**: پیام بدون رفرش، نشان نخوانده کنار نام، تیک خواندن، تایپ مداوم، Enter/Shift+Enter، دانلود واقعی با نام فارسی، انتخاب مجدد فایل، پیش‌نمایش PNG/WAV/WebM واقعی، حفظ پیوست هنگام خطا، قطع سوکت، بازیابی پیام و شکست اتصال اولیه.
- آزمون‌های قبلی نیز موفق بودند: ۳۱ بررسی API پیام‌رسان و ۱۱ سناریوی مرورگر جست‌وجو.
- پس از راه‌اندازی مجدد، تعداد کاربران، گفتگوها، اعضا، پیام‌ها و اطلاعات پیوست‌ها ثابت ماند و hash فایل قدیمی و فایل خصوصی جدید یکسان بود.
- **شبیه‌سازی دو استقرار**: نمونهٔ B در پوشهٔ ریلیز تازه همان خطای کاربر (۴۰۴ «فایل پیوست روی سرور یافت نشد.») را داد؛ پس از تنظیم `Files__ChatRoot` به پوشهٔ واقعی فایل‌ها، دانلود متقابل A↔B موفق شد (بخش ۵).
- **۹ سناریوی مرورگر با سرویس‌ورکر فعال (PWA)**: دانلود با کلیک روی دکمه در همان صفحه بدون تب اضافه؛ کلیک روی تصویر → لایت‌باکس همان صفحه و دانلود از داخل آن؛ واکشی/ناوبری مستقیم به آدرس دانلود و preview به سرور می‌رسد نه به شِلِ کش‌شدهٔ برنامه؛ و مسیرهای عادی SPA همچنان شِل کش را می‌گیرند (گارد هدفمند است). این سوئیت قبلاً با `service_workers="block"` اجرا می‌شد و همین حفره را نمی‌دید — هم‌اکنون هم حالت بدون SW و هم حالت با SW سبز است (بخش ۶).
- **صفحهٔ آرشیو اسناد**: در کلاینت منتشرشده با مرورگر تازه (بدون کش PWA) مسیر `/doc-archive` برای حساب admin و کاربر تازهٔ Operator بدون خطای صفحه باز شد و منوی «آرشیو اسناد و مدارک» + «کارتابل آرشیو» دیده می‌شود؛ کد صفحه (Explorer/Cartable/View/Form/MyArchive) در سورس موجود است. اگر در نصب کاربر دیده نمی‌شود، ترتیب بررسی: کش/PWA مرورگر، کلاینت منتشرنشده، دسترسی نقش به ماژول DocArchive (منو با `Auth.HasModule("DocArchive")` پنهان می‌شود).
- API ساخته و Client در Release منتشر شد. به‌دلیل محدودیت ۲ گیگابایتی محیط و کمبود حافظهٔ analyzerهای اختیاری، اجرای نهایی ساخت/آزمون API با `-m:1 -p:RunAnalyzers=false` انجام شد؛ این تغییر فقط در فرمان اجراست، نه در تنظیمات پروژه.

**محدودیت:** آزمون اجرایی دیتابیس با SQLite انجام شده است. اسکریپت SQL Server تولید و مدل آن بررسی شده، اما SQL Server واقعی و تنظیمات IIS/nginx نصب شما در این محیط اجرا یا تأیید نشده‌اند. این گزارش ادعای ممیزی امنیتی تمام ماژول‌های نرم‌افزار نیست.
