# قرارداد همکاری تیمی

این سند قوانینی است که با قلاب‌های git به‌صورت خودکار اعمال می‌شوند. خلاصهٔ دلیلش در بخش «چرا» هر قانون آمده.

---

## گام صفر — یک بار برای همیشه

```bash
./tools/install-hooks.sh
```

این اسکریپت هویت git شما را می‌پرسد و تنظیم می‌کند، قلاب‌ها را فعال می‌کند و اگر در فهرست تیم نیستید به شما می‌گوید.

---

## قانون ۱ — هویت واقعی

هر commit باید به یک **انسان مشخص** برسد. دو حالت پذیرفته است:

**حالت الف) خودتان commit می‌زنید:**
```bash
git config user.name  "Ali Lak"
git config user.email "ali@example.com"
```

**حالت ب) ایجنت AI کد را زده:**
```bash
git commit --trailer "Requested-by=Ali Lak <ali@example.com>"
```

نام و ایمیل باید **دقیقاً** با `tools/team-members.txt` یکی باشد.

### چرا

در تاریخچهٔ این مخزن ۷۶٪ از commitهای `main` به نام «Arena Agent» ثبت شده و ۲۱ هویت نویسندهٔ مختلف وجود دارد. برنچ‌هایی به نام `Lak` و `Salehian` و `Abbac` هست ولی حتی یک commit به نام خود آن افراد ثبت نشده. نتیجه اینکه git نمی‌توانست بگوید چه کسی روی چه ماژولی کار کرده — و مجبور می‌شدیم دستی ممیزی بنویسیم.

---

## قانون ۲ — نام برنچ

```
<ماژول>/<کلیدکار>-<توضیح-کوتاه>
```

| درست | نادرست |
|---|---|
| `hr/HR-42-payroll-overtime` | `Lak` |
| `office/OFF-17-incoming-letter-archive` | `Salehian` |
| `docarchive/DOC-3-ocr-tags` | `fix` |
| `itreassets/IT-9-workorder-filter` | `arena/01a07a0e-totallcollectionsoftware` |

ماژول‌های معتبر در `tools/ownership-map.csv` فهرست شده‌اند.

### چرا

**برنچ به‌نام شخص، ضدالگوی اصلی این مخزن بود.** برنچ شخصی یعنی:
- یک نفر می‌تواند هر ماژولی را بدون بازبینی عوض کند
- عمر برنچ هفته‌ها طول می‌کشد و merge آن دردناک می‌شود
- هیچ‌کس نمی‌داند آن برنچ الان روی چه کاری است
- ۴۴ برنچ موقت رهاشده جمع شد که ۱۵ مگابایت زباله در تاریخچه نگه داشته بودند

برنچ به‌نام **کار**، عمر کوتاه دارد، ماژولش از نامش پیداست، و وقتی merge شد می‌توان حذفش کرد.

---

## قانون ۳ — هیچ push مستقیمی به `main`

همه‌چیز از راه Pull Request. حتی یک خط اصلاح.

```bash
git switch -c hr/HR-42-payroll-overtime
git push -u origin hr/HR-42-payroll-overtime
gh pr create --title "HR-42: اضافه‌کردن اضافه‌کاری حقوق" --label "module:Hr" --fill
```

### چرا

PR واحدِ «چه آیتمی در حال انجام است» است. وقتی PR باز است یعنی کار در جریان است؛ وقتی merge شد یعنی تمام شده. این وضعیت **خودکار** است و هیچ‌کس لازم نیست به‌روزش کند — بر خلاف یک فایل markdown تاریخ‌دار که از روز نوشتن کهنه می‌شود.

---

## قانون ۴ — اندازهٔ PR

یک PR باید یک کار مشخص باشد. اگر توصیفتان بیشتر از پنج جمله شد، PR بزرگ است و باید بشکند.

### چرا

۲۹ جفت commit تکراری روی `main` پیدا شد — همان پیام، همان تاریخ، همان محتوا، دو بار commit شده. این نشانهٔ کار موازی روی تغییرات بزرگ و بدون هماهنگی است.

---

## قانون ۵ — هرگز دادهٔ واقعی در مخزن

هیچ فایل حاوی رکورد پرسنلی، dump پایگاه داده، csv ورود و خروج، یا zip بایگانی commit نکنید.

```gitignore
*.zip
*.tar.gz
*.bak
inventory/sql/legacy-aria/
**/tests/parity/expected.json
```

### چرا

در سپتامبر ۲۰۲۶ مشخص شد مخزن عمومی بوده و فایل‌های `ReportWork.csv` (رکورد کارکرد پرسنل با شناسهٔ کاربر و ساعت ورود و خروج)، `Entryandexitlist.csv` و `aria-dump.json` (dump کامل پایگاه دادهٔ قدیم) بدون احراز هویت قابل دانلود بوده‌اند. برای دادهٔ نمونه از دادهٔ ساختگی استفاده کنید.

---

## قانون ۶ — مالک ماژول

`/.github/CODEOWNERS` تعیین می‌کند هر ماژول چه کسی مالکش است. گیت‌هاب خودش او را به‌عنوان بازبین به PR اضافه می‌کند و بدون تأیید او merge ممکن نیست.

منبع حقیقت: `tools/ownership-map.csv` → بعد از پر کردن ستون `owner_github`، اجرا کنید:
```bash
./tools/gen-codeowners.sh
```

---

## جریان کار روزانه

```bash
# ۱) Issue مربوط به کار را پیدا یا بسازید (بورد همان‌جا است)
gh issue list --label "module:Hr"

# ۲) برنچ بسازید
git switch main && git pull
git switch -c hr/HR-42-payroll-overtime

# ۳) کار کنید و commit بزنید (قلاب‌ها خودکار هویت و نام برنچ را چک می‌کنند)

# ۴) PR بزنید
git push -u origin hr/HR-42-payroll-overtime
gh pr create --label "module:Hr" --fill

# ۵) بعد از merge، برنچ را حذف کنید
git branch -d hr/HR-42-payroll-overtime
git push origin --delete hr/HR-42-payroll-overtime
```

---

## پرسش‌های سریع

**روی یک برنچ شخصی قدیمی کار نیمه‌تمام دارم، چه کنم؟**  
به برنچ کاری جدید منتقلش کنید: `git switch -c hr/HR-42-payroll-overtime` سپس برنچ قدیمی را حذف کنید. اگر کار دیگری هم روی آن برنچ مانده، برای هرکدام یک برنچ جدا بسازید.

**قلاب‌ها اذیتم می‌کنند و باید سریع چیزی را رد کنم.**  
`SKIP_BRANCH_CHECK=1` یا `SKIP_ATTRIBUTION=1` یا `ALLOW_DIRECT_MAIN=1` — اما هر استفاده از اینها یک بدهی است که بعداً باید صاف شود.

**ایجنت AI را روی یک کار بزرگ گذاشته‌ام، چطور PR بزنم؟**  
یک PR به‌ازای هر واحد قابل بازبینی. برچسب `review:agent-generated` را اضافه کنید تا بازبین بداند باید دقیق‌تر بخواند.
