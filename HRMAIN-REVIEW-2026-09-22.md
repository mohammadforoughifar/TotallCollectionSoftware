# بررسی مستقل ماژول «منابع انسانی اصلی» (HrMain) — ایرادها و ضعف‌ها

تاریخ: ۳۱ شهریور ۱۴۰۵ (2026-09-22)
دامنه‌ی بررسی: کد فعلیِ روی شاخه‌ی main (بعد از کامیت‌های ۱۹ و ۲۰ شهریور) — Entities، Schema، Service، Controller، DTO، صفحات Blazor و نقاط اتصال به ماژول‌های دیگر (FaAtt / FaPay / Hr قدیمی).
روش: مرور خط‌به‌خط کد + ردیابی مصرف واقعی هر فیلد/قانون در کل سورس. این گزارش **مستقل** از گزارش‌های `HRMAIN-DEEP-ANALYSIS-2026-09-19.md` و `HR-GAP-ANALYSIS-2026-09-20.md` تهیه شده و موارد تازه‌ای که در آن‌ها نبوده علامت 🆕 دارد.

---

## خلاصه‌ی مدیریتی (۳۰ ثانیه)

ماژول HrMain از نظر **معماری و تمیزی کد** در سطح خوبی است (لایه‌بندی، RBAC روی همه‌ی اندپوینت‌ها، اعتبارسنجی حذف، جلوگیری از حلقه در درخت، تاریخچه فیلد‌به‌فیلد). مشکل اصلی **عمقی‌تر از کد است: این ماژول «ستون فقرات» اعلام می‌شود ولی ستون فقرات واقعی نیست** —

1. از ۱۶ قانونِ تنظیم‌شده در صفحه‌ی «قوانین پیش‌فرض»، **فقط ۱ مورد** جای دیگری از سیستم خوانده می‌شود؛ بقیه تنظیمات مرده‌اند.
2. هم‌زمان **یک سیستم قوانین موازی قدیمی** (HrTimeRules) با مقادیر متناقض هنوز زنده است.
3. اسناد و عکس پرسنل **بدون احراز هویت** از مسیر استاتیک سرو می‌شوند.
4. مجوزهای تفکیکیِ خوبی که در API طراحی شده، **در رابط کاربری اصلاً پشتیبانی نمی‌شود**.
5. هیچ تست خودکاری وجود ندارد.

---

## ۱) یافته‌های با شدت بالا

### 1.1 🆕 قوانین HrMain تقریباً هیچ‌جا مصرف نمی‌شوند (تنظیمات مرده)
`HrMainRules` در `HrMainEntities.cs` به‌عنوان «مبنای محاسبات ماژول‌های مرخصی، تأخیر/غیبت و اضافه‌کاری» معرفی می‌شود و صفحه‌ی `DefaultRules.razor` با تب‌های کامل مرخصی/تأخیر/غیبت/اضافه‌کاری به کاربر ارائه می‌شود. ردیابی مصرف در کل سورس:

| فیلد | مصرف واقعی خارج از خود ماژول |
|---|---|
| `AnnualLeaveDays` | ✅ فقط یک جا: `FaAttService.ResolveEntitledAsync` (خط ۱۳۲۷) |
| `OvertimeFactor` | ❌ — FaPay ضریب **جدای خودش** را دارد (`FaPaySettings.OvertimeFactor`) |
| `MaxConsecutiveLeaveDays`، `MaxCarryOverDays`، `UnpaidLeaveAllowed` | ❌ هیچ‌جا |
| `LateGraceMinutes`، `MonthlyAllowedLateMinutes`، `MaxLateWithoutLeaveMinutes`، `LateDeductionFactor` | ❌ هیچ‌جا |
| `AbsenceDailyDeductionFactor`، `UnexcusedAbsenceWarningAfter` | ❌ هیچ‌جا |
| `MaxMonthlyOvertimeHours`، `OvertimeNeedsApproval` | ❌ هیچ‌جا |
| `DefaultWorkStartTime/EndTime`، `DefaultWeeklyOffDays` | ❌ هیچ‌جا (کامنتِ کد می‌گوید «پیش‌فرضِ شیفت‌های بدون FaAtt»، ولی FaAtt هیچ‌گاه این‌ها را نمی‌خواند) |

**ریسک:** مدیر HR سقف اضافه‌کاری ۶۰ ساعت یا ضریب ۱٫۴ را تنظیم می‌کند و تصور می‌کند اعمال می‌شود؛ در عمل هیچ ماژولی (نه FaAtt، نه FaPay، نه HR قدیمی) این مقادیر را نمی‌خواند. این «تنظیمات بی‌اثر» از نبودِ تنظیمات خطرناک‌تر است چون حس کنترل کاذب می‌دهد.

### 1.2 🆕 دو سیستم قوانین موازی با مقادیر متناقض
- سیستم قدیمی: `HrTimeService.DefaultRules` → `leave.annual.grant = "24"` روز (سهمیه استحقاقی)
- سیستم جدید: `HrMainRules.AnnualLeaveDays = 26` روز (طبق قانون کار)

ماژول HR قدیمی (`HrTimeService.GetBalancesAsync`) سهمیه را از **۲۴** می‌خواند و ماژول جدید FaAtt برای مرخصی استحقاقی از **۲۶**. یعنی بسته به اینکه درخواست از کدام ماژول ثبت شود، پرسنل سهمیه‌ی متفاوتی می‌گیرد. تا زمانی که هر دو ماژول زنده‌اند (و طبق نقشه‌ی ماژول‌ها هر دو در منو هستند)، این تناقض عملیاتی است.

### 1.3 🆕 اسناد و عکس پرسنل بدون احراز هویت قابل دریافت است
- عکس پرسنل: `wwwroot/uploads/hr/employee/photos/{id}/...` (`HrCoreController.cs:643`)
- **مدارک پرسنل (قرارداد، اسکن مدارک هویتی و…):** `wwwroot/uploads/hr/employee/documents/{docId}/...` (`HrCoreController.cs:601`)
- لوگو: `wwwroot/uploads/hr/logo/...` (`HrMainService.cs:101`)

در `Program.cs` مسیرهای `/SecureFiles`، `/uploads/projects` و `/uploads/office/outgoingletter` صریحاً از سرو استاتیک حذف شده‌اند، ولی **`/uploads/hr/**` در فهرست بلاک نیست** و `UseStaticFiles` بدون احراز هویت سروش می‌کند. اگر سرور از بیرون شبکه قابل دسترسی باشد، اسناد هویتی پرسنل با یک URL ساده (بدون لاگین) قابل دانلود است — فقط GUID در نام فایل مانع حدس است، و GUID جزو رازِ امنیتی محسوب نمی‌شود.

### 1.4 🆕 آپلود SVG در لوگو = XSS ذخیره‌شده
`HrMainService.SaveLogoAsync` (خط ۹۴) فرمت `.svg` را مجاز می‌کند و `FileStore` بدون هیچ سانیتی‌سازی‌ای فایل را در wwwroot می‌گذارد. SVG می‌تواند حاوی `<script>` باشد و با بازکردن مستقیم URL لوگو در مرورگر، جاوااسکریپت در مبدأی همان سایت اجرا می‌شود (stored XSS). یا SVG را از فهرست حذف کنید یا هنگام سرو، `Content-Disposition: attachment` + `Content-Security-Policy` بگذارید.

---

## ۲) یافته‌های با شدت متوسط

### 2.1 🆕 مجوزهای تفکیکی API در رابط کاربری مرده‌اند
کنترلر برای هر بخش مجوز اختصاصی می‌پذیرد (`ForbiddenUnlessAnyAsync(Mod, "Read", "Company")` و مشابه برای `Branches/Org/Positions/Calendar/Rules`) و `RbacSeeder.cs:125` هم هر ۱۲ اکشن را تعریف کرده — کامنت کنترلر صراحتاً می‌گوید «می‌توان یک نفر فقط مسئول شعب باشد». اما:
- همه‌ی صفحات کلاینت فقط مجوز درشت‌دانه را چک می‌کنند: `canRead => Auth.Has("HrMain.Read") || Auth.IsAdmin` (در Branches، Positions، OrgStructure، CalendarSettings، DefaultRules، CompanyProfile)
- منوی `HrNavigation.cs` (خط ۳۶–۴۲) همه‌ی آیتم‌ها را با `"HrMain.Read"` می‌بندد

**نتیجه:** کاربری که فقط `HrMain.Branches` دارد (سناریویی که کل طراحی API برای آن ساخته شده) هیچ منو و صفحه‌ای نمی‌بیند؛ قابلیت عملاً غیرقابل استفاده است. یا درویِ UI باید همان OR منطق API را داشته باشد، یا مجوزهای تفکیکی از Seeder حذف شود.

### 2.2 🆕 باگ در تاریخچه: مقدار قبلیِ «شعبه» هرگز ثبت نمی‌شود
`HrMainService.cs:323`:
```csharp
await LogFieldChangeAsync(..., "شعبه", before.BranchId is > 0 ? null : "—", branchName ?? "—", ...);
```
نام شعبه‌ی **قبلی** هیچ‌وقت واکشی نمی‌شود؛ اگر گره قبلاً شعبه‌ی «تهران» داشته و به «اصفهان» منتقل شود، تاریخچه به‌جای «تهران → اصففان» مقدار «(خالی) → اصفهان» نشان می‌دهد. در گزارش «مقایسه دو تاریخ» همین رکوردها مبناست، پس داده‌ی تاریخچه آلوده می‌شود.

### 2.3 🆕 پوشش ناقص تاریخچه‌ی فیلد‌به‌فیلد
- گره سازمانی: تغییرات «تلفن»، «توضیحات» و «ترتیب نمایش» لاگ نمی‌شوند (حتی در snapshot قبل از تغییر هم کپی نمی‌شوند — `HrMainService.cs:289-297`)
- پست سازمانی: «کد»، «شرح وظایف» و «شرایط احراز» لاگ نمی‌شوند
- **قوانین پیش‌فرض (حساس‌ترین داده‌ی ماژول): هیچ تاریخچه‌ای ندارند.** تغییر `OvertimeFactor` یا `AbsenceDailyDeductionFactor` که مستقیماً بر فیش حقوقی اثر دارد، فقط در AuditLog عمومی (payload جدید، بدون مقدار قبلی) ثبت می‌شود؛ نه «چه کسی، چه پارامتری را از چند به چند» قابل استخراج است.

### 2.4 🆕 عدم قطعیت تراکنشی و کاراییِ لاگ‌نویسی
در `SaveNodeAsync`/`SavePositionAsync`:
1. `SaveChangesAsync` اصلی
2. سپس برای **هر فیلدِ تغییرکرده یک `SaveChangesAsync` جدا** (`LogFieldChangeAsync` → `LogChangeAsync`)

یک ویرایش گره = تا ۸ رفت‌وبرگشت جداگانه به دیتابیس، بدون هیچ تراکنشی. اگر بعد از ذخیره‌ی اصلی خطا رخ دهد: تغییر اعمال شده ولی تاریخچه ناقص/فاقد است و به کاربر پیام خطا می‌دهد (فکر می‌کند ذخیره نشده). راه‌حل: افزودن همه‌ی رکوردهای لاگ به همان ChangeSet و یک SaveChanges.

### 2.5 🆕 دیتابیس بدون قید یکتایی و FK
در `HrMainSchemaV1.cs` (هر دو گویش SQLite و SQL Server):
- `HrMainBranches.Code`، `HrMainOrgNodes.Code`، `CompanyHoliday.HolidayDate` — **UNIQUE ندارند**؛ جلوگیری از تکرار فقط چک اپ‌لیکیشنی است (دو درخواست هم‌زمان = دو رکورد تکراری؛ race طبیعی TOCTOU)
- جدول‌های singleton (Company/Locale/Rules) ایندکس یکتا ندارند → دو درخواست هم‌زمانِ اول (`GetOrCreateCompanyAsync` و مشابه در خطوط ۵۶۰ و ۶۱۰) = **دو ردیف**؛ بعد از آن «نخستین ردیف» مبنا می‌شود و ویرایشِ ردیف دوم گم می‌شود
- هیچ FK واقعی‌ای روی `ParentId`، `BranchId`، `OrgNodeId`، `HrEmployees.HrMainNodeId/HrMainPositionId` نیست — تمام aسلامت ارجاعی فقط در کد C# است؛ هر تغییر مستقیم SQL یا از مسیر ماژول دیگر، یتیم‌سازی بی‌صدا تولید می‌کند

### 2.6 🆕 سازگاری گره/پست و سقف پست در انتساب پرسنل اعمال نمی‌شود
`HrCoreService.ValidateRefsAsync` فقط **وجود** گره و پست را چک می‌کند:
- می‌توان پرسنل را به پستی متعلق به **گره‌ی دیگر** منصوب کرد (ناسازگاری ساختاری، بدون هیچ هشداری)
- `HeadCount` (تعداد پست مصوب) هرزمان اعمال نمی‌شود — فقط آمار «ظرفیت خالی» را می‌سازد؛ منصوب‌کردن نفر دهم روی پستِ تک‌نفره نیز بی‌صدا انجام می‌شود (حداقل باید هشدار over-establishment دهد)

### 2.7 🆕 وابستگی به ساعت محلی سرور
تمام timestamp ها `DateTime.Now` (نه UTC و نه `Iran Standard Time` مشخص) و محاسبه‌ی سال شمسی با `PersianDate.FromGregorian(DateTime.Today)` است. اگر سرور روی هاست خارج از ایران باشد، «امروز»، مرز سال شمری (نوروز) و فیلترهای تاریخ تاریخچه/مقایسه یک روز جابه‌جا می‌شوند. برای نرم‌افزاری که تقویم شمسی جزو هویتش است، این ریسک استقراری واقعی است.

---

## ۳) یافته‌های با شدت جزئی

| # | مورد | جزئیات |
|---|---|---|
| 3.1 | خطای اسکیمای خودتعمیر بلعیده می‌شود | `HrMainSchemaV1.EnsureAsync` هر خطا را فقط `Console.WriteLine` می‌کند؛ برنامه بالا می‌آید و بعداً اندپوینت‌ها 500 می‌دهند بدون ردیابی راحت |
| 3.2 | سقف ۲۰۰ مدیر در ویزارد | `EmployeeWizard.InitWizard` فهرست مدیران را با take=۲۰۰ می‌گیرد؛ سازمان‌های بزرگ‌تر، نصف مدیران را در انتخابگر نمی‌بینند |
| 3.3 | شمارش‌های ناهمگون | `ListBranchesAsync` تعداد گره‌ها را با احتساب غیرفعال‌ها می‌شمارد و `ListNodesAsync` پست‌های غیرفعال را هم می‌شمارد، اما `EmployeeCount` فقط فعال‌ها را — آمارهای صفحه با هم جمع نمی‌شوند |
| 3.4 | امکان «جهش سطح» در درخت | پیام خطا زنجیره‌ی سخت‌گیرانه وعده می‌دهد ولی شرط واقعی فقط «عمیق‌تر از والد» است (`dto.Level <= parent.Level`)؛ «تیم» مستقیماً زیر «شرکت» هم مجاز است و معنای سطوح را آبکی می‌کند |
| 3.5 | گارد پیمایش درخت بی‌صدا | `IsDescendantAsync` با سقف ۱۰۰ مرحله، در صورت عبور بی‌هشدار false برمی‌گرداند؛ در داده‌ی خراب می‌تواند حلقه را بپذیرد |
| 3.6 | compare بدون سقف بازه | `CompareStructureAsync` بدون محدودیت بازه همه‌ی لاگ‌ها را بار می‌کند؛ پارامتر from/to هم مقدار پیش‌فرض `default(DateTime)` را می‌پذیرد |
| 3.7 | بدون هشدار تغییرات ذخیره‌نشده | ویزارد ۵ مرحله‌ای هیچ محافظتی برای خروج تصادفی (بستن تب/ناوبری) ندارد؛ داده‌ی فرم گم می‌شود |
| 3.8 | checksum کد ملی اعمال نمی‌شود | اعتبارسنجی فقط «۱۰ رقم» است (کلاینت و سرور)؛ الگوریتم رسمی کنترل رقم آخر پیاده نشده |
| 3.9 | حلقه‌ی مدیر ممکن است | فقط self-manager چک می‌شود؛ زنجیره A←B←A قابل ساخت است و هر گزارشی که زنجیره مدیر را پیمایش کند گرفتار می‌شود |
| 3.10 | صفر تست خودکار | سولوشن ۴ پروژه دارد و هیچ پروژه‌ی تستی وجود ندارد؛ برای ماژولی که «ستون فقرات» است و حذف‌های شرطی/درخت/تاریخچه دارد، ریسسک رگرسیون بالاست |
| 3.11 | عملکرد ذخیره/برگشت | هر Save بعد از ثبت، کل `ListNodesAsync`/`ListPositionsAsync` (۴-۵ کوئری روی همه‌ی رکوردها) را صرفاً برای مپ‌کردن یک آیتم اجرا می‌کند |

---

## ۴) نقاط قوت (برای انصافِ گزارش)

- لایه‌بندی تمیز Controller → Service → Entity → DTO و جدول‌ها با پیشوند یکتا بدون تداخل با سامانه‌های قدیمی
- RBAC روی **تک‌تک** اندپوینت‌ها + فیلتر سراسری خطا (تبدیل Exception به ۴۰۰ با پیام فارسی) + AuditLog خودکار همه‌ی نوشتن‌ها
- اعتبارسنجی حذفِ سه‌لایه (زیرمجموعه/پست/پرسنل) و جلوگیری از حلقه در انتقال گره
- تاریخچه‌ی فیلد‌به‌فیلد با گزارش «مقایسه‌ی دو تاریخ» — طراحی درست (لاگ خوانا به‌جای snapshot سنگین)
- UX خوب: ویزارد پرسنل با اعتبارسنجی مرحله‌ای، Setup Wizard در هاب، حالت‌های خطا/تلاش مجدد در همه‌ی صفحات، RTL و اعداد فارسی
- جلوگیری از Path Traversal در FileStore

---

## ۵) اولویت‌بندی پیشنهادی رفع

| اولویت | اقدام | جا به جا کردن |
|---|---|---|
| 🔴 فوری | بستن دسترسی عمومی `/uploads/hr/**` (مانند projects) یا انتقال اسناد پرسنل به سرو از مسیر API با RBAC | `Program.cs` |
| 🔴 فوری | حذف svg از فرمت‌های مجاز لوگو (یا سانیتی‌سازی) | `HrMainService.cs:94` |
| 🟠 این هفته | تصمیم معماری درباره‌ی قوانین: یا FaAtt/FaPay/Hr قدیمی موظف به خواندن HrMainRules شوند، یا صفحات قوانین به «به‌زودی/غیرفعال» تبدیل شود؛ و حذف تداخل ۲۴/۲۶ روز | `HrMainRules` + مصرف‌کنندگان |
| 🟠 این هفته | یکسان‌سازی مجوزهای UI با مجوزهای تفکیکی API | `HrNavigation.cs` + ۶ صفحه |
| 🟠 این هفته | اصلاح باگ مقدار قبلی شعبه در لاگ | `HrMainService.cs:323` |
| 🟡 بعدی | تراکنشی‌کردن ذخیره+لاگ در یک SaveChanges؛ افزودن فیلدهای جاافتاده به تاریخچه + تاریخچه برای قوانین | `HrMainService.cs` |
| 🟡 بعدی | UNIQUE روی کدهای شعبه/گره/تاریخ تعطیلی + ایندکس یکتا برای singleton ها | `HrMainSchemaV1.cs` |
| 🟡 بعدی | چک سازگاری پست↔گره + هشدار عبور از HeadCount | `HrCoreService.ValidateRefsAsync` |
| 🟢 برنامه‌ریزی | پروژه‌ی تست (xUnit) حداقل برای SaveNode/حذف‌ها/تاریخچه/قوانین؛ اعمال checksum کد ملی؛ نگه‌داشتن «امروز» روی TimeZone ایران |

---

## ۶) مقایسه با گزارش‌های قبلی

- `HRMAIN-DEEP-ANALYSIS-2026-09-19.md` بیشتر **توصیف قابلیت‌ها** بود و جمع‌بندی‌اش «✅ تمیز و کامل»؛ موارد ۱.۱ تا ۱.۴ و ۲.۱ تا ۲.۷ این گزارش در آن **نبود**.
- `HR-GAP-ANALYSIS-2026-09-20.md` روی ماژول‌های عملیاتی (HrCore/FaPay/FaAtt) و RBAC عمومی متمرکز بود؛ یافته‌های اختصاصیِ HrMain این گزارش (قوانین مرده، تعارض ۲۴/۲۶، باگ لاگ شعبه، مجوزهای تفکیکی مرده، عدم تراکنش، نبود UNIQUE) در آن **پوشش داده نشده بود**.
