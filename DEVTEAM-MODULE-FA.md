# ماژول «مدیریت برنامه‌نویسان» (DevTeam)

> ماژول مستقل در منوی اصلی برنامه — نسخهٔ درون‌برنامه‌ای از همان چیزی که بستهٔ
> گردش کار تیم (`CODEOWNERS`، برچسب‌ها، قالب PR) بیرون از برنامه ردیابی می‌کرد.

---

## ۱) چرا این ماژول

تا پیش از این، پاسخ سه پرسش سادهٔ تیم فقط با ممیزی دستی `git` ممکن بود:

| پرسش | پیش از این | حالا |
|---|---|---|
| چه کسی روی چه بخشی کار کرده؟ | نام برنچ‌های شخصی + `git log` | بورد + تاریخچهٔ هر آیتم |
| کدام آیتم در حال انجام است؟ | هیچ‌جا | ستون «در حال انجام» در بورد |
| مالک هر ماژول کیست؟ | فایل CSV بیرون از برنامه | صفحهٔ «ماژول‌ها و مالکیت» |

این ماژول آن سه پرسش را داخل خود برنامه پاسخ می‌دهد و منبع دادهٔ زندهٔ
`CODEOWNERS` می‌شود.

### تفاوت کلیدی با بستهٔ گردش کار

بستهٔ گردش کار (`tools/`، `.githooks/`، `.github/`) **قانون** را تعریف می‌کند:
چه کسی باید بازبینی کند، چه برچسب‌هایی وجود دارد، پیام کامیت چه شکلی است.
این ماژول **وضعیت جاری** را نگه می‌دارد: الان چه کاری دست کیست و کجای راه است.
دو مکمل هم‌اند و به یک داده وصل‌اند — ستون `owner_github` در
`tools/ownership-map.csv`.

---

## ۲) جای ماژول در منو

گروه مستقل در منوی کناری، با رنگ نیلی (`#818cf8`) که با هیچ گروه دیگری تداخل ندارد:

```
مدیریت برنامه‌نویسان        ←  گروه
├── بورد کار تیم            /dev-team
├── آیتم‌های کاری           /dev-team/tasks
├── ماژول‌ها و مالکیت       /dev-team/modules
└── اعضای تیم               /dev-team/members
```

گروه فقط وقتی رندر می‌شود که کاربر مجوز `DevTeam.Read` (یا `View`/`Access`)
داشته باشد — همان قاعدهٔ `Auth.CanSee` بقیهٔ ماژول‌ها. وقتی روی هر یک از
صفحه‌های `dev-team/*` باشید، گروه خودکار باز می‌شود.

---

## ۳) مدل داده

چهار جدول، همه با پیشوند `Dev`:

```
DevMembers ──┬──< DevModules      (هر ماژول یک مالک)
             ├──< DevTasks        (هر آیتم یک مسئول)
             └──< DevTaskLogs     (هر رویداد یک انجام‌دهنده)

DevModules ────< DevTasks ────< DevTaskLogs
```

### `DevMembers` — اعضای تیم
`FullName`، `GithubHandle`، `Email`، `Phone`، `Role`، `IsActive`، `ColorHex`، `Note`.

نقش‌ها: برنامه‌نویس، ارشد، سرپرست تیم، تست، طراح UI، عملیات، مسئول محصول،
**و ایجنت هوش مصنوعی**.

> **چرا «ایجنت AI» یک نقش است؟** تیم این مخزن ترکیبی است: حساب‌های شخصی، حساب
> اشتراکی، و ایجنت‌هایی که بخشی از کار را انجام می‌دهند. اگر ایجنت را عضو
> حساب نکنیم، سهم کارش در آمار گم می‌شود و — مهم‌تر — نمی‌فهمیم کدام تغییرات
> به بازبینی سخت‌گیرانه‌تر نیاز دارند. آیتم کاری هم تیک جداگانهٔ
> `AgentAssisted` دارد و در بورد با آیکن 🤖 علامت می‌خورد.

### `DevModules` — ماژول‌ها و مالکیت
`Key` (یکتا، هم‌نام با پوشهٔ کد مثل `HrCore`)، `Title` (فارسی)، `OwnerId`،
`RepoPaths`، `ServiceLines`، `PageCount`.

### `DevTasks` — آیتم‌های کاری
`Number` (خودکار: `HRCORE-1`)، `Title`، `ModuleId`، `AssigneeId`، `Status`،
`Priority`، `Size`، `BranchName`، `PullRequestUrl`، `AgentAssisted`،
`StartedAt`/`DueDate`/`CompletedAt`.

### `DevTaskLogs` — تاریخچه
هر تغییر وضعیت، واگذاری، کامیت، بازبینی یا یادداشت یک ردیف می‌گیرد:
`MemberId`، `Action`، `Note`، `FromStatus`/`ToStatus`، `CommitSha`، `At`.

### وضعیت‌ها

| وضعیت | معنی | رفت‌وآمد خودکار |
|---|---|---|
| در صف (Backlog) | ثبت شده، شروع نشده | — |
| در حال انجام | کار رویش شروع شده | `StartedAt` پر می‌شود |
| در بازبینی | تمام شده، منتظر تأیید | — |
| مسدود | منتظر رفع مانع | — |
| انجام شد | بسته شده | `CompletedAt` پر می‌شود |

ترتیب ستون‌های بورد عمداً «جاری» را اول می‌گذارد، نه «در صف» را: سؤال اول
مدیر این است که **الان** چه چیزی در جریان است.

---

## ۴) دسترسی‌ها (RBAC)

ماژول `DevTeam` در `RbacSeeder` با پنج کنش ثبت می‌شود:

| کنش | چه کاری می‌دهد |
|---|---|
| `Read` | دیدن بورد، اعضا، ماژول‌ها و تاریخچه — مبنای نمایش در منو |
| `Create` | ساخت عضو، ماژول و آیتم کاری |
| `Update` | ویرایش، تغییر وضعیت و واگذاری آیتم |
| `Delete` | حذف عضو/ماژول/آیتم (با گاردهای حفاظتی) |
| `Manage` | تعیین مالک ماژول و هم‌زمان‌سازی با مخزن |

در «تنظیمات ← نقش‌ها و دسترسی‌ها» زیر گروه **مدیریت برنامه‌نویسان** دیده
می‌شود. `Manage` برچسب ماژول‌خاص دارد، چون برچسب عمومی `Manage` در این برنامه
«مدیر IT (ارجاع و تایید)» است و اینجا معنای دیگری می‌دهد.

کنترلر از `IEffectivePermissions` استفاده می‌کند — همان منبع واحدی که منوی
کلاینت با آن آیتم‌ها را نشان/پنهان می‌کند. پس سرور و منو هیچ‌وقت واگرا نمی‌شوند.

---

## ۵) API

همه زیر `api/devteam` و با `[Authorize]`:

| متد | مسیر | کنش |
|---|---|---|
| GET | `/board?moduleId=&assigneeId=` | کل بورد در یک پاسخ |
| GET | `/activity?days=&take=` | رویدادهای اخیر تیم |
| GET/POST | `/members` | فهرست / ذخیرهٔ عضو |
| DELETE | `/members/{id}` | حذف عضو |
| GET/POST | `/modules` | فهرست / ذخیرهٔ ماژول |
| DELETE | `/modules/{id}` | حذف ماژول |
| POST | `/modules/{id}/owner` | تعیین مالک |
| POST | `/modules/sync-from-repo` | هم‌زمان‌سازی از `tools/ownership-map.csv` |
| GET/POST | `/tasks` | فهرست صفحه‌بندی‌شده / ذخیرهٔ آیتم |
| GET/DELETE | `/tasks/{id}` | جزئیات / حذف |
| POST | `/tasks/{id}/status` | تغییر وضعیت (ستون بورد) |
| POST | `/tasks/{id}/assign` | واگذاری به یک عضو |
| GET/POST | `/tasks/{id}/logs` | تاریخچه / ثبت رویداد دستی |

خطاها همان قرارداد سراسری برنامه‌اند: `ApiExceptionFilter` استثنا را به
`400` با `{ "message": "…" }` فارسی تبدیل می‌کند.

---

## ۶) استقرار

### جدول‌ها خودشان ساخته می‌شوند — مایگریشن لازم نیست

این ماژول از الگوی **خودتعمیری اسکیمای** موجود در همین کدبیس پیروی می‌کند
(`DevTeamSchemaV1`)، نه از مایگریشن EF. دلیلش همان است که برای داشبورد شخصی
و نامه وارده نوشته شده:

> `EnsureCreated` (SQLite) و `Migrate` (SQL Server) جدول‌های تازه را به
> دیتابیس‌های **قدیمی و در حال استفاده** اضافه نمی‌کنند.

پس اگر مایگریشن می‌ساختیم، روی دیتابیس فعلی تیم جدول‌ها ساخته نمی‌شدند و
ماژول با خطای «Invalid object name» بالا می‌آمد. `DevTeamSchemaV1` در
`DbInitializer` صدا زده می‌شود، هم برای SQL Server و هم SQLite، کاملاً
`idempotent` است، و در پایان راه‌اندازی وضعیت را چاپ می‌کند:

```
[DB] ✔ جدول‌های مدیریت برنامه‌نویسان آماده‌اند.
```

اگر حساب کاربری سرویس مجوز DDL نداشته باشد، پیام راهنما می‌دهد و اسکریپت
آماده در **`inventory/sql/DevTeam.sql`** را یک DBA یک‌بار اجرا می‌کند.

### فایل `ownership-map.csv`

دکمهٔ «هم‌زمان‌سازی ماژول‌ها با مخزن» این فایل را از ریشهٔ مخزن می‌خواند.
برای اینکه در استقرار واقعی هم پیدا شود، دو کار شده:

1. در `Inventory.Api.csproj` به‌عنوان `Content` کپی می‌شود → کنار exe در
   `tools/ownership-map.csv` قرار می‌گیرد.
2. کنترلر از پوشهٔ اجرا و پوشهٔ کاری تا ۸ سطح به بالا می‌رود و نخستین
   نمونهٔ موجود را برمی‌دارد؛ پس هم در `bin/Debug` و هم در اجرای از ریشهٔ
   مخزن جواب می‌دهد.

اگر فایل پیدا نشود، پاسخ `400` با پیام راهنماست — نه خطای خام.

### هم‌زمان‌سازی چه می‌کند

- ماژول‌های تازه را اضافه می‌کند (با عنوان فارسی از `DevTeamLabels.ModuleTitles`).
- ماژول‌های موجود را فقط از نظر حجم کد و مسیر به‌روز می‌کند.
- **مالک تعیین‌شدهٔ دستی را هرگز بازنویسی نمی‌کند.**
- اگر ستون `owner_github` پر شده باشد، مالک را خودکار از روی نام کاربری
  GitHub به عضو تیم وصل می‌کند.

یعنی همان فایلی که `CODEOWNERS` از آن ساخته می‌شود، بورد را هم پر می‌کند.

---

## ۷) گاردهای حفاظتی

چند قاعده که عمداً سفت گرفته شده‌اند تا دادهٔ تاریخی از بین نرود:

- **عضو ارجاع‌شده حذف نمی‌شود**؛ فقط غیرفعال می‌شود و پیام می‌دهد که ابتدا
  آیتم‌هایش را واگذار کنید. دلیل: تاریخچهٔ «کی روی چی کار کرد» ارزشمندترین
  دادهٔ این ماژول است.
- **ماژول دارای آیتم کاری حذف نمی‌شود**؛ باید آیتم‌ها منتقل یا ماژول غیرفعال شود.
- **کلید ماژول یکتاست** (`UX_DevModules_Key`)، چون شمارهٔ آیتم از آن ساخته
  می‌شود (`HRCORE-1`).
- **مالک غیرفعال پذیرفته نمی‌شود.**
- حذف `DevTasks` تاریخچه‌اش را آبشاری می‌برد، ولی حذف ماژول عمداً
  `NO ACTION` است تا اشتباهِ «حذف ماژول، پاک شدن کل کارها» رخ ندهد.

---

## ۸) فایل‌های ماژول

**`Inventory.Shared`** (مشترک بین API و کلاینت)
- `Dtos/DevTeamDtos.cs` — همهٔ DTOها
- `Enums.cs` — پنج enum: `DevRole`، `DevTaskStatus`، `DevPriority`، `DevTaskSize`، `DevLogAction`
- `DevTeamLabels.cs` — برچسب‌های فارسی، ترتیب ستون‌ها، وزن اندازه، و عنوان فارسی ۳۷ ماژول

**`Inventory.Api`**
- `Entities/DevTeam/{DevMember,DevModule,DevTask,DevTaskLog}.cs`
- `Services/DevTeam/{IDevTeamService,DevTeamService}.cs`
- `Controllers/DevTeam/DevTeamController.cs`
- `Data/DevTeamSchemaV1.cs` — خودتعمیری جدول‌ها
- `Data/AppDbContext.cs` — `DbSet`ها و پیکربندی ایندکس/رابطه
- `Data/RbacSeeder.cs` — ماژول و کنش‌ها
- `Data/DbInitializer.cs` — فراخوانی ساخت جدول در راه‌اندازی

**`Inventory.Client`**
- `Pages/DevTeam/{DevTeamBoard,DevTeamTasks,DevTeamModules,DevTeamMembers}.razor`
- `Services/IInventoryApi.cs` + `Services/InventoryApiServices.cs`
- `Layout/NavMenu.razor` — گروه منو
- `Pages/Settings/{Roles,AuditLog}.razor` — برچسب‌های فارسی
- `wwwroot/css/app.css` — رنگ گروه

**دیگر**
- `inventory/sql/DevTeam.sql` — اسکریپت آماده برای DBA
- `tools/ownership-map.csv` — ردیف `DevTeam` اضافه شد (۳۸ ماژول)

---

## ۹) راستی‌آزمایی انجام‌شده

ماژول روی SQLite واقعی اجرا و سرتاسر تست شد — نه فقط کامپایل:

- `dotnet build` روی `Inventory.Api` و `Inventory.Client`: **۰ خطا**
  (۱۱ هشدار موجود، همه از ماژول‌های قدیمی؛ صفر هشدار از کد DevTeam)
- `[DB] ✔ جدول‌های مدیریت برنامه‌نویسان آماده‌اند.` — و در اجرای دوم هم
  بدون خطا (idempotent)
- هم‌زمان‌سازی: **۳۷ ماژول** از CSV با عنوان فارسی و حجم کد واقعی وارد شد
- گردش کامل: ساخت عضو → تعیین مالک → ساخت آیتم (شمارهٔ خودکار `HRCORE-1`)
  → تغییر وضعیت (`StartedAt` خودکار) → ثبت کامیت با SHA → واگذاری به ایجنت
  → انتقال به بازبینی → انجام شد (`CompletedAt` خودکار)
- بورد: ستون‌ها، بار کاری اعضا، بار ماژول‌ها و شاخص‌ها درست
- تاریخچه: زنجیرهٔ کامل «کی، کی، چه کرد» با وضعیت از→به
- گاردها: حذف عضو ارجاع‌شده، حذف ماژول دارای آیتم، کلید تکراری، ماژول
  نامعتبر → همه `400` با پیام فارسی
- امنیت: بدون توکن → `401`

---

## ۱۰) گام‌های بعدی پیشنهادی

1. **پر کردن `owner_github`** در `tools/ownership-map.csv` برای ۳۸ ماژول،
   سپس اجرای `./tools/gen-codeowners.sh` تا `CODEOWNERS` فعال شود.
   حالا ۳۸ ماژول «در انتظار مالک» کامنت‌شده‌اند.
2. **گره زدن PR به آیتم**: وقتی `PullRequestUrl` پر می‌شود، وضعیت را خودکار
   به «در بازبینی» ببر.
3. **گزارش سهم ایجنت**: شاخص `AgentAssistedShare` الان محاسبه می‌شود؛ یک
   ویجت داشبورد برایش بسازید تا روندش دیده شود.
4. **یادآور سررسید**: آیتم‌های `DaysToDue < 0` در بورد قرمزند؛ اعلان
   پیامکی/ایمیلی برای مسئول و مالک ماژول اضافه شود.
