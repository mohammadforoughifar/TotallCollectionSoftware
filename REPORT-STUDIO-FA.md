# گزارش‌ساز حرفه‌ای (Report Studio)

جایگزین گزارش‌ساز قدیمی — با دو قابلیتی که درخواست شد:
**جوین چند جدول** و **اشتراک‌گذاری با کاربرانِ مشخص و نقش‌ها**.

---

## ۱. جوین چند جدول

برخلاف نسخهٔ قبلی که «دیتاست‌های از پیش join‌شده» داشت و کاربر فقط می‌توانست
یکی را انتخاب کند، حالا کاربر خودش جدول‌ها را می‌چیند:

```
جدول اصلی: نامه‌ها
   └── + ارجاع‌ها      (از مسیر «نامه ← ارجاع‌های آن»)

جدول اصلی: فاکتورها
   ├── + اقلام فاکتور
   ├── + طرف حساب
   └── + کالا          (از مسیر «قلم فاکتور ← کالا»)
```

- تا **۵ جدول** در یک گزارش
- هر جوین **Left** (همهٔ سطرهای جدول اصلی) یا **Inner** (فقط تطابق‌ها)
- جوین‌های یک‌به‌چند خودکار **هشدار تکرار سطر** می‌دهند و پیشنهاد «تعداد یکتا» می‌کنند

### جدول‌های موجود (۶) و مسیرهای جوین (۴)

| جدول | دسته | ماژول RBAC |
|---|---|---|
| نامه‌ها (هر سه نوع) | اتوماسیون اداری | `InnerLetters` |
| ارجاع‌ها | اتوماسیون اداری | `InnerLetters` |
| فاکتورها | فروش و خرید | `FacInvoices` |
| اقلام فاکتور | فروش و خرید | `FacInvoices` |
| طرف حساب‌ها | اطلاعات پایه | `Parties` |
| کالاها | اطلاعات پایه | `Products` |

مسیرها: `نامه←ارجاع` · `فاکتور←اقلام` · `فاکتور←طرف حساب` · `قلم←کالا`

> افزودن جدول جدید = یک ورودی در `RsSchemaCatalog.Tables` + یک `case` در `RsRowSource`.

---

## ۲. اشتراک‌گذاری (دقیقاً خواستهٔ شما)

سه حالت دید:

| حالت | یعنی |
|---|---|
| **فقط خودم** | خصوصی |
| **کاربران و نقش‌های انتخابی** | فهرست دلخواه از کاربران + نقش‌ها |
| **همه** | هر کسی که مجوز ماژول‌های به‌کاررفته را دارد |

برای هر کاربر یا نقش، **سطح دسترسی جداگانه**:

| سطح | اجازه |
|---|---|
| `View` | فقط اجرا و دیدن نتیجه |
| `Export` | اجرا + خروجی Excel/CSV |
| `Edit` | ویرایش تعریف گزارش |

**ترکیب سطوح:** اگر کاربری هم مستقیم و هم از طریق نقش دسترسی داشته باشد،
بالاترین سطح برنده می‌شود. حذف و تغییر اشتراک‌ها فقط برای **مالک و ادمین**.

### نکتهٔ امنیتی کلیدی — دو لایهٔ مستقل

```
لایهٔ ۱: دسترسی به گزارش  → مالک / اشتراک کاربری / اشتراک نقشی / عمومی
لایهٔ ۲: دسترسی به داده   → مجوز RBAC همهٔ ماژول‌های به‌کاررفته
```

هر دو باید برقرار باشد. اگر گزارشی از حقوق پرسنل را با کارمند انبار share کنید،
او چون مجوز ماژول HR را ندارد **داده را نمی‌بیند** — اشتراک‌گذاری هرگز RBAC را دور نمی‌زند.

---

## ۳. امکانات گزارش

- **تجمیع:** تعداد، تعداد یکتا، مجموع، میانگین، کمینه، بیشینه
- **گروه‌بندی زمانی شمسی:** روزانه / ماهانه / فصلی / سالانه
- **فیلتر:** ۱۴ عملگر با ترکیب AND/OR، و **پارامتر پرسشی** (کاربر هنگام اجرا مقدار می‌دهد)
- **بازهٔ نسبی:** امروز، دیروز، ۷/۳۰/۹۰ روز اخیر، ماه جاری، سال جاری
- **خروجی:** جدول، نمودار (میله‌ای/خطی/دایره‌ای/دونات)، KPI، **جدول محوری (Pivot)**
- **خروجی CSV** با BOM (اکسل فارسی را درست باز می‌کند)

---

## ۴. چرا «بدون ارور»

۱. **فهرست سفید مطلق** — کلاینت هیچ‌وقت SQL یا نام واقعی جدول نمی‌فرستد، فقط
   کلیدهای کاتالوگ. هر کلید ناشناخته رد می‌شود. تزریق SQL ممکن نیست.
۲. **اعتبارسنج اختصاصی** (`RsValidator`) — قبل از هر اجرا، کوئری را بررسی و
   خطاها را با **پیام فارسی قابل‌فهم** برمی‌گرداند، نه خطای ۵۰۰.
۳. **پروجکشن صریح و تایپ‌سیف** — به‌جای Expression-building شکننده، هر ترکیب
   جدول یک متد مشخص دارد که در زمان کامپایل بررسی می‌شود.
۴. **درس نسخهٔ قبل** — هرگز `Select().Concat()`؛ همیشه ریشه از جدول هاب.
۵. **پوشش خطا** — هر استثنا به پیام فارسی ترجمه می‌شود (`Invalid object name`،
   timeout و…). کاربر هیچ‌وقت stack trace نمی‌بیند.

### نمونهٔ پیام‌های اعتبارسنجی

```
جدول «hackers» ناشناخته است.
ستون «نام کالا» از جدول «کالاها» است که به گزارش اضافه نشده.
برای افزودن «کالاها» باید اول جدول «اقلام فاکتور» در گزارش باشد.
تجمیع «مجموع» روی ستون «موضوع» معنا ندارد.
نمای KPI دقیقاً به یک سنجه نیاز دارد.
```

---

## ۵. فایل‌ها

| فایل | نقش |
|---|---|
| `Inventory.Shared/Dtos/ReportStudioDtos.cs` | قراردادهای مشترک |
| `Inventory.Api/Services/ReportStudio/RsSchemaCatalog.cs` | فهرست سفید جدول/ستون/جوین |
| `…/RsValidator.cs` | اعتبارسنجی با پیام فارسی |
| `…/RsRowSource.cs` | ساخت IQueryable هر ترکیب جدول |
| `…/RsExecutor.cs` | فیلتر، تجمیع، مرتب‌سازی، قالب‌بندی |
| `…/RsAccessService.cs` | کنترل دسترسی دولایه |
| `Inventory.Api/Entities/ReportStudio/ReportStudioEntities.cs` | موجودیت‌ها |
| `Inventory.Api/Data/ReportStudioSchemaV1.cs` | ساخت خودکار جدول‌ها |
| `Inventory.Api/Controllers/ReportStudio/ReportStudioController.cs` | API |
| `Inventory.Client/Services/ReportStudioClient.cs` | کلاینت |
| `Inventory.Client/Pages/ReportStudio/ReportStudio.razor` | رابط کاربری |

### مجوزهای جدید
`ReportStudio.View` · `ReportStudio.Design` · `ReportStudio.Share`
(مجوز View به‌صورت پیش‌فرض به همهٔ نقش‌های فعال داده می‌شود)

### جدول‌های دیتابیس
`RsReports` · `RsReportUserShares` · `RsReportRoleShares`
هنگام بالا آمدن برنامه به‌صورت idempotent روی SQL Server و SQLite ساخته می‌شوند.

---

## ۶. نتایج تست زنده

**بیلد:** API ✅ `0 Error` — Client ✅ `0 Error`

### آزمون نهایی: ۱۰ از ۱۰ موفق
```
✅ کاتالوگ: ۶ جدول            ✅ ali گزارش اشتراکی می‌بیند
✅ کاتالوگ: ۴ جوین            ✅ ali گزارش خصوصی نمی‌بیند
✅ جوین ۲ جدول                ✅ ali حذف = ۴۰۳
✅ جوین ۴ جدول                ✅ admin خروجی CSV = ۲۰۰
✅ رد جوین نامعتبر            ✅ رد ستون ناشناخته
```

### جوین ۴ جدول با داده واقعی
```
نام            | دسته        | فروش          | تعداد فاکتور
شرکت آلفا      | الکترونیک   | ۱,۲۶۰,۰۰۰,۰۰۰ | ۱
فروشگاه گاما   | الکترونیک   | ۶۰۰,۰۰۰,۰۰۰   | ۱
بازرگانی بتا   | الکترونیک   | ۴۵۰,۰۰۰,۰۰۰   | ۱
بازرگانی بتا   | مبلمان      | ۳۵۰,۰۰۰,۰۰۰   | ۱
```

### جدول محوری: نوع نامه × فوریت
```
نوع نامه | آنی | خیلی فوری | عادی | فوری
داخلی    | —   | —         | ۲    | ۲
صادره    | —   | —         | ۲    | ۱
وارده    | ۱   | ۱         | ۲    | ۱
```

### کنترل دسترسی (کاربر `ali` با نقش Operator)
```
اشتراک نقشی View   → می‌بیند ✔ | اجرا ✔ | خروجی ✗ (۴۰۳) | حذف ✗ (۴۰۳)
ارتقا به Export    → خروجی CSV ✔
ارتقا به Edit      → ویرایش ✔
گزارش خصوصی مدیر   → اصلاً در فهرستش نیست ✔
```

### ۱۳ کوئری نامعتبر
همه با پیام فارسی دقیق رد شدند — **هیچ خطای ۵۰۰**.
