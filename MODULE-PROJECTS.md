# 📁 ماژول مدیریت پروژه‌ها — راهنمای نسخه جدید

این سند بخش جدیدی را که به برنامه اضافه شده شرح می‌دهد:
**ورود و خروج پروژه‌ها، گزارش‌های کار، کارفرماها، انواع فاکتور و پیوست‌های رمزنگاری‌شده پروژه.**

---

## ۱. بخش‌های ماژول و دسترسی‌ها (RBAC)

پنج ماژول دسترسی جدید به سیستم نقش‌ها اضافه شده است (بخش **تنظیمات → نقش‌ها و دسترسی‌ها**، دسته‌ی «مدیریت پروژه‌ها»):

| ماژول | نام فارسی | اکشن‌ها |
|---|---|---|
| `Projects` | ورود و خروج پروژه‌ها | ایجاد / مشاهده / ویرایش / حذف / خروجی |
| `ReportWorks` | گزارش‌های کار | ایجاد / مشاهده / ویرایش / حذف / خروجی |
| `Karfarmas` | کارفرماها | ایجاد / مشاهده / ویرایش / حذف / خروجی |
| `TypeFactors` | انواع فاکتور | ایجاد / مشاهده / ویرایش / حذف / خروجی |
| `ProjectAttach` | پیوست‌های پروژه | ایجاد (آپلود) / مشاهده (دانلود) / حذف |

- پرمیشن‌ها به‌صورت **خودکار** هنگام اجرای برنامه سید می‌شوند و به نقش **Admin** تخصیص می‌یابند.
- هر دکمه/عمل در رابط کاربری هم با همان مجوز کنترل می‌شود (مثلاً بدون `Projects.Delete` دکمه حذف دیده نمی‌شود).
- کاربران قدیمی با نقش Operator (بدون نقش RBAC): مشاهده + ایجاد را دارند؛ حذف ندارند.

## ۲. کمبوی «کاربر»

در فرم‌های ورود/خروج پروژه و گزارش کار، کمبوی **کاربر** از **کاربران لاگین برنامه** (بخش «کاربران») پر می‌شود —
نام و نام خانوادگی (اگر ثبت شده باشد) وگرنه نام کاربری.
سمت سرور هم صحت کاربر فعال چک می‌شود.

> توجه: فیلد «سمت» (Semat) طبق درخواست شما فعلاً پیاده نشده؛ بعد از پیاده‌سازی چارت سازمانی اضافه می‌شود.

## ۳. محاسبات خودکار گزارش کار

```
ساعت خالص = (ساعت پایان − ساعت شروع) − (مدت صبحانه + مدت ناهار)   [حداقل صفر]
```
- شیفت شبانه پشتیبانی می‌شود (مثلاً ۲۲ تا ۶ صبح = ۸ ساعت).
- **جمع ساعات هر پروژه** به‌صورت خودکار هنگام ثبت/ویرایش/حذف هر گزارش کار به‌روزرسانی و در لیست پروژه‌ها نمایش داده می‌شود.

## ۴. پیوست‌های رمزنگاری‌شده (Attach)

- فایل‌ها با **AES-256-GCM** روی دیسک سرور (پوشه `SecureFiles`) با نام تصادفی ذخیره می‌شوند.
- **نام اصلی فایل هم رمزنگاری** شده و در دیتابیس نگهداری می‌شود.
- دانلود فقط از مسیر API با مجوز `ProjectAttach.Read` + رمزگشایی انجام می‌شود.
- **کلید رمزنگاری:**
  - از تنظیم `Files:EncryptionKey` در `appsettings.json` خوانده می‌شود (توصیه می‌شود مقدار دلخواه قوی بگذارید).
  - اگر تنظیم نشده باشد، کلید تصادفی در فایل `SecureFiles/.key` ساخته می‌شود.
  - ⚠️ **از فایل `.key` بکاپ بگیرید** — بدون آن فایل‌های قدیمی قابل رمزگشایی نیستند. این پوشه در `.gitignore` قرار دارد.
  - مسیر ذخیره‌سازی با تنظیم `Files:SecureRoot` قابل تغییر است.
- حداکثر حجم هر درخواست آپلود: **۲۵۰ مگابایت** (چند فایل همزمان).
- انواع پیوست پیش‌فرض: ۱=قرارداد، ۲=مدارک فنی، ۳=تاییدیه تحویل، ۴=سایر
  (در صورت نیاز در فایل `ProjectAttachPanel.razor` قابل ویرایش‌اند.)

## ۵. منوی جدید

**به‌روزرسانی (نسخه ۳) — فرم بزرگ، شماره‌های جدا و کاربر خودکار:**

- **مودال بزرگ و طراحی‌شده (modal-xl):** فرم ثبت پروژه با کارت‌های انتخاب نوع ثبت (ورود جدید / پروژه برگشتی)، سکشن‌های رنگی با آیکون، فیلد قفل‌شده شماره پروژه و چیپ سبز ثبت‌کننده.
- **شماره پروژه و شماره سریال دو فیلد جدا:**
  - **شماره پروژه** → کاملاً خودکار (آخرین شماره + ۱) و **قفل/غیرقابل تغییر** (همان Id رکورد)
  - **شماره سریال** → **دستی** توسط کاربر وارد می‌شود (مثل SA-1001) — تکراری قابل ثبت نیست
- **کاربر ثبت‌کننده خودکار:** کمبوی کاربر از همه‌ی فرم‌ها حذف شد؛ **در بک‌اند** کاربر لاگین فعلی (از توکن JWT) ثبت می‌شود — حتی اگر کلاینت userId جعلی بفرستد نادیده گرفته می‌شود (در UI چیپ «ثبت‌کننده: نام شما» نمایش داده می‌شود).
- **کمبوی سرچ‌دار:** کارفرما، نوع فاکتور، پروژه برگشتی و فیلترها — با جستجوی حرفه‌ای (نادیده‌گرفتن فاصله، انگلیسی هم‌ارز، ارقام فارسی).
- **پروژه برگشتی:** سریال = `re<n>-<شماره پروژه مبدأ>` (مثل re1-1) — «re» ثابت + عدد قابل تغییر.

**به‌روزرسانی (نسخه ۲) — فرم ثبت و گرید:**

- **سریال خودکار (ورود جدید):** هنگام ثبت پروژه‌ای که «برای اولین بار وارد مجموعه شده»، فیلد شماره سریال به‌صورت خودکار با **آخرین شماره پروژه + ۱** پیش‌پر می‌شود (قابل ویرایش). ثبت سریال تکراری با پیام فارسی جلوگیری می‌شود.
- **پروژه برگشتی:** با انتخاب حالت «پروژه برگشتی»، پروژه از **کمبوی سرچ‌دار** انتخاب می‌شود و سریال به‌صورت **`re<n>-<شماره پروژه>`** ساخته می‌شود — «re» ثابت است و عدد <n> قابل تغییر (پیش‌فرض: شماره برگشت بعدی همان پروژه). اطلاعات کارفرما/نوع فاکتور/کاربر از پروژه مبدأ پیش‌پر می‌شود و `ReturnProjectId` به پروژه مبدأ وصل می‌شود.
- **جستجوی حرفه‌ای کمبوها:** فاصله/نیم‌فاصله/خط‌تیره نادیده گرفته می‌شود، حروف عربی (ي/ك/ة) معادل فارسی و **نام انگلیسی** (case-insensitive) و ارقام فارسی/عربی همه جستجوپذیرند — برای کارفرما، نوع فاکتور، کاربر و پروژه برگشتی.
- **تاریخ ورود و تاریخ ثبت پروژه** فقط در فرم «ثبت» (ورود جدید) نمایش داده می‌شوند؛ در فرم ویرایش و حالت برگشتی دیده نمی‌شوند.
- **دیتاگرید** با ستون‌های: کد پروژه، نام پروژه، شماره سریال، کارفرما، تاریخ ورود، تاریخ خروج، شماره فاکتور، نوع فاکتور، کارشناسی اولیه، تاریخ ثبت پروژه، تاریخ نیاز مشتری، کل زمان مصرفی، پوشه دارد/ندارد.
- **دراپ‌داون عملیات روی هر سطر:**
  - ✏️ ویرایش اطلاعات
  - ⏱ افزودن گزارش کار (مودال سریع با پروژه انتخاب‌شده)
  - ℹ️ نمایش جزئیات پروژه (تاریخ نیاز، تحویل، تحویل پرونده، خروج، خروج موقت و…)
  - 📎 مستندات (پیوست‌های رمزنگاری‌شده)
  - 🖼 تصاویر پروژه (فقط فایل‌های تصویری — آپلود، نمایش در تب جدید، دانلود، حذف)
  - 📊 خروجی اکسل (فایل xlsx با دو شیت: «اطلاعات پروژه» و «گزارش‌های کار» — نیازمند مجوز `Projects.Export`)
  - 🗑 حذف اطلاعات



گروه **«مدیریت پروژه‌ها»** در منوی سمت راست:
- ورود و خروج پروژه‌ها (`/projects`) — با فیلتر کارفرما/نوع فاکتور/کاربر/برگشتی + پیوست‌ها
- گزارش‌های کار (`/report-works`) — با فیلتر پروژه/کاربر/بازه تاریخ + جمع ساعات
- کارفرماها (`/karfarmas`)
- انواع فاکتور (`/type-factors`)

تمام تاریخ‌ها شمسی (DatePicker موجود برنامه) و تمام خطاها با پیام فارسی برمی‌گردند.

## ۶. فایل‌های اضافه/تغییریافته

**API:**
- `Entities/Projects.cs` — موجودیت‌های KarFarma، TypeFactor، ProjectEntryExit، ReportWork، ProjectAttach
- `Controllers/RbacControllerBase.cs` — بررسی مجوز سرور
- `Controllers/ProjectsController.cs` (+endpoint کمبوها: `GET api/projects/lookups`)
- `Controllers/KarfarmasController.cs` ، `TypeFactorsController.cs` ، `ReportWorksController.cs` ، `ProjectAttachController.cs`
- `Services/ProjectFileProtection.cs` — رمزنگاری فایل
- `Data/AppDbContext.cs` ، `Data/RbacSeeder.cs` ، `Program.cs`
- `Migrations/20260823120204_AddProjectManagement.*` — مایگریشن خودکار (SQL Server)

**Shared:**
- `Dtos/ProjectDtos.cs`

**Client:**
- `Services/ProjectServices.cs` ، `Pages/Projects/*.razor` (۴ صفحه) ، `Shared/ProjectAttachPanel.razor`
- `Layout/NavMenu.razor` ، `Pages/Settings/Roles.razor` ، `Program.cs` ، `wwwroot/index.html` (helper دانلود)

## ۷. اجرا

1. پروژه را بیلد و اجرا کنید (`RUN.ps1`) — دیتابیس به‌صورت خودکار مایگریت می‌شود.
2. با کاربر Admin وارد شوید؛ دسترسی‌های ماژول پیش‌فرض داده شده‌اند.
3. برای سایر کاربران: تنظیمات → نقش‌ها و دسترسی‌ها → دسته «مدیریت پروژه‌ها».

---

# به‌روزرسانی: مدیریت نشست و خطای 401 (۲۰۲۶-۰۸-۲۴)

## ۱. مدت اعتبار توکن: ۲۴ ساعت + قابل تنظیم
- در `AuthService` انقضای توکن از ۱۲ ساعت به **۲۴ ساعت** تغییر کرد.
- از تنظیمات قابل تغییر است — در `src/Inventory.Api/appsettings.json`:
  ```json
  "Jwt": { "ExpireHours": 24 }
  ```
- تست شد: `exp` توکن دقیقاً ۲۴ ساعت بعد از صدور است ✔

## ۲. هدایت خودکار به صفحه ورود هنگام خطای 401
- در `ApiClient` (مسیر واحد همه‌ی درخواست‌های HTTP) هر پاسخ **401** — مثلاً توکن منقضی یا نامعتبر — نشست کاربر را با پیام «نشست شما منقضی شده است.» پایان می‌دهد و چون MainLayout بدون نشست همیشه «صفحه ورود» را نشان می‌دهد، کاربر **فوراً به لاگین هدایت می‌شود** (بنر هشدار زرد رنگ بالای فرم).
- درخواست ورود (`api/auth/login`) از این قاعده **مستثناست** تا خطای «نام کاربر یا رمز اشتباه» همان‌جا نمایش داده شود.
- خروج دستی (دکمه خروج) بدون پیام انقضا انجام می‌شود و پیام پس از ورود موفق بعدی پاک می‌شود.

**فایل‌های تغییریافته:**
- API: `Services/AuthService.cs` ، `appsettings.json`
- Client: `Services/AuthState.cs` (+`SessionNotice`،`ExpireSessionAsync`) ، `Services/ApiClient.cs` (هندل 401 در هر دو متد ارسال/آپلود) ، `Shared/LoginView.razor` (بنر انقضای نشست)

---

# به‌روزرسانی ۴ — میانبرها، فرم مجزای فاکتور، منطق RE و جستجوی ستونی (۲۰۲۶-۰۸-۲۴)

## ۱. مدیریت نشست (یادآوری مرحله قبل)
- خطای 401 ← هدایت خودکار به صفحه ورود + پیام انقضای نشست
- انقضای توکن ۲۴ ساعت (قابل تنظیم در `appsettings.json` ← `"Jwt": { "ExpireHours": 24 }`)

## ۲. کلیدهای میانبر در کل ماژول پروژه
| کلید | عمل |
|---|---|
| **F1** | ثبت/ذخیره در همه‌ی فرم‌ها (پروژه، فاکتور، گزارش کار سریع، گزارش کار، کارفرما، نوع فاکتور) |
| **F2** | رکورد جدید در هر ۴ صفحه لیست (وقتی مودال باز است غیرفعال است) |
| **Esc** | بستن مودال |
- روی خود دکمه‌ها هم برچسب میانبر نمایش داده می‌شود: «ذخیره (F1)»، «پروژه جدید (F2)»

## ۳. فیلدهای فاکتور → فرم مجزا + مجوز رویت ستون
- «شماره فاکتور» و «نوع فاکتور» از فرم ثبت/ویرایش پروژه **حذف شدند** و اختیاری شدند (`FactorTypeId` در دیتابیس nullable — مایگریشن `ProjectFactorOptional`).
- فرم مجزای فاکتور: از منوی سطر (⋮) ← «ثبت/ویرایش فاکتور» (نیازمند مجوز `Projects.Update`) — اندپوینت جدید `PUT api/projects/{id}/factor`.
- آپدیت اصلی پروژه دیگر فیلدهای فاکتور را **دست نمی‌زند** (مقاوم در برابر دورزدن از کلاینت — تست شد).
- **مجوز جدید `Projects.ViewFactor`** (رویت ستون‌های فاکتور): بدون آن، ستون‌های «شماره فاکتور/نوع فاکتور» در گرید، جزئیات و خروجی اکسل **و حتی جستجوی سروری** مخفی است — در سرور اعمال می‌شود نه فقط ظاهر. در صفحه نقش‌ها با عنوان «رویت ستون‌های فاکتور (شماره/نوع) 👁» قابل تخصیص است و به نقش Admin خودکار اضافه می‌شود.

## ۴. منطق پروژه برگشتی (RE)
- کمبوی برگشتی: «نام پروژه (کد X)» نمایش می‌دهد (جستجو بر اساس نام و کد).
- **شماره پروژه برگشتی = `REn-کد پروژه مبدأ`** (مثلا کد 2001 ← `RE1-2001`) — خودکار، غیرقابل ویرایش، محاسبه در سرور (n = تعداد برگشتی‌های قبلی همان مبدأ + ۱).
- **سریال کاملاً مستقل** و دستی است (مثلاً 234589) — دیگر به کد پروژه مرتبط نیست.
- در گرید، شماره پروژه برگشتی با بج طلایی `RE1-2001` نمایش داده می‌شود؛ در جزئیات و اکسل هم همان.
- اعتبارسنجی سرور: پروژه مبدأ باید موجود باشد و نمی‌تواند خودِ پروژه باشد.

## ۵. جستجوی سرستون‌ها (بخصوص تاریخ‌ها)
- ردیف جستجو زیر سربرگ گرید برای همه‌ی ستون‌ها: شماره پروژه، نام، سریال، کارفرما، **تاریخ ورود/خروج/ثبت/نیاز مشتری** (تایپ شمسی مثل `1405/06`)، شماره/نوع فاکتور (در صورت مجوز)، کارشناسی، زمان مصرفی و پوشه (کمبو همه/دارد/ندارد).
- نرمال‌سازی هوشمند: ارقام فارسی/عربی ↔ لاتین، حذف فاصله/نیم‌فاصله، تبدیل ي/ك عربی.
- دکمه «پاک کردن» و شمارنده «X از Y» در سربرگ صفحه.

## فایل‌های کلیدی این مرحله
- API: `Controllers/ProjectsController.cs` (UpdateFactor + ViewFactor + ProjectNumber)، `Entities/Projects.cs` (FactorTypeId nullable)، `Data/RbacSeeder.cs` (+ViewFactor)، `Migrations/20260824051902_ProjectFactorOptional.*`
- Shared: `Dtos/ProjectDtos.cs` (+ProjectNumber، +ProjectFactorDto)
- Client: `Pages/Projects/ProjectsList.razor` (بازنویسی: جستجوی ستونی، مودال فاکتور، RE، میانبرها)، سه صفحه دیگر ماژول (F2/برچسب‌ها)، `Services/ProjectServices.cs`، `Pages/Settings/Roles.razor`

---

# به‌روزرسانی ۵ — فیلد CodeProject و عدد برگشتی دستی (۲۰۲۶-۰۸-۲۴ — تأییدشده توسط کاربر)

## مدل داده
- `ProjectEntryExit.CodeProject` (nvarchar 60 + ایندکس): کد واقعی پروژه. «ورود جدید» ← خودکار = بزرگ‌ترین کد عددی + ۱ (حذف‌شده‌ها هم لحاظ می‌شوند تا کد تکراری صادر نشود). «برگشتی» ← خودکار = `REn-کد مبدأ`.
- `ProjectEntryExit.ReturnProjectId` (معنای جدید): **عدد برگشتی n** که کاربر **دستی** وارد می‌کند (۰ = برگشتی نیست). در DTO فیلد کمکی `ReturnOfProjectId` = شناسه پروژه مبدأ (فقط ورودی ایجاد).
- `ReportWork.CodeProject`: کد پروژه موقع ثبت گزارش روی خود گزارش کپی می‌شود — گزارشات روزانه بر اساس **کد پروژه** هستند نه Id جدول (FK داخلی ProjectId برای یکپارچگی و حذف آبشاری حفظ شده — به انتخاب کاربر).
- `Id` جدول دیگر نقش نمایشی ندارد.

## بک‌اند
- `ProjectsController.Create`: صدور خودکار کد؛ عدم تکرار کد RE (پیام فارسی)؛ آپدیت اصلی کد/عدد برگشتی را **تغییر نمی‌دهد** (مقاوم در برابر دورزدن — تست شد).
- `GET api/projects/next-serial` ← پیشنهاد کد بعدی (بزرگ‌ترین کد عددی + ۱).
- `ReportWorksController`: ذخیره کد پروژه موقع ثبت/انتقال گزارش + نمایش در DTO.
- اکسل: «کد پروژه» = CodeProject + ردیف «عدد برگشتی». کمبوی پروژه: نام + کد.
- **مایگریشن `AddCodeProject`** + **بک‌فیل خودکار** در `DbInitializer`: دیتابیس‌های قدیمی ← کد عادی = Id قبلی (شماره‌های موجود حفظ می‌شود)، برگشتی‌ها ← `REn-کد مبدأ` و معنای ReturnProjectId از Id مبدأ به n تبدیل، گزارش‌ها کد می‌گیرند — تست شد روی داده شبیه‌سازی‌شده‌ی قدیمی.

## کلاینت
- فرم برگشتی: کمبوی پروژه + ورودی دستی «عدد برگشتی» + پیش‌نمایش قفل‌شده‌ی کد (`REn-کد`) با پیشنهاد خودکار عدد بعدی (قابل تغییر).
- گرید/جزئیات/فاکتور: «کد پروژه» از فیلد جدید (بج طلایی برای RE). جستجوی ستونی هم روی کد کار می‌کند.
- صفحه گزارش‌های کار: ستون «کد پروژه» (بج طلایی RE). توست موفقیت ثبت، کد صادرشده را نشان می‌دهد.

---

# به‌روزرسانی ۶ — کمبوی برگشتی بدون RE، آپلود در فرم جدید، نمایش کد، اصلاح منوی سطر (۲۰۲۶-۰۸-۲۴)

## ۱. کمبوی «پروژه برگشتی» فقط پروژه‌های بدون RE
- `ProjectLookups.BaseProjects` (جدید در lookups): فقط پروژه‌هایی که `ReturnProjectId <= 0` — برگشتیِ برگشتی ساخته نمی‌شود.

## ۲. آپلود تصاویر و مستندات در خود فرم «ثبت پروژه»
- دو باکس جدید در فرم (فقط حالت ثبت، با مجوز `ProjectAttach.Create`): «افزودن تصویر» (فقط فرمت تصویری، حداکثر ۶) و «افزودن سند» (با انتخاب نوع سند: قرارداد/مدارک فنی/تاییدیه تحویل/سایر، حداکثر ۶) + لیست فایل‌های انتخابی با امکان حذف قبل از ثبت.
- جریان: پروژه ثبت می‌شود ← Id برمی‌گردد ← فایل‌ها **خودکار و رمزنگاری‌شده** بارگذاری می‌شوند ← توست نتیجه (با تعداد فایل‌های پیوست‌شده؛ در صورت خطای جزئی، هشدار با راهنمایی افزودن بعدی از منوی سطر).
- `IProjectService.CreateAsync` حالا `(Id, Code)` برمی‌گرداند.

## ۳. نمایش کد پروژه بدون جداکننده هزارگان
- گرید: کدهای عددی دیگر «1,003» نمایش داده نمی‌شوند — همان‌طور که ثبت شده‌اند ظاهر می‌شوند («1003»).

## ۴. رفع قطعی باز نشدن منوی سطر (⋮)
- علت: منوی داخل `<td>` توسط `.table-responsive` (overflow) بریده می‌شد و دیده نمی‌شد.
- راه‌حل: **منوی شناور سراسری** — یک نمونه واحد که با `position: fixed` دقیقاً در نقطه کلیک موس باز می‌شود (سرویس از مختصات رویداد، بدون نیاز به JS)؛ کلیک بیرون آن را می‌بندد. اقلام منو همان موارد با همان سطح دسترسی‌ها هستند.

## نکته نصب
- این مرحله مایگریشن جدیدی ندارد؛ فقط کد بیلد را جایگزین کنید.

---

# به‌روزرسانی ۷ — اکسل کل پروژه‌ها، رفع رفتن کمبو پشت گرید، ارقام فارسی، ورودی ساعت ۲۴ساعته، چاپ گزارش‌ها (۲۰۲۶-۰۸-۲۴)

## ۱. دکمه «خروجی اکسل کل پروژه‌ها»
- دکمه جدید در هدر صفحه پروژه‌ها (با مجوز `Projects.Export`) — مستقل از فیلترها، **همه پروژه‌ها** در یک فایل اکسل.
- اندپوینت جدید: `GET /api/projects/export` — شیت «کل پروژه‌ها» راست‌به‌چپ با سرستون ثابت (فریز)، یک سطر به‌ازای هر پروژه (کد، نام، سریال، کارفرما، تحویل‌گیرنده، تاریخ‌ها، برگشتی، تعداد گزارش کار، کل زمان مصرفی، پوشه، ثبت‌کننده، شرح) + **سطر جمع** (تعداد پروژه‌ها و جمع کل ساعات).
- ستون‌های «نوع فاکتور/شماره فاکتور» فقط برای دارندگان مجوز `Projects.ViewFactor` در فایل قرار می‌گیرند (تست‌شده با کاربر بدون مجوز).

## ۲. رفع رفتن منوی کمبو پشت گریدویو (گزارش‌های کار)
- **علت اصلی پیدا شد:** انیمیشن ورود صفحه (`fadeSlideIn`) با `fill-mode: both` و مقدار پایانی `translateY(0)` روی هر فرزند اصلی محتوا یک *stacking-context* دائمی می‌ساخت؛ در نتیجه کارت گرید (که بعداً در DOM است) روی منوی بازشوی کمبو (z-index:1080) قرار می‌گرفت.
- رفع: مقدار پایانی انیمیشن به `transform: none` تغییر کرد + z-index منوهای بازشو (کمبو و تقویم) به **۱۳۰۰** افزایش یافت. این رفع برای **همه صفحات** هم اعمال می‌شود، نه فقط ماژول پروژه.

## ۳. نمایش ارقام فارسی در همه اعداد و ساعت‌ها
- تاریخ‌ها (۱۴۰۵/۰۶/۰۱)، ساعت شروع/پایان (۰۸:۰۰)، ساعت خالص (۸:۳۰ ساعت)، کدهای عددی پروژه (۱۰۰۳)، سریال، شمارنده‌ها، حجم فایل‌ها — همه با ارقام فارسی نمایش داده می‌شوند.
- کدهای `RE` (مثل RE1-1003) به‌همان شکل لاتین با `dir=ltr` می‌مانند تا خوانایی حفظ شود.
- `PersianDate.TryParse` حالا ارقام فارسی/عربی را هم می‌پذیرد؛ پس تایپ تاریخ با هر دو نوع رقم کار می‌کند. جستجوی سرستون‌ها هم با هر دو نوع رقم تطابق پیدا می‌کند.

## ۴. ورودی ساعت ۲۴ساعته بدون AM/PM (کامپوننت جدید `TimeInput`)
- `input[type=time]` که بسته به مرورگر AM/PM نشان می‌داد (و 00:30 با 12:30 اشتباه گرفته می‌شد) با دو کمبوی مستقل **ساعت** (۰۰ تا ۲۳) و **دقیقه** (۰۰ تا ۵۹) جایگزین شد — ارقام فارسی، بدون ابهام.
- برای «مدت صبحانه/ناهار» سقف ساعت ۸ است و راهنمای «مدت استراحت — نه ساعت روز» کنار آن دیده می‌شود.
- در هر دو فرم (گزارش کار جدید در صفحه گزارش‌های کار + گزارش سریع از منوی سطر پروژه) اعمال شد.

## ۵. چاپ گزارش‌های کار
- **از منوی سطر هر پروژه (⋮ ← «چاپ گزارش‌های پروژه»):** سند A4 چاپی شامل مشخصات پروژه (نام، کد، سریال، کارفرما، تحویل‌گیرنده، تاریخ‌ها) + جدول گزارش‌ها (ردیف، تاریخ، کاربر، شروع، پایان، استراحت، خالص، شرح) + جمع ساعت خالص + خط امضا + تاریخ چاپ و نام چاپ‌کننده. با مجوز `ReportWorks.Read`.
- **از هدر صفحه گزارش‌های کار (دکمه «چاپ»):** چاپ همان لیست فیلترشده (پروژه/کاربر/بازه تاریخ) با جمع ساعات.
- پیاده‌سازی با تابع JS جدید `printTableReport` در `index.html` (پنجره چاپ مستقل، فونت Tahoma، RTL، قالب A4).

## فایل‌های کلیدی این مرحله
- سرور: `Controllers/ProjectsController.cs` (ExportAll)، `Shared/PersianDate.cs` (ToShortFa + پذیرش ارقام فارسی در TryParse)
- کلاینت: `Shared/TimeInput.razor` (جدید)، `Services/PrintModels.cs` (جدید)، `Extensions/PersianExtensions.cs` (ToFaDate/TimeFa/HoursFa/FaDigits)، `Shared/DatePicker.razor` (نمایش فارسی)، `Pages/Projects/ReportWorks.razor` و `ProjectsList.razor` (چاپ/اکسل/ارقام فارسی/TimeInput)، `wwwroot/index.html` (printTableReport)، `wwwroot/css/app.css` (رفع stacking-context)

## نکته نصب
- این مرحله **مایگریشن جدیدی ندارد**؛ فقط کد بیلد را جایگزین کنید.

---

# به‌روزرسانی ۷ (اصلاحیه) — رفع کرش DatePicker که صفحه گزارش‌های کار و چاپ را خراب می‌کرد (۲۰۲۶-۰۸-۲۴)

- **علت کرش:** صفحه گزارش‌های کار (و صفحه هزینه‌ها) به کامپوننت `DatePicker` ویژگی `Placeholder` می‌دادند، ولی این پارامتر روی کامپوننت تعریف نشده بود → خطای رندر و از کار افتادن کل صفحه (از جمله دکمه چاپ).
- **رفع:** پارامتر `Placeholder` به `Shared/DatePicker.razor` اضافه شد — پیش‌فرض `۱۴۰۳/۰۵/۱۲` و قابلیت متن دلخواه مثل «از تاریخ»/«تا تاریخ». صفحه گزارش‌های کار و چاپ گزارش‌ها حالا بدون خطا بارگذاری می‌شوند.
- نکته نصب: مایگریشن ندارد؛ پس از جایگزینی بیلد، یک بار Ctrl+F5.

---

# به‌روزرسانی ۷ (اصلاحیه ۲) — ریشه مشکل تکراری: کش PWA نسخه قدیمی را اجرا می‌کرد (۲۰۲۶-۰۸-۲۴)

- **تشخیص:** اصلاح `DatePicker.Placeholder` در سورس و خروجی publish موجود بود (در `Inventory.Client.wasm` تأیید شد) ولی مرورگر هنوز بیلد قدیمی را اجرا می‌کرد — سرویس‌ورکر PWA فایل‌های قدیمی را از کش سرو می‌کرد و Ctrl+F5 هم کش سرویس‌ورکر را دور نمی‌زند.
- **رفع دائمی (از این نسخه به بعد):**
  - `service-worker.published.js`: `self.skipWaiting()` در install + `clients.claim()` در activate → نسخه جدید بلافاصله فعال می‌شود.
  - `index.html`: ثبت سرویس‌ورکر با `updateViaCache:'none'` + رفرش خودکار صفحه هنگام تعویض نسخه (`controllerchange`) + چک به‌روزرسانی هر بار برگشت به تب.
  - پس از **یک بار** پاک‌سازی دستی کش (پایین همین بخش)، انتشارهای بعدی خودکار به مرورگر کاربر می‌رسند.
- **نشان نسخه روی صفحه ورود:** بج «نسخه ۷.۱ — ۱۴۰۵/۰۶/۰۲» (`AppInfo.cs`) تا همیشه مشخص باشد کدام بیلد در حال اجراست.

## پاک‌سازی یک‌باره کش (فقط همین بار لازم است)
1. سایت را باز کنید → F12 → تب **Application** → بخش **Service Workers** → **Unregister**.
2. همان تب، بخش **Storage** → دکمه **Clear site data**.
3. همه تب‌های برنامه را ببندید و دوباره باز کنید.
(راه سریع‌تر برای تست: باز کردن سایت در پنجره InPrivate/Incognito.)

---

# به‌روزرسانی ۷ (اصلاحیه ۳) — رفع دام دوفایلی کانکشن‌استرینگ (۲۰۲۶-۰۸-۲۴)

- **مشکل گزارش‌شده:** `Update-Database` با وجود تغییر کانکشن در `appsettings.json` همچنان به دیتابیس لوکال (`Server=.`) وصل می‌شد.
- **علت:** فایل `appsettings.Development.json` هم `ConnectionStrings:Default` داشت و در محیط Development روی فایل اصلی **غلبه** می‌کرد (لاگ PM دقیقاً همان رشته را چاپ می‌کرد).
- **رفع:** بلوک `ConnectionStrings` از `appsettings.Development.json` حذف شد — کانکشن فقط از `appsettings.json` خوانده می‌شود (تک‌منبع).
- راه اضطراری برای اجرای مایگریشن روی کانکشن خاص بدون تغییر فایل: `Update-Database -Args "--environment Production"`.

---

# به‌روزرسانی ۷ (اصلاحیه ۴) — آماده‌سازی استقرار روی سرور http://192.168.30.108:8091 (۲۰۲۶-۰۸-۲۴)

## تغییرات
- **`index.html` (کلاینت):** تابع `inventoryApiBase` قبلاً برای هر آدرس غیر از localhost به «http://localhost:5100» برمی‌گشت! حالا برای هر هاست غیرلوکال (مثل IP سرور شبکه) همان مبدا (`origin`) صفحه برگردانده می‌شود — بدون این اصلاح، کلاینت روی مرورگر کاربران به کامپیوتر خودشان وصل می‌شد نه سرور.
- **`Program.cs` (API):** پورت سرو حالا از کانفیگ خوانده می‌شود — اولویت: متغیر محیطی `PORT` ← `Server:Port` در appsettings ← پیش‌فرض ۵۱۰۰. (قبلاً فقط `PORT ?? 5100` بود و appsettings تأثیری نداشت.) اتصال روی `0.0.0.0` است پس با IP سرور هم پاسخ می‌دهد.
- **`appsettings.json` (API):** افزوده شد `Server:Port = 8091` و رشته اتصال به SQL سرور `192.168.30.104` تنظیم شد.
- **`appsettings.Development.json` (API):** `Server:Port = 5100` افزوده شد تا اجرای محلی روی سیستم توسعه همان پورت قدیمی بماند (روی سرور که Production است، ۸۰۹۱ اعمال می‌شود).
- **`deploy-single.ps1`:** حالا علاوه بر انتشار کلاینت در wwwroot، خروجی نهایی سرور را هم در `publish\server` می‌سازد + راهنمای کپی به سرور و دستور باز کردن پورت فایروال را چاپ می‌کند.

## تست‌شده
- Development → گوش روی ۵۱۰۰ ✅ | Production → گوش روی ۸۰۹۱ ✅ | ورود admin روی ۸۰۹۱ → ۲۰۰ ✅

## اجرا روی سرور
1. روی سیستم توسعه: `deploy-single.ps1` را اجرا کنید.
2. محتویات `publish\server` را به پوشه‌ای روی سرور کپی کنید و `Inventory.Api.exe` را اجرا کنید (نیاز به ASP.NET Core Runtime 8).
3. پورت را در فایروال سرور باز کنید: `New-NetFirewallRule -DisplayName "Inventory 8091" -Direction Inbound -Protocol TCP -LocalPort 8091 -Action Allow`
4. مرورگر: `http://192.168.30.108:8091` — در اولین اجرا مایگریشن دیتابیس خودکار اعمال می‌شود.

---

# به‌روزرسانی ۷ (اصلاحیه ۵) — دام apiBaseUrl که لاگین را به سرور اشتباه می‌فرستاد (۲۰۲۶-۰۸-۲۴)

- **مشکل گزارش‌شده:** بعد از پابلیش، هنگام لاگین خطای ۴۰۵ صفحه IIS از آدرس `http://192.168.30.104:8061/api/auth/login` می‌آمد (نه از سرور برنامه!).
- **علت:** صفحه عمومی «ثبت درخواست IT» (`/request`) یک کلید `apiBaseUrl` در localStorage می‌نویسد؛ تابع سراسری `inventoryApiBase` آن را برای **همه صفحات** اعمال می‌کرد — پس هر مرورگری که یک بار آن آدرس را وارد کرده بود، همه درخواست‌هایش (حتی لاگین) به آن سرور می‌رفت. آنجا IIS فایل استاتیک سرو می‌کند و POST به `/api/...` با ۴۰۵ رد می‌شود.
- **رفع:** در `index.html` اعمال `apiBaseUrl` فقط به مسیر `/request` محدود شد (خود آن صفحه کلید را جداگانه می‌خواند). برنامه اصلی همیشه همان مبدأ صفحه را استفاده می‌کند.
- **رفع فوری روی مرورگر آسیب‌دیده (بدون نیاز به دیپلوی):** در کنسول F12: `localStorage.removeItem('apiBaseUrl'); location.reload();`

---

# به‌روزرسانی ۷ (اصلاحیه ۶) — بایند به IP مشخص سرور (۲۰۲۶-۰۸-۲۵)

- درخواست کاربر: آدرس گوش‌دهنده برنامه همیشه `0.0.0.0` نباشد و **IP سرور** از کانفیگ خوانده شود.
- `Program.cs`: متغیر `host` افزوده شد — اولویت: محیطی `HOST` ← `Server:Host` در appsettings ← پیش‌فرض `0.0.0.0`.
- `appsettings.json`: `Server:Host = 192.168.30.108` تنظیم شد (روی سرور برنامه فقط روی همین IP پاسخ می‌دهد).
- `appsettings.Development.json`: `Server:Host = 0.0.0.0` افزوده شد — در غیر این صورت bind روی IP سرور روی سیستم توسعه خطا می‌داد.
- ⚠️ نکته: با بایند روی IP مشخص، برنامه روی سرور دیگر روی `localhost` پاسخ نمی‌دهد — حتی از خود سرور هم باید با `http://192.168.30.108:8091` وارد شد.
- تست‌شده: `HOST=127.0.0.1` ← گوش روی `127.0.0.1:8091` ✅ (پیکربندی Production خوانده می‌شود و env بر آن غلبه می‌کند)

---

# به‌روزرسانی ۸ — قابلیت‌های جدید گریدها، فیلتر تاریخ دومتنی + تقویم، مسیر ذخیره پیوست در wwwroot و ۹ درخواست کاربر (۲۰۲۶-۰۸-۲۵)

این دور ۹ درخواست کاربر را پوشش می‌دهد. **بدون مایگریشن دیتابیس** — فقط جایگزینی فایل‌های دیپلوی کافی است.

## ۱) فیلتر سرستون تاریخی: هم تایپ دستی هم تقویم
- کامپوننت جدید **`Shared/DateFilterInput.razor`**: یک تکست‌باکس (تاریخ شمسی `۱۴۰۴/۰۵/۰۱`) با آیکن تقویم؛ با کلیک روی آیکن، **تقویم جلالی کوچک** (popover) باز می‌شود و تاریخ انتخابی همان‌جا درج می‌شود. دکمه «پاک‌سازی» هم دارد.
- در **لیست پروژه‌ها** روی فیلترهای سرستون «تاریخ ورود / تاریخ خروج / تاریخ ثبت / تاریخ نیازمندی» فعال شد (قبلاً فقط تایپ دستی بود).

## ۲) تغییر مسیر ذخیره تصاویر/مستندات به داخل wwwroot
- **`ProjectFileProtection`**: ریشه پیش‌فرض فایل‌های رمزنگاری‌شده به `wwwroot/SecureFiles` منتقل شد (قبلاً `SecureFiles` کنار ContentRoot بود).
- **مهاجرت خودکار:** هنگام استارت، فایل‌های مسیر قدیمی (به‌همراه کلید `.key`) به مسیر جدید کپی می‌شوند و در لاگ کنسول ثبت می‌شود — فایل‌های قبلی بدون دخالت شما حفظ و سرو می‌شوند.
- تست‌شده ✅: آپلود عکس → فایل رمزنگاری‌شده در `wwwroot/SecureFiles` ایجاد شد و فایل‌های تستی مسیر قدیمی هم خودکار منتقل شدند.
- ⚠️ نکته مهم: فایل `.key` داخل همان پوشه است؛ از پوشه `wwwroot/SecureFiles` **پشتیبان دوره‌ای** بگیرید (بدون کلید، پیوست‌ها بازنمی‌شوند).

## ۳) ساعت قبل از دقیقه در ثبت گزارش کار
- **`TimeInput`**: چیدمان LTR شد تا دقیقاً مثل ساعت دیجیتال **ابتدا ساعت، بعد دقیقه** نمایش داده شود (در الزام RTL صفحه نمی‌چرخد).

## ۴) حذف متن راهنمای «ساعت خالص» از فرم ثبت گزارش
- هشدار/راهنمای محاسبه ساعت خالص از **فرم ثبت/ویرایش گزارش** حذف شد؛ کاربر فقط در **لیست گزارش‌ها** ستون ساعت خالص را می‌بیند (سردرگمی هنگام ورود داده کمتر شد).

## ۵) مرتب‌سازی (سورت) صعودی/نزولی روی سرستون گریدها
- **لیست پروژه‌ها** و **گزارش کارها**: با کلیک روی سرستون، مرتب‌سازی صعودی ↔ نزولی با فلش ▲▼ تغییر می‌کند (کلیک مجدد = معکوس).
- پروژه‌ها: کد، نام، سریال، کارفرما، تاریخ ورود/خروج/ثبت/نیازمندی، فاکتور، کارشناسی، هزینه انجام‌شده، پوشه فایل — پیش‌فرض: جدیدترین اول.
- گزارش‌ها: تاریخ، کد پروژه، نام پروژه، کاربر، ساعت شروع/پایان، استراحت، ساعت خالص — پیش‌فرض: تاریخ نزولی.

## ۶) نمای کارتی (Card View) برای گریدها
- در هر دو لیست بالای جدول، کلید دوحالته **جدول ⇄ کارت** اضافه شد؛ انتخاب کاربر در localStorage ذخیره می‌شود و بار بعد حفظ است.
- کارت‌ها تمام اطلاعات سطر را خلاصه و خوانا نشان می‌دهند؛ منوی ⋮ سه‌نقطه پروژه‌ها و دکمه‌های ویرایش/حذف گزارش‌ها در کارت هم در دسترس است (مشمول همان سطوح دسترسی).

## ۷) افزایش زمان نمایش پیام‌های خطا
- پیام‌های خطا: **۱۲ ثانیه** | اطلاعاتی: ۸ ثانیه | موفقیت: ۶٫۵ ثانیه (قبلاً همه ~۴ ثانیه بودند و خطاها فراموش می‌شدند).

## ۸) بسته‌نشدن مودال با کلیک بیرون
- **`Shared/Modal.razor`**: کلیک روی پس‌زمینه (backdrop) دیگر فرم را نمی‌بندد؛ بستن فقط با دکمه **انصراف/✕** (یا میان‌بر Esc) انجام می‌شود — جلوگیری از بسته‌شدن ناخواسته فرم‌های طولانی.

## ۹) نمایش REهای موجود یک کد پروژه هنگام ثبت برگشتی
- در فرم «پروژه برگشتی»، وقتی کدی از پروژه انتخاب می‌شود، کادر راهنما **لیست REهای قبلی همان کد** (تراشه‌های کد پروژه) را نشان می‌دهد و **عدد بعدی پیشنهادی** به‌صورت خودکار پر می‌شود — کاربر می‌داند RE چندم را ثبت می‌کند.

---

---

# به‌روزرسانی ۸ (اصلاحیه ۱) — بازبینی عمیق سه درخواست تکراری کاربر (۲۰۲۶-۰۸-۲۵)

کاربر سه درخواست «۱) فیلتر تاریخ دستی+تقویم، ۲) ذخیره پیوست در wwwroot، ۳) اول ساعت بعد دقیقه» را دوباره فرستاد. هر سه در به‌روزرسانی ۸ پیاده شده بود؛ این اصلاحیه علت‌های احتمالی کارنکردن در محیط کاربر را پیدا و رفع کرد:

## تغییرات
- **تقویم فیلتر تاریخ (باگ واقعی — بریده‌شدن پاپ‌اور):** پاپ‌اور تقویم `DateFilterInput` با `position: absolute` داخل ردیف فیلتر جدول رندر می‌شد و چون جدول‌ها داخل `.table-responsive` (با `overflow-x: auto`) هستند، تقویم در بسیاری حالت‌ها **بریده/پنهان** می‌شد. حالا مثل منوی ⋮ (که قبلاً کاربر تأیید کرد) با `position: fixed` در مختصات کلیک باز می‌شود و دیگر بریده نمی‌شود. دکمه «امروز» هم به تقویم اضافه شد.
- **نکته فنی فیلتر دستی:** از قبل تابع `Norm` ارقام فارسی/عربی/لاتین را در **دو طرف** مقایسه نرمال می‌کند؛ یعنی تایپ دستی `1405/06` هم کار می‌کند (نیازی به ارقام فارسی نیست) — اگر کاربر نتیجه نمی‌گرفت به‌خاطر نسخه کش‌شده بود.
- **چیدمان ساعت/دقیقه:** کد `TimeInput` بررسی مجدد شد — `direction: ltr` و ترتیب ساعت:دقیقه درست و در هر ۸ محل استفاده (فرم گزارش + گزارش سریع) اعمال است؛ دیدن حالت قدیمی یعنی نسخه کش‌شده مرورگر. برای اثبات این موضوع **شماره نسخه به «۷.۲ — ۱۴۰۵/۰۶/۰۳» ارتقا یافت** تا روی صفحه لاگین مشخص شود کاربر چه نسخه‌ای را می‌بیند.
- **امنیت پیوست‌ها (مهم با انتقال به wwwroot):** فایل‌های رمزنگاری‌شده حالا زیر wwwroot قرار دارند؛ یک میان‌افزار جدید در `Program.cs` **هر دسترسی مستقیم استاتیک به `/SecureFiles` را با ۴۰۴ مسدود می‌کند** تا پیوست‌ها فقط از مسیر API (با احراز هویت و سطح دسترسی ProjectAttach) قابل دانلود باشند.
- **`deploy-single.ps1`:** هشدار برجسته فارسی در پایان اسکریپت — هنگام آپدیت سرور پوشه `wwwroot\SecureFiles` پاک/جایگزین نشود و دستور بکاپ PowerShell چاپ می‌شود.

## تست‌شده (اجرای واقعی API)
- آپلود عکس → فایل رمزنگاری‌شده در `src/Inventory.Api/wwwroot/SecureFiles/…` ساخته شد ✅
- دسترسی عمومی (بدون توکن) به فایل واقعی زیر `/SecureFiles/…` → **۴۰۴** ✅ / حتی فایل `.key` → ۴۰۴ ✅
- دانلود از مسیر API با توکن → **۲۰۰** ✅ / بدون توکن → **۴۰۱** ✅
- مهاجرت خودکار فایل‌های مسیر قدیمی به wwwroot/SecureFiles هنگام استارت → در لاگ ثبت شد ✅

## ⚠️ نکته برای کاربر
- بعد از دیپلوی این نسخه، روی هر مرورگر **یک بار** کش PWA را پاک کنید (F12 ← Application ← Service Workers ← Unregister ← Clear site data ← Ctrl+F5). روی صفحه لاگین باید **«نسخه ۷.۲ — ۱۴۰۵/۰۶/۰۳»** دیده شود؛ اگر نسخه قدیمی‌تر بود یعنی هنوز نسخه کش‌شده را می‌بینید و تغییرات جدید اعمال نشده‌اند.

---

# به‌روزرسانی ۸ (اصلاحیه ۲) — انتقال API به ماشین دیتابیس + پابلیش آماده‌اجرا برای http://192.168.30.104:8091 (۲۰۲۶-۰۸-۲۵)

- **درخواست کاربر:** پابلیش API با آدرس `http://192.168.30.104:8091` (قبلاً هاست `192.168.30.108` بود — حالا API روی **همان ماشین دیتابیس ۱۹۲.۱۶۸.۳۰.۱۰۴** اجرا می‌شود).
- **`appsettings.json`:** `Server:Host = 192.168.30.104` شد (پورت همان ۸۰۹۱). رشته اتصال دیتابیس بدون تغییر (`Data Source=192.168.30.104`، کاتالوگ InventoryDb) — چون API و دیتابیس حالا هم‌ماشین‌اند، با همان IP هم درست کار می‌کند.
- **`deploy-single.ps1`:** همه ارجاع‌های ۱۰۸ به ۱۰۴ به‌روزرسانی شد.
- **خروجی پابلیش (تحویل‌شده به کاربر):** `Publish_Server_104_8091.zip` (~۲۶MB) = پوشه `publish/server` شامل `Inventory.Api.exe` + کلاینت Blazor داخل wwwroot — ساخته‌شده با `dotnet publish -c Release -r win-x64 --self-contained false` (کلاینت داخل wwwroot تزریق شد؛ `appsettings.Development.json` از خروجی حذف شد تا در Production سردرگمی ایجاد نکند).
  - ⚠️ نیازمند **ASP.NET Core Runtime 8** روی ماشین ۱۰۴ (framework-dependent). اگر نصب نیست و کاربر نسخه Self-Contained خواست، با `--self-contained true` بسازید.

## دستورالعمل استقرار روی ماشین ۱۹۲.۱۶۸.۳۰.۱۰۴
1. zip را در پوشه کوتاه‌مسیر (مثل `C:\Forough\Server`) اکسترکت کنید.
2. **اگر نسخه قبلی روی ماشین دیگری (یا همین ماشین در پوشه دیگر) اجرا می‌شد و پیوست دارد:** پوشه `wwwroot\SecureFiles` — به‌ویژه فایل `.key` — را از پوشه قبلی به پوشه جدید کپی کنید؛ وگرنه پیوست‌های قبلی غیرقابل فتح می‌شوند. (دیتابیس که روی همین ماشین است، پس رکوردها حفظ‌اند.)
3. اجرای `Inventory.Api.exe` (کنسول یا Windows Service). توجه: چون `Server:Host` روی IP مشخص است، **IP ماشین باید ثابت/دقیقاً ۱۹۲.۱۶۸.۳۰.۱۰۴ باشد**؛ اگر bind خطا گرفت یعنی IP دستگاه فرق دارد (`ipconfig` را چک کنید).
4. باز کردن پورت در فایروال ویندوز ۱۰۴:
   `New-NetFirewallRule -DisplayName "Inventory 8091" -Direction Inbound -Protocol TCP -LocalPort 8091 -Action Allow`
5. مرورگر کاربران: `http://192.168.30.104:8091` — روی صفحه لاگین باید **«نسخه ۷.۲ — ۱۴۰۵/۰۶/۰۳»** دیده شود؛ در غیر این صورت کش PWA را یک بار پاک کنید (F12 ← Application ← Service Workers ← Unregister ← Clear site data ← Ctrl+F5).
6. در اولین اجرا مایگریشن دیتابیس خودکار اعمال می‌شود (رشته اتصال همان InventoryDb روی همین ماشین).

---

# به‌روزرسانی ۹ — پوشه‌بندی پیوست‌ها بر اساس پروژه + کارتابل مدیر/کارشناس + زنگ اعلان + SignalR زنده (۲۰۲۶-۰۸-۲۵)

چهار درخواست جدید کاربر. **دارای مایگریشن خودکار دیتابیس** (`AddProjectFlowCartableAndAttachFolders`) — کافی است نسخه را اجرا کنید؛ ستون‌ها ساخته و پروژه‌های موجود «نهایی» می‌شوند تا کارتابل‌ها فقط موارد تازه را نشان دهند. **ماژول دسترسی جدید `ProjectCartable` خودکار سید و به نقش Admin داده می‌شود** — برای نقش‌های دیگر از صفحه «نقش‌ها» تیک بزنید.

## ۱) پوشه‌های اختصاصی پیوست‌ها در wwwroot (تصاویر جدا از مستندات)
- پوشه `wwwroot/SecureFiles` در سورس API ایجاد شد (با README راهنما).
- ساختار ذخیره‌سازی جدید: `wwwroot/SecureFiles/<کد پروژه>/<تصاویر|مستندات|سایر>/<نام رمزنگاری‌شده>.bin` — هر پروژه پوشهٔ خودش (مثل پروژه «آریا») و فایل‌ها بر اساس پسوند تفکیک می‌شوند.
- `StoredFileName` حالا مسیر نسبی را نگه می‌دارد؛ سازگاری کامل با فایل‌های قدیمی (فال‌بک تخت و مسیر legacy حفظ شد).
- **سازماندهی خودکار در استارت:** فایل‌های تخت قدیمی به پوشهٔ پروژهٔ خودشان + نوع‌شان منتقل و رکورد دیتابیس به‌روز می‌شود (لاگ: «X پیوست … منتقل شد») — تست واقعی انجام شد ✅.
- باگ شکارشده در تست: `GetPath` برای «مقصد» نباید فال‌بک فایل‌تخت بگیرد (متد مجزای `GetNewPath`)؛ و `string.Contains(char)` در EF/Core روی SQLite ترجمه نمی‌شود (به رشته تبدیل شد).
- گارد امنیتی `/SecureFiles/**` از قبل دسترسی عمومی (حتی با پوشه‌های فارسی) را ۴۰۴ می‌کند — تست ✅.

## ۲) کارتابل مدیر + کارتابل کارشناس (کارتابل بخش مدیریت پروژه)
- صفحهٔ جدید «کارتابل پروژه» (مسیر `/project-cartable`) با دو تب: **مدیر** (پروژه‌های تازه‌ثبت‌شده در انتظار بررسی) و **کارشناسی** (تاییدشده‌های مدیر در انتظار کارشناسی). طراحی کارتی واکنش‌گرا + شمارندهٔ هر صف + ترتیب FIFO + برچسب «X روز در انتظار».
- گردش‌کار: «ثبت» → وضعیت ۰ (کارتابل مدیر) → «تایید مدیر» (با یادداشت اختیاری) → وضعیت ۱ (کارتابل کارشناسی) → «اتمام کارشناسی» (یادداشت اختیاری) → وضعیت ۳ (نهایی). «رد مدیر» با **دلیلِ الزامی** → وضعیت ۲.
- ستون «وضعیت» رنگی (FlowChip) به جدول و کارت‌های «ورود و خروج پروژه‌ها» اضافه شد + فیلتر+سورت سرستون وضعیت.
- مجوزها: `ProjectCartable.Read` (مشاهده)، `.Manager` (تایید/رد)، `.Expert` (اتمام کارشناسی) — سرورساید + سمت کلاینت (گارد مسیر و تب‌ها).

## ۳) بعد از ثبت پروژه: در کارتابل مدیر + زنگ اعلان
- ثبت پروژه → خودکار در کارتابل مدیر + **اعلان زنگ** به همهٔ کاربران دارای `ProjectCartable.Manager` (ادمین‌های legacy هم) با لینک مستقیم به کارتابل.
- تایید مدیر → زنگ به کارشناس‌ها + به ثبت‌کننده؛ رد → زنگ به ثبت‌کننده همراه دلیل؛ اتمام کارشناسی → زنگ به مدیران + ثبت‌کننده. (به خودِ انجام‌دهندهٔ عمل اعلان نمی‌رود.)
- هلپر مشترک `UsersWithPermissionAsync` در `RbacControllerBase` برای یافتن کاربران یک مجوز (الگوی ItRequests).

## ۴) SignalR روی مدیریت پروژه
- همهٔ رویدادها (ثبت/ویرایش/حذف/تایید/رد/اتمام) `BroadcastChangedAsync("projects")` می‌زنند (روی هاب موجود NotifyHub — اتصال سراسری از قبل در MainLayout برقرار است).
- صفحات «ورود و خروج پروژه‌ها» و «کارتابل پروژه» روی `RealtimeService.DataChanged` مشترک‌اند و خودکار رفرش می‌شوند — بدون F5. نشانگر «متصل زنده» در کارتابل.

## تست‌شده (اجرای واقعی API + SQLite)
- ثبت پروژه → FlowStatus=0 و ظاهر در صف مدیر ✅ | زنگ به کاربر مدیر دوم ✅
- رد بدون دلیل → ۴۰۰ فارسی ✅ | رد با دلیل → وضعیت ۲ ✅ | تایید → وضعیت ۱ و انتقال به صف کارشناسی ✅ | اتمام → وضعیت ۳ و counts صفر ✅
- آپلود PNG → `SecureFiles/1/تصاویر/…` و PDF → `SecureFiles/1/مستندات/…` ✅
- فایل تختِ ساختگی → در بوت بعدی خودکار به `1/تصاویر/` منتقل و دیتابیس به‌روز شد ✅
- دانلود پیوست واقعی از API با توکن ۲۰۰ ✅ | دسترسی عمومی به `/SecureFiles/1` → ۴۰۴ ✅
- بیلد نهایی راه‌حل: ۰ خطا ✅

---

# یادداشت فنی — رفع مشکل دانلود فضای کاری (۲۰۲۶-۰۸-۲۵)

علت ناموفق‌بودن دانلود WORKSPACE: ابزارهای توسعه (کیت .NET SDK ~۵۷۸مگ و کش بسته‌های NuGet ~۱.۱گیگ) داخل فضای کاری نصب شده بودند و حجمش را به ~۱.۹ گیگابایت رسانده بودند (بالاتر از سقف دانلود). این ابزارها از فضای کاری خارج/پاک شدند — حجم فعلی ~۳۴ مگابایت و دانلود مجدد ممکن است. محتوای پروژه و بسته‌های تحویلی دست‌نخورده مانده‌اند.

---

# به‌روزرسانی — قواعد وضعیت، فرم تاریخ‌ها و رفع چاپ گزارش (۲۰۲۶-۰۸-۳۱)

## ۱) روش کار «وضعیت» در مدیریت پروژه — قواعد نهایی

### چرخهٔ وضعیت (FlowStatus)

| کد | وضعیت | چه کسی عبورش می‌دهد | گزارش کار؟ |
|---|---|---|---|
| ۰ | در انتظار تایید مدیر | مدیر (`ProjectCartable.Manager`) | ❌ **مسدود** |
| ۱ | در انتظار کارشناسی | کارشناس (`ProjectCartable.Expert`) | ✅ مجاز |
| ۲ | رد شده توسط مدیر | ثبت‌کننده (اصلاح + ارسال مجدد) | ❌ **مسدود** |
| ۳ | نهایی (کارشناسی انجام شد) | — (پایان چرخه) | ✅ مجاز |

```
ثبت پروژه ──► ۰ در انتظار تایید مدیر
                 │ تایید مدیر            │ رد مدیر (دلیل الزامی)
                 ▼                       ▼
              ۱ در انتظار کارشناسی     ۲ رد شده
                 │ اتمام کارشناسی        │ ارسال مجدد (بعد از اصلاح)
                 ▼                       └──────────► برمی‌گردد به ۰
              ۳ نهایی
```

### پاسخ به پرسش‌ها

**«پروژه‌ای که در انتظار تایید مدیر است، آیا باید بتوان برایش گزارش کار ثبت کرد؟» → خیر.**
منطق: تا وقتی مدیر پروژه را نپذیرفته، کار روی آن رسماً شروع نشده؛ اگر گزارش کار ثبت شود و بعد مدیر
رد کند، ساعات ثبت‌شده بی‌اعتبار و آمار «کل زمان مصرفی» آلوده می‌شود.

**«اگر مدیر رد کرد چطور؟» → باز هم خیر.**
پروژهٔ ردشده متوقف است. مسیر درست: اصلاح ایرادها → **ارسال مجدد به مدیر** → تایید → آنگاه گزارش کار.

**«اگر تایید شد چه اتفاقی می‌افتد؟»**
وضعیت به «۱ = در انتظار کارشناسی» می‌رود، پروژه از کارتابل مدیر خارج و وارد کارتابل کارشناسی می‌شود،
به کارشناس‌ها و به ثبت‌کننده زنگ اعلان می‌رود، و **از همین لحظه ثبت گزارش کار فعال می‌شود**.
با «اتمام کارشناسی» وضعیت به «۳ = نهایی» می‌رسد و گزارش کار همچنان مجاز می‌ماند
(کارهای تکمیلی/گارانتی روی پروژهٔ بسته‌شده هم قابل ثبت است).

### پیاده‌سازی
- **سرور (منبع حقیقت):** `ReportWorksController.ValidateAsync` وضعیت پروژه را چک می‌کند و برای
  وضعیت‌های ۰ و ۲ خطای ۴۰۰ با پیام فارسی گویا برمی‌گرداند — هم در ثبت و هم در ویرایش گزارش کار.
- **کمبوی پروژه در فرم گزارش کار:** فیلد جدید `ProjectLookups.ReportableProjects` فقط پروژه‌های
  وضعیت ۱ و ۳ را برمی‌گرداند؛ صفحهٔ «گزارش‌های کار» از همین فهرست استفاده می‌کند
  (کمبوی *فیلتر* بالای صفحه همچنان همهٔ پروژه‌ها را دارد تا گزارش‌های قدیمی قابل جستجو بمانند).
- **منوی سطر پروژه:** گزینهٔ «افزودن گزارش کار» برای وضعیت‌های ۰ و ۲ **غیرفعال** (با آیکون قفل)
  و تولتیپ فارسیِ دلیل نمایش داده می‌شود.
- **ارسال مجدد:** اندپوینت `POST api/projectcartable/{id}/resubmit` (مجوز `Projects.Update`) —
  وضعیت ۲ → ۰، یادداشت رد مدیر پاک و به مدیران با ذکر «دلیل رد قبلی + توضیح اصلاح» اعلان می‌رود.
  در UI: منوی سطر ⋮ ← «ارسال مجدد به مدیر» (فقط روی پروژه‌های ردشده دیده می‌شود).
- **جزئیات پروژه:** سطرهای «وضعیت گردش‌کار»، «دلیل رد مدیر / یادداشت مدیر» و «یادداشت کارشناسی»
  با تاریخ اقدام اضافه شد.

## ۲) فرم «ثبت/ویرایش تاریخ‌ها» در منوی سطر

مثل فرم فاکتور، یک فرم مجزا از دراپ‌داون هر سطر: **⋮ ← ثبت/ویرایش تاریخ‌ها**
شامل پنج تاریخ که کاربر می‌تواند بعداً وارد یا ویرایش کند:

1. تاریخ نیاز مشتری (`CustomerRequiredDate`)
2. تاریخ تحویل پروژه (`DeliveryDate`)
3. تاریخ خروج موقت (`TemporaryExitDate`)
4. تاریخ خروج (`ExitDate`)
5. تاریخ تحویل پرونده (`FileDate`)

- هر فیلد **اختیاری** است و دکمهٔ «پاک» جداگانه دارد (خالی‌کردن = پاک‌شدن مقدار در دیتابیس).
- کارت هر تاریخ وقتی پر شود سبز می‌شود؛ کنار گزینهٔ منو نشانگر «n/۵» تعداد تاریخ‌های تکمیل‌شده دیده می‌شود.
- هدر فرم: نام/کد/سریال/کارفرمای پروژه + **تاریخ ورود** به‌عنوان مرجع.
- **اعتبارسنجی:** هیچ‌کدام از تاریخ‌ها نمی‌تواند قبل از «تاریخ ورود» باشد — هم سمت کلاینت (Toast)
  و هم سمت سرور (۴۰۰ فارسی).
- اندپوینت: `PUT api/projects/{id}/dates` با `ProjectDatesDto` — مجوز `Projects.Update`.
  بعد از ذخیره، `BroadcastChangedAsync("projects")` لیست همهٔ کاربران را زنده به‌روز می‌کند.
- **بدون مایگریشن** — هر پنج ستون از قبل روی موجودیت `ProjectEntryExit` وجود داشتند و فقط
  از فرم بزرگ ویرایش قابل دسترسی نبودند.

## ۳) رفع «چاپ گزارش انجام نمی‌شود»

سه اشکال مستقل با هم باعث می‌شدند دکمهٔ چاپ هیچ کاری نکند:

1. **کلاس `PrintTableOptions` اصلاً وجود نداشت.** هر دو صفحهٔ `ProjectsList` و `ReportWorks` از آن
   استفاده می‌کردند ولی فایل `Services/PrintModels.cs` در ادغام ماژول جا مانده بود → پروژهٔ کلاینت
   **کامپایل نمی‌شد**. فایل ساخته شد.
2. **متدهای الحاقی فارسی جا مانده بودند:** `FaDigits()`، `ToFaDate()`، `TimeFa()` و `HoursFa()`
   در `Extensions/PersianExtensions.cs` تعریف نشده بودند (۶۳ فراخوانی در صفحات پروژه).
   همگی اضافه شدند — `HoursFa` عمداً ساعت‌های بیش از ۲۴ را هم درست نشان می‌دهد
   (`۱۲۵:۳۰` برای جمع ساعات پروژه) و با `InvariantGlobalization=true` سازگار است.
3. **بلاک‌شدن پاپ‌آپ:** `printTableReport` با `window.open` کار می‌کرد، ولی از یک ادامهٔ `async`
   در C# (بعد از `await` روی فراخوانی API) صدا زده می‌شود؛ مرورگر آن را «خارج از تعامل کاربر»
   می‌بیند و پنجره را بی‌صدا بلاک می‌کند. حالا چاپ با **iframe پنهانِ هم‌مبدأ** انجام می‌شود
   (بلاک نمی‌شود)، و اگر iframe به هر دلیلی کار نکند به `window.open` و در نهایت پیام فارسی
   فال‌بک می‌شود. `onafterprint` + تایمر ایمنی، iframe را پاک می‌کنند.

بهبودهای جانبی همان فایل JS: پذیرش هر دو حالت camelCase/PascalCase در کلیدهای گزینه‌ها،
تکرار سرستون جدول در صفحات بعدی چاپ (`display: table-header-group`)، جلوگیری از شکستن سطر بین صفحات،
و فال‌بک دانلود برای `openBlob` وقتی پاپ‌آپ بلاک شود.


---

# به‌روزرسانی ۱۲ — مهاجرت داده‌های سیستم قدیمی (ویندوز فرم / دیتابیس aria) + فیلد «اپراتور» گزارش کار (۲۰۲۶‑۰۹‑۰۳)

**دارای مایگریشن خودکار** (`LegacyAriaImportPrep`) — با اجرای نسخهٔ جدید اعمال می‌شود.

## تغییرات مدل
- `ReportWork.OperatorId` (اختیاری، FK به `Users`): **اپراتور = کسی که عملاً روی پروژه کار کرده**؛ با `UserId` (ثبت‌کنندهٔ گزارش) فرق دارد. معادل ستون `Operator` جدول `ReportWork` قدیمی.
- `ProjectEntryExit.KarshenasiAvalie`: ۵۰ → ۱۰۰ کاراکتر.
- `ProjectEntryExit.TotalSpentTime`: ذخیره به‌صورت `bigint` (تیک) به‌جای `time` — چون `time` بیش از ۲۴ ساعت را نمی‌پذیرد و پروژه‌های واقعی صدها ساعت گزارش دارند. مقادیر موجود در مایگریشن تبدیل می‌شوند.

## UI / API
- صفحهٔ گزارش‌های کار: ستون و فیلتر «اپراتور» + انتخاب اپراتور در فرم ثبت/ویرایش (لیست کاربران). ستون قبلی «کاربر» به «ثبت‌کننده» تغییر نام داد. چاپ و اکسل هم ستون اپراتور دارند.
- فرم «گزارش سریع» در لیست پروژه‌ها هم انتخاب اپراتور دارد.
- `GET /api/reportworks?operatorId=` فیلتر جدید.

## مهاجرت داده‌ها
- پوشهٔ `inventory/sql/legacy-aria/` — تحلیل کامل بک‌آپ (`LEGACY-ARIA-ANALYSIS.md`)، خروجی CSV/JSON همهٔ جداول، و اسکریپت `Import-LegacyAria.sql`.
- شمارش: ۲۰۱ کارفرما، ۱۱ نوع فاکتور، ۱۷۷۴ پروژه (۵۳ برگشتی `REn-کد`)، ۶۶۹۸ گزارش کار (۶۶۰۱ با اپراتور)، ۴۹ کاربر قدیمی.
- مراحل اجرا در بخش ۵ همان فایل تحلیل.
