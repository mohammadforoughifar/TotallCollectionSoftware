# قابلیت «گزارش‌ساز» — ساخت گزارش و ویجت داشبورد توسط خود کاربر

تاریخ: ۱۴۰۵/۰۶/۲۵ (2026-09-16) — وضعیت: پیاده‌سازی کامل، بیلد بدون خطا، تست اجرای واقعی روی همهٔ دیتاست‌ها

## ۱) هدف

کاربر بدون نیاز به برنامه‌نویس، گزارش دلخواه خودش را بسازد:

۱. یک **دیتاست آماده** (از قبل join‌شده) انتخاب کند؛
۲. ستون‌ها، **سنجه‌ها** (جمع/تعداد/میانگین/کمینه/بیشینه/تعداد یکتا)، **فیلترها** و **مرتب‌سازی** را تعیین کند؛
۳. خروجی را در یکی از سه شکل **جدول / شاخص (KPI) / نمودار** ببیند؛
۴. گزارش را **ذخیره** کند و با **نقش‌ها** به اشتراک بگذارد؛
۵. گزارش ذخیره‌شده را به‌عنوان **ویجت داشبورد** («داشبورد من») اضافه کند.

خروجی فقط نمایشی است (export/print در این فاز ندارد).

## ۲) معماری

```
Inventory.Shared/Dtos/ReportBuilderDtos.cs      enumها + DTOها (مشترک بین API و کلاینت)
Inventory.Api/Services/Reports/
    ReportModel.cs            ReportColumn<T>، RawRow، IReportDataset، ReportDataset<T>، ColumnSet<T>، EnumFa
    ReportRows.cs             ~۳۰ کلاس ردیف تخت (یکی برای هر دیتاست)
    ReportDatasetRegistry.cs  تعریف ۳۱ دیتاست + IReportDatasetProvider
    ReportExecutor.cs         موتور اجرا: فیلتر → ماده‌سازی → گروه‌بندی/تجمیع → خروجی (جدول/KPI/نمودار)
Inventory.Api/Entities/Reports/UserReportEntities.cs   UserReport + UserReportRoleShare
Inventory.Api/Data/ReportBuilderSchemaV1.cs            ساخت idempotent جداول (SQL Server و SQLite)
Inventory.Api/Services/Core/EffectivePermissions.cs    مجوزهای مؤثر کاربر (اول claimهای JWT، بعد DB)
Inventory.Api/Controllers/Reports/ReportBuilderController.cs   CRUD + preview + run + roles
Inventory.Client/Services/ReportClient.cs              کلاینت HTTP
Inventory.Client/Pages/Reports/ReportBuilder.razor     صفحهٔ گزارش‌ساز (مسیر /report-builder)
```

**الگوی هر دیتاست:** یک کلاس «ردیف تخت» با همهٔ فیلدهای لازم؛ joinها در یک `.Select()` انجام می‌شود
(بدون GroupBy روی join، چون EF ترجمه‌اش نمی‌کند). ستون‌ها accessor روی همان کلاس‌اند.

**RawRow:** برای اجرای یکنواخت فیلتر/تجمیع، مقدار هر ستون در خانه‌های نوعی کپی می‌شود:
`S0..S9` (رشته)، `M0..M9` (decimal)، `I0..I9` (int)، `R0..R4` (double)، `D0..D4` (تاریخ)، `B0..B3` (bool).
این خانه‌ها **field** هستند نه property (در ReportExecutor با `GetField`/`Expression.Field` خوانده می‌شوند).

**فیلترها:** رشته/تاریخ/عدد صحیح/بول به SQL push می‌شوند؛ ستون‌های `decimal` (خانهٔ M) به‌دلیل محدودیت
ترجمهٔ decimal در SQLite در حافظه فیلتر می‌شوند (`ServerFilterable=false`). سقف خواندن: ۲۰٬۰۰۰ ردیف
(`MaxScan`) و در پاسخ `ScannedRows`/`Truncated` برمی‌گردد تا UI هشدار بدهد.

**تاریخ‌ها:** همهٔ بازه‌های آماده (`ReportDatePreset`) بر اساس **تقویم شمسی** محاسبه می‌شوند؛
ورودی دستی هم شمسی پذیرفته می‌شود (`۱۴۰۵/۰۶/۰۱` یا `1405-06-01`) و سال ≤ ۱۵۰۰ شمسی تفسیر می‌شود.

**مقادیر Enum/وضعیت:** ستون‌های Enum به‌صورت int پروژه می‌شوند و برچسب فارسی از `EnumFa` می‌آید.
برای ستون‌های وضعیتِ رشته‌ای (دستور کار، درخواست IT، مرخصی) `TextLabels` اضافه شده تا هم نمایش فارسی شود
و هم کاربر بتواند در فیلتر **برچسب فارسی** بفرستد (سرور با `Unlabel` به مقدار خام برمی‌گرداند).

## ۳) endpointها

| متد | مسیر | کاربرد | مجوز |
|---|---|---|---|
| GET | `api/reports/datasets` | دیتاست‌های مجاز کاربر + `canDesign` | `ReportBuilder.View` + مجوز ماژول هر دیتاست |
| GET | `api/reports/roles` | نقش‌های فعال برای اشتراک‌گذاری | `ReportBuilder.Design` |
| POST | `api/reports/preview` | اجرای کوئری بدون ذخیره | `ReportBuilder.View` + ماژول دیتاست |
| GET | `api/reports` | گزارش‌های من + اشتراک‌شده با نقش‌هایم | `ReportBuilder.View` |
| POST | `api/reports` | ذخیرهٔ گزارش جدید | `ReportBuilder.Design` |
| PUT | `api/reports/{id}` | ویرایش (فقط مالک) | `ReportBuilder.Design` |
| DELETE | `api/reports/{id}` | حذف (فقط مالک) | `ReportBuilder.Design` |
| GET | `api/reports/{id}/data` | اجرای گزارش ذخیره‌شده | `ReportBuilder.View` + دسترسی به گزارش |
| GET | `api/my-dashboards/widget-catalog` | شامل ویجت‌های `rep:{id}` در دستهٔ «گزارش‌های من» | `MyDashboards.View` |
| GET | `api/my-dashboards/widget-data/rep:{id}` | دادهٔ ویجت گزارش | کنترل مالکیت/اشتراک + ماژول داده |

### امنیت (سه لایه)

1. **ماژول ReportBuilder** برای ورود به API (View برای دیدن/اجرا، Design برای ساخت/ویرایش/حذف).
2. **ماژول دادهٔ هر دیتاست** هم در فهرست دیتاست‌ها، هم در preview، هم در run، هم هنگام پین‌کردن ویجت
   کنترل می‌شود → داشتن گزارش‌ساز به معنی دیدن همهٔ داده‌ها نیست و دستکاری `QueryJson` در کلاینت هم نشت داده نمی‌دهد.
3. **گزارش دیگران** فقط با `Visibility=RoleShared` و عضویت در یکی از نقش‌های اشتراک‌گذاشته‌شده دیده می‌شود؛
   در غیر این صورت ۴۰۴ (بدون افشای وجود گزارش).

نکتهٔ عملی: `RbacSeeder` در هر اجرا مجوزهای `ReportBuilder.View/Design` را به **همهٔ نقش‌های فعال** می‌دهد
(مثل «داشبورد من») تا بعد از ارتقا از منوی کسی حذف نشود؛ مدیر می‌تواند از «تنظیمات ← نقش‌ها و دسترسی‌ها» بردارد.
کلاینت مجوزها را از claimهای JWT می‌خواند، بنابراین بعد از ارتقا **یک بار خروج/ورود دوباره** لازم است.

## ۴) دیتاست‌ها (۳۱ دیتاست در ۱۱ دسته)

### اسناد و مکاتبات
- **مدارک آرشیو** (`doc-archive` — ماژول `DocArchive`) — مدارک بایگانی‌شده با پوشه و انقضا.
  - ابعاد: تاریخ ثبت, تاریخ انقضا, عنوان مدرک, کد مدرک, پوشه, کد مشتری, ثبت‌کننده, فعال
- **نامه‌های داخلی** (`inner-letter` — ماژول `InnerLetters`) — نامه‌های داخلی ثبت‌شده.
  - ابعاد: تاریخ ثبت, شماره نامه, موضوع, ایجادکننده, محرمانگی, فوریت, نشان‌دار
- **نامه‌های صادره** (`out-letter` — ماژول `InnerLetters`) — نامه‌های صادره از دبیرخانه.
  - ابعاد: تاریخ ثبت, تاریخ صدور, شماره, وضعیت, ایجادکننده, نشان‌دار

### انبار و موجودی
- **ارزش موجودی** (`inv-stock-value` — ماژول `InvDocs`) — ارزش ریالی موجودی بر اساس کالا و انبار.
  - ابعاد: آخرین به‌روزرسانی, کالا, دسته, انبار
  - سنجه‌ها: مقدار, میانگین بها, ارزش ریالی
- **اسناد انبار** (`inv-doc` — ماژول `InvDocs`) — رسیدها، حواله‌ها و انتقال‌های انبار.
  - ابعاد: تاریخ, نوع سند, اثر روی موجودی, وضعیت, انبار, طرف حساب, شرح
  - سنجه‌ها: تعداد ردیف, مقدار کل, ارزش کل
- **انبارگردانی** (`stk-count` — ماژول `StkSessions`) — مقایسهٔ موجودی سیستم و شمارش واقعی.
  - ابعاد: تاریخ جلسه, جلسه انبارگردانی, انبار, کالا, شمارش شده
  - سنجه‌ها: موجودی سیستم, موجودی شمارش‌شده, بهای واحد, تعداد اسکن
- **ردیف سند انبار** (`inv-doc-line` — ماژول `InvDocs`) — ریز گردش کالا در اسناد انبار (کاردکس).
  - ابعاد: تاریخ سند, نوع سند, انبار, کالا, واحد, تاریخ انقضا
  - سنجه‌ها: مقدار, فی, تخفیف
- **موجودی انبار** (`inv-stock` — ماژول `Stock`) — موجودی لحظه‌ای هر کالا در هر انبار.
  - ابعاد: کالا, کد کالا, دسته, واحد, انبار
  - سنجه‌ها: موجودی, بهای تمام‌شدهٔ میانگین, نقطه سفارش

### حسابداری
- **اسناد حسابداری — سرجمع** (`acc-voucher` — ماژول `AccVouchers`) — سرجمع اسناد حسابداری.
  - ابعاد: تاریخ, شماره سند, وضعیت, منبع سند, سال مالی
  - سنجه‌ها: تعداد ردیف, بدهکار, بستانکار
- **ردیف سند حسابداری** (`acc-voucher-line` — ماژول `AccVouchers`) — تراز آزمایشی و گزارش حساب: هر ردیف = یک گردش روی یک حساب.
  - ابعاد: تاریخ سند, شماره سند, وضعیت, کد حساب, نام حساب, سطح حساب, نوع حساب, طرف حساب, سال مالی
  - سنجه‌ها: بدهکار, بستانکار

### خزانه‌داری
- **اسناد خزانه** (`trs-voucher` — ماژول `TrsVouchers`) — دریافت‌ها، پرداخت‌ها و انتقال‌های خزانه.
  - ابعاد: تاریخ, شماره, نوع, وضعیت, طرف حساب, از حساب, به حساب
  - سنجه‌ها: مبلغ کل, نقد, چک, کارمزد, تخفیف
- **ردیف پرداخت/دریافت** (`trs-payment-line` — ماژول `TrsVouchers`) — جزئیات روش‌های پرداخت هر سند خزانه.
  - ابعاد: تاریخ سند, شماره سند, نوع سند, روش پرداخت, حساب, طرف حساب, شماره چک
  - سنجه‌ها: مبلغ
- **چک‌ها** (`trs-cheque` — ماژول `TrsCheques`) — چک‌های دریافتی و صادره با وضعیت و سررسید.
  - ابعاد: تاریخ صدور, سررسید, شماره چک, نوع, وضعیت, طرف حساب, بانک, شعبه, حساب خزانه
  - سنجه‌ها: مبلغ

### دارایی ثابت
- **دارایی ثابت** (`fa-asset` — ماژول `FixedAssets`) — دارایی‌های ثابت، بها و استهلاک انباشته.
  - ابعاد: تاریخ خرید, کد دارایی, نام دارایی, دسته, محل استقرار, فروشنده, وضعیت, روش استهلاک
  - سنجه‌ها: عمر مفید, بهای تمام‌شده, ارزش اسقاط, استهلاک انباشته

### عملیات و پشتیبانی
- **تعمیرات — اقلام** (`rep-item` — ماژول `Repairs`) — اقلام و خدمات هر سفارش تعمیر.
  - ابعاد: تاریخ پذیرش, شماره سفارش, وضعیت سفارش, شرح خدمت, قطعه/کالا
  - سنجه‌ها: تعداد, بهای تمام‌شده, مبلغ فروش
- **تعمیرات — سفارش** (`rep-order` — ماژول `Repairs`) — سفارش‌های تعمیرات.
  - ابعاد: تاریخ پذیرش, تاریخ تحویل, شماره, مشتری, تکنسین, نوع دستگاه, مدل, وضعیت
  - سنجه‌ها: تعداد قلم, مبلغ اعلامی
- **درخواست‌های IT** (`it-request` — ماژول `ItRequests`) — درخواست‌های پشتیبانی IT.
  - ابعاد: تاریخ ثبت, تاریخ ارجاع, تاریخ تکمیل, شماره, عنوان, درخواست‌کننده, سامانه, نوع درخواست, وضعیت
- **دستورهای کار** (`wo-order` — ماژول `WorkOrders`) — دستورهای کار با وضعیت، مهلت و مسئولان.
  - ابعاد: تاریخ ایجاد, مهلت, تاریخ بستن, شماره, عنوان, درخواست‌دهنده, وضعیت, ماژول مبدأ, برچسب‌ها, اولویت
  - سنجه‌ها: تعداد مسئول, تعداد تمدید

### فروش و درآمد
- **خرید — قلم فاکتور** (`fac-purchase-line` — ماژول `FacInvoices`) — هر ردیف = یک قلم فاکتور خرید با مشخصات تأمین‌کننده، کالا و انبار.
  - ابعاد: تاریخ, شماره فاکتور, تأمین‌کننده, کالا, کد کالا, دسته کالا, انبار, واحد, وضعیت, نوع تسویه
  - سنجه‌ها: مقدار, فی, تخفیف, مالیات, مبلغ کل
- **فاکتورها — سرجمع** (`fac-invoice` — ماژول `FacInvoices`) — سرجمع هر فاکتور (یک ردیف به ازای هر فاکتور).
  - ابعاد: تاریخ, شماره, نوع, وضعیت, تسویه, طرف حساب, انبار
  - سنجه‌ها: تعداد قلم, مبلغ ناخالص, تخفیف, مالیات, هزینه ارسال, مبلغ خالص
- **فروش — قلم فاکتور** (`fac-sale-line` — ماژول `FacInvoices`) — هر ردیف = یک قلم فاکتور فروش با مشخصات فاکتور، مشتری، کالا و انبار. برای تحلیل فروش بر اساس کالا/مشتری/ماه.
  - ابعاد: تاریخ, شماره فاکتور, مشتری, کالا, کد کالا, دسته کالا, انبار, واحد, وضعیت, نوع تسویه
  - سنجه‌ها: مقدار, فی, تخفیف, مالیات, مبلغ کل

### منابع انسانی
- **تردد روزانه پرسنل** (`fa-att-daily` — ماژول `FaAtt`) — کارکرد روزانهٔ هر پرسنل (محاسبه‌شده از تردد).
  - ابعاد: تاریخ, پرسنل, کد پرسنلی, اولین ورود, آخرین خروج, وضعیت روز, تردد ناقص
  - سنجه‌ها: دقیقه کارکرد, دقیقه تأخیر, دقیقه تعجیل, دقیقه اضافه‌کاری, دقیقه شب‌کاری
- **تردد کاربران** (`att-record` — ماژول `Attendance`) — تردد کاربران سامانه (وب‌کلک/موبایل).
  - ابعاد: تاریخ, کاربر, شیفت, ورود, خروج, وضعیت نهایی, مرخصی مصوب
  - سنجه‌ها: دقیقه کارکرد, دقیقه تأخیر, دقیقه خروج زودهنگام, دقیقه اضافه‌کاری, دقیقه کسری
- **فیش حقوق** (`hr-payslip` — ماژول `HrPay`) — فیش‌های حقوق با ناخالص، کسور و خالص.
  - ابعاد: تاریخ صدور, پرسنل, کد پرسنلی, سال, ماه
  - سنجه‌ها: روزهای کارکرد, حقوق پایه, ناخالص, مالیات, بیمه, سایر کسور, خالص
- **مرخصی‌ها** (`leave-request` — ماژول `LeaveRequests`) — درخواست‌های مرخصی با وضعیت و مدت.
  - ابعاد: از تاریخ, تا تاریخ, تاریخ تصویب, شماره, نوع مرخصی, درخواست‌کننده, وضعیت, تصویب‌کننده
  - سنجه‌ها: تعداد روز, تعداد ساعت
- **پرسنل** (`hr-employee` — ماژول `HrCore`) — پروندهٔ پرسنل با واحد سازمانی و حقوق پایه.
  - ابعاد: تاریخ استخدام, کد پرسنلی, نام و نام خانوادگی, واحد سازمانی, عنوان پست, موبایل, تحصیلات, نوع استخدام, وضعیت, فعال
  - سنجه‌ها: حقوق پایه

### هزینه و بودجه
- **ردیف بودجه** (`bud-budget-item` — ماژول `Budgets`) — مقایسهٔ بودجهٔ مصوب، تعهدشده و کارکرد.
  - ابعاد: سال مالی, کد حساب, نام حساب
  - سنجه‌ها: بودجه مصوب, تعهدشده, کارکرد واقعی
- **هزینه‌ها** (`exp-expense` — ماژول `Expenses`) — هزینه‌های ثبت‌شده با دسته و روش پرداخت.
  - ابعاد: تاریخ, شماره, دسته هزینه, روش پرداخت, پرداخت‌کننده, شرح
  - سنجه‌ها: مبلغ

### پروژه‌ها
- **گزارش کار پروژه** (`prj-report-work` — ماژول `ReportWorks`) — گزارش‌های کار روزانهٔ پروژه‌ها.
  - ابعاد: تاریخ گزارش, کد پروژه, ثبت‌کننده, اپراتور, شرح کار

### کالا و اشخاص
- **اشخاص (مشتری/تأمین‌کننده)** (`party-list` — ماژول `Parties`) — طرف‌های حساب: مشتری‌ها و تأمین‌کننده‌ها.
  - ابعاد: نام, نوع, تلفن, موبایل, آدرس, فعال, معرف
- **کالاها** (`prod-catalog` — ماژول `Products`) — فهرست کالاها با قیمت و نقطه سفارش.
  - ابعاد: کد کالا, نام کالا, واحد, دسته, بارکد, برند, فعال, خدماتی
  - سنجه‌ها: قیمت فروش, قیمت خرید, نقطه سفارش, حداکثر موجودی

## ۵) صفحهٔ کلاینت (`/report-builder`)

- منو: «داشبوردها و صفحات شخصی ← گزارش‌ساز» (آیکون `bi-clipboard-data-fill`)؛ بدون مجوز، آیتم کاملاً پنهان است.
- چیدمان دو ستونهٔ RTL: ستون تنظیمات (۶ جعبهٔ شماره‌دار) + ستون نتیجه (پیش‌نمایش و گزارش‌های ذخیره‌شده).
- جعبه‌ها: ۱) دیتاست (جستجو + دسته‌بندی) ۲) نوع خروجی (جدول/شاخص/نمودار + نوع نمودار + ستون تاریخ +
  مقایسه با بازهٔ قبل + حداکثر ردیف) ۳) ستون‌ها (chip با ترتیب قابل جابه‌جایی) ۴) سنجه‌ها
  (ستون + نوع تجمیع + عنوان دلخواه) ۵) فیلترها (عملگر متناسب با نوع ستون، بازهٔ آمادهٔ شمسی،
  انتخاب چندتایی برچسب‌ها) ۶) مرتب‌سازی (ستون یا سنجهٔ `m:0`).
- **اجرای خودکار** با هر تغییر (قابل خاموش‌کردن) + نوار آمار (زمان اجرا، ردیف‌های خوانده‌شده، تعداد نتیجه)
  و هشدار «سقف ردیف» در صورت ناقص بودن نتیجه.
- نمودار با همان `appDash.renderChart` (Chart.js) داشبورد رندر می‌شود.
- ذخیره: نام + دسترسی (خصوصی / اشتراک با نقش‌ها) + خلاصهٔ کوئری؛ پس از ذخیره، دکمهٔ
  «افزودن به داشبورد» ویجت `rep:{id}` را با اندازهٔ پیش‌فرض مناسبِ نوع خروجی به داشبورد انتخابی اضافه می‌کند.

## ۶) تست‌های انجام‌شده (اجرای واقعی API روی SQLite با دادهٔ نمونه)

- **هر ۳۱ دیتاست** در سه حالت (ریز / تجمیعی / نمودار) بدون خطای ترجمهٔ EF اجرا شدند.
- joinها بررسی شدند: نام پوشه، کد/نام/نوع حساب، نام کارمند، انبار/کالا/دسته، نام طرف حساب،
  شمارهٔ چک، حساب خزانه (join تودرتو)، وضعیت‌های فارسی.
- خروجی‌ها: جدول تجمیعی چندسنجه‌ای، جدول ریز ۶ ستونه، KPI با `ComparePrev` (دلتا ٪۲۳۳+)،
  نمودار میله‌ای/خطی/دایره‌ای با مرتب‌سازی روی سنجه.
- فیلترها: Contains / StartsWith / Eq / Ne / Gt(e) / Lt(e) / Between (شمسی و میلادی و با ارقام فارسی) /
  In (ترکیب برچسب فارسی و مقدار خام) / IsNull / NotNull / True / False / بازه‌های آمادهٔ شمسی / decimal در حافظه.
- CRUD و اشتراک: ساخت، فهرست، اجرا با شناسه، ویرایش، حذف، پنهان‌بودن گزارش خصوصی از کاربر دیگر (۴۰۴)،
  دیده‌شدن گزارش اشتراکی برای عضو نقش، ۴۰۳ برای کاربر بدون مجوز ماژول داده.
- یکپارچگی داشبورد: ویجت `rep:{id}` در کاتالوگ با نوع و عنوان درست؛ اجرای `widget-data`؛
  ذخیرهٔ چیدمان با ویجت گزارش؛ ردشدن ویجت نامعتبر یا گزارش دیگران؛ کاربر محدود نتوانست گزارش دیگری را پین کند.

## ۷) باگ‌هایی که حین پیاده‌سازی/تست پیدا و رفع شد

1. **خانه‌های RawRow فیلد بودند نه property** → `GetProperty`/`Expression.Property` به
   `GetField`/`Expression.Field` تغییر کرد (خطای «خانهٔ S0 در RawRow وجود ندارد»).
2. **`ScannedRows`/`Truncated` اشتباه گزارش می‌شد**: بعد از محاسبهٔ بازهٔ قبل، آخرین `LoadAsync`
   مقادیر را بازنویسی می‌کرد → مقادیر از اولین خواندن در متغیر محلی نگه داشته می‌شوند.
3. **`QueryJson` در داشبورد خوانده نمی‌شد**: ذخیره با camelCase بود ولی
   `JsonSerializer.Deserialize` در `DashboardsController` حساس به بزرگی/کوچکی → ویجت گزارش خالی
   («حداقل یک ستون یا یک سنجه») برمی‌گشت. `ParseReportQuery` با `PropertyNameCaseInsensitive` اضافه شد.
4. **ویجت گزارش در چیدمان داشبورد ذخیره نمی‌شد**: `ReplaceWidgetsAsync` فقط کلیدهای کاتالوگ ایستا را
   می‌پذیرفت و `rep:{id}` را بی‌صدا حذف می‌کرد → `ResolveWidgetAsync` اضافه شد (اعتبارسنجی مالکیت/اشتراک
   با نقش‌ها + مجوز ماژول داده، و اندازهٔ پیش‌فرض بر اساس نوع خروجی).
5. **تاریخ شمسی در فیلتر Between کار نمی‌کرد**: `DateTime.TryParse` مقدار `1404-01-01` را به‌عنوان
   سال میلادی ۱۴۰۴ می‌خواند → ترتیب تشخیص عوض شد (اول سه‌بخشی عددی: سال ≤ ۱۵۰۰ = شمسی).
6. **ستون‌های وضعیت رشته‌ای فارسی نبودند** → `TextLabels` + `Unlabel` (فیلتر با برچسب فارسی هم کار می‌کند).

## ۸) اجرا / تست دستی

```bash
# API (SQLite برای تست)
cd inventory/src/Inventory.Api
Database__Provider=Sqlite \
ConnectionStrings__Default="Data Source=/tmp/inv.db" \
Database__SeedDemoData=true Database__SeedDemoFinancials=true \
ASPNETCORE_URLS=http://0.0.0.0:5100 dotnet run

# کلاینت
cd inventory/src/Inventory.Client && dotnet run
# ورود با admin/123456 ← منوی «داشبوردها و صفحات شخصی ← گزارش‌ساز»
```

جداول `UserReports` و `UserReportRoleShares` در شروع برنامه با `ReportBuilderSchemaV1.EnsureAsync`
به‌صورت idempotent ساخته می‌شوند (نیاز به migration دستی ندارد).

## ۹) کارهای باقی‌مانده / پیشنهادها

- export به Excel/PDF و چاپ (طبق تصمیم فعلی: خروجی فقط نمایشی است).
- drill-down از ردیف جدول گزارش به صفحهٔ مربوطه.
- کش کوتاه‌مدت نتیجهٔ گزارش‌های پرتکرار روی داشبورد.
- افزودن دیتاست‌های مؤدیان (Moadian) در صورت نیاز (فعلاً طبق تصمیم کنار گذاشته شد).
